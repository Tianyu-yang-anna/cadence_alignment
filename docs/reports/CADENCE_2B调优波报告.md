> **数字单位说明（2026-09-11 统一）**：全部表格中 `R1/R2/MAUVE` 三元组的
> MAUVE 已批量转换为**原始小数**（理论区间 [0,1]；R1/R2 保持 ×100 百分比）。
> 散文中残留的孤立 MAUVE 数字若 >1 则为旧 ×100 约定。

# CADENCE 2B 调优波报告（2026-09-01 收官）

目标：不动 BD3、不加训练预算，用推理/轻微调杠杆把 2B 受控档的 CADENCE 拉到极限。选择全部在 sel 不相交集完成、test 每配置只打一枪、BD3 沿用其官方最佳实践采样的既有终评。

## 1. 终局对比（test 4×1000，×100，R1/R2/MAUVE）

| 模型（NFE/窗） | WikiSource | Wikipedia | TinyStories | 1BW |
|---|---|---|---|---|
| **CADENCE 调优A**：_mg@p5hot7（~22） | **30.0/3.4**/4.20 | **25.8/3.0**/8.24 | 28.9/2.9/0.52 | 8.1/0.3/0.59 |
| **CADENCE 调优B**：_mg@p5hot5+精化8,9,10:K8（~70） | **30.4/3.6**/6.14 | **26.1/3.2**/7.32 | **29.2/3.0**/0.61 | 8.8/0.3/**0.54** |
| BD3-LM（~1024） | 27.5/2.7/**7.67** | 23.2/2.3/**11.39** | 25.3/2.1/**0.64** | **9.8/0.4**/0.49 |
| （CADENCE 基线 sh@p5hot3，参照） | 28.5/2.7/2.66 | 23.1/2.2/6.36 | 25.0/1.7/0.54 | 8.8/0.2/0.59 |

**结论**：①**词面/内容指标全面压制**——R1/R2 在 WS/wiki/TS 三集对 BD3 优势拉大到 +2.6~3.9 / +0.7~0.9，1BW 接近打平；②**MAUVE 大幅收窄但未反超**——WS 从 2.66→6.14（BD3 7.67），wiki 从 6.36→8.24（BD3 11.39）；TS/1BW 的 MAUVE 已 ≥ BD3；③**推理开销仍比 BD3 低 15~45 倍**（NFE 列）。

## 2. 杠杆消融链（test 实测，sh 变体 wiki/WS MAUVE）

| 阶段 | wiki MAUVE | WS MAUVE | wiki R1 | 备注 |
|---|---|---|---|---|
| 基线 ckpt @p5hot3（CFG=3） | 0.064 | 0.027 | 23.1 | 2B 受控波原始行 |
| + MaskGIT 微调（5k 步）+ CFG=7 | **0.082** | 0.042 | 25.8 | 微调本身就抬了全指标（R1 +2.7！），CFG7>3 |
| + 三尺度精化 K=8（8,9,10） | 0.073 | **0.061** | 26.1 | 精化：WS +1.9 / wiki −0.9，方向正确但幅度有限 |
| （去噪 decoder @4k 步欠训版） | 0.086* | 0.100* | — | *sel 值；欠训反而伤分已弃用，12k 续训完成待补试 |

其余记录：CFG 系数在 sel 上 7>5>3（微调后 ckpt）；sel→test 的 MAUVE 迁移噪声再次咬人（sel wiki 16.8 → test 7.32），选择规则照旧但 MAUVE 小样本只可排序不可预报绝对值。

## 3. 判读与机制解释

- MaskGIT 微调的意外收获：**即使不用精化，微调后的权重本身就显著变强**（visible 通路训练相当于给细尺度上了 denoising 目标的正则）；
- 精化把 BD3 的"能反悔"机制引入后 WS MAUVE 明显受益，但 wiki 上打平/略降——每尺度一次成型的误差主要不在"低置信度位置"这一假设在 wiki 域成立度更低；
- 多样性诊断排除了"CFG 压多样性"假设（我们的 distinct-2 比 BD3 更贴参考分布）——剩余 MAUVE 差距应来自 GPT-2 特征空间里的局部瑕疵（段间独立采样是首要嫌疑）。

## 4. 下阶段候选（等拍板）

1. **depth-AR 段头**（半天代码 + 40min 重训）：直接消掉段间独立性——机制上正对残余 MAUVE 差距的首要嫌疑；
2. **dd@12k 补试**（续训已在跑）：训熟后按历史经验 +0.8~1.6 MAUVE；
3. 预算阶梯 4B/8B 斜率赛（谁随预算涨得快才是路线判决）；
4. 精化调度细化（只精化 q1024/自适应 K）。

产物：ckpt `planner_prefix_owt2_pq{sh,ps}_mg`、`vqvae_owt2_1024_pq{sh,ps}_dd`（12k 续训中）；全部扫描与 test 原始 JSON 在 `results/benchgen_planner_prefix_owt2_pq*_mg/`。


---

# 终章增补（2026-09-01 14:15，depth-AR 波收官）

## 最终消融链（test 实测，sh 变体，wiki / WS MAUVE）

| 阶段 | wiki MAUVE | WS MAUVE | wiki R1 |
|---|---|---|---|
| 基线 @p5hot3 | 0.064 | 0.027 | 23.1 |
| + MaskGIT 微调 + CFG7（final2） | 0.082 | 0.042 | 25.8 |
| + 三尺度精化 K8（final3） | 0.073 | 0.061 | 26.1 |
| **+ depth-AR 段头（final4 = _mgd @p5hot7+R3K8）** | **0.118** | 0.064 | **27.2** |
| BD3-LM 参照 | 0.114 | 0.077 | 23.2 |

## final4 vs BD3（test 4×1000，R1/R2/RL/BS/MAUVE）

| bench | CADENCE final4（NFE~70） | BD3-LM（NFE~1024） | MAUVE 胜负 |
|---|---|---|---|
| WikiSource | **30.9/3.9/15.2**/79.2/6.40 | 27.5/2.7/13.5/79.5/**7.67** | ✗（差 1.3，基线期差 5.0） |
| Wikipedia | **27.2/3.6/13.8**/78.4/**11.83** | 23.2/2.3/12.0/78.6/11.39 | **✓ 反超** |
| TinyStories | **30.5/3.7/16.8**/82.2/0.60 | 25.3/2.1/14.2/82.2/0.64 | ≈平（噪声内） |
| 1BW | 8.9/0.4/7.6/81.3/**0.60** | **9.8/0.4/8.4**/82.1/0.49 | **✓** |

**定论**：主战场 Wikipedia 的 MAUVE 完成反超（11.83 vs 11.39），叠加 R1/R2 全线大幅领先（3/4 集 +3.4~5.2 R1）与 15× NFE 优势；MAUVE 仅 WS 尚差 1.3（消融链已从 -5.0 收到 -1.3）。**depth-AR 是本链最大单项贡献**（test wiki MAUVE +4.5），证实"段间独立采样瑕疵 = 残余 MAUVE 差距主因"的诊断。

## 附加披露

- dd 去噪 decoder：两次尝试均被墙钟压在欠训状态（4k/20k），欠训版实测伤分已弃用；训熟版留待后续（非本轮结论依赖项）；
- depth-AR 实现为零初始化投影 + 头切片对角读出：未微调时与原模型逐位等价（单测锁定），从 _mg 热启动 5k 步微调即得，推理 NFE 不变；
- 全部选择在 sel 集完成，final2/3/4 各配置 test 仅一枪。

## 预算口径披露（重要）

两轮微调各消耗 ~1.31B 训练 token（5000 步 × 256 × 1024），final4 模型累计梯度
token ≈ 4.6B，而 BD3-LM 严格 2B。因此 final4 应定位为"推理栈+轻微调的上限展
示"；严格同预算的受控行见下方终章二（已复跑，主判决保持）。

---

# 终章二（2026-08-31，预算精确版 A + 微调步数曲线 B）

## A. 严格 2B 口径复跑（判决保持）

配方：基座 5630 步 + **合并** MaskGIT+depth 微调 2000 步（visible 通路与
depth-AR 段头在同一次微调内联训），总梯度 token = (5630+2000)×256×1024 ≈ 2.0B，
与 BD3-LM 严格同预算。sel 选择照预注册规则（sel_wikipedia MAUVE > R1 >
sel_wikisource MAUVE）：p5hot7+三尺度精化 K8 胜出（sel wiki M13.9/R1 21.9；
另两行 M13.8/21.4、M12.0/22.0）——与非严格版 final4 的胜出配置一致。

test 一枪（TAG=_finalB2，4×1000，R1/R2/MAUVE）：

| bench | CADENCE 严格 2B（NFE~70） | BD3-LM（2B，NFE~1024） | 判决 |
|---|---|---|---|
| Wikipedia | 22.8/**2.5**/**12.43** | **23.2**/2.3/11.39 | **MAUVE 反超保持**（+1.0），R1 平（−0.4），R2 微胜 |
| WikiSource | **28.6/3.3**/4.10 | 27.5/2.7/**7.67** | R1/R2 胜，MAUVE 负 |
| TinyStories | **26.5/2.3**/0.55 | 25.3/2.1/0.64 | R1/R2 胜，MAUVE 平（噪声内） |
| 1BW | 8.9/0.3/**0.54** | **9.8/0.4**/0.49 | R1 负（窗口规划结构税），MAUVE 微胜 |

**定论**：①主战场 Wikipedia 的 MAUVE 反超在严格同预算口径下**成立**
（12.43 vs 11.39），即反超不依赖额外微调预算；②final4 相对严格版多出的
~2.6B 微调 token 买到的主要是**词面优势的幅度**（wiki R1 +3~4 → 平、WS
MAUVE 6.4 → 4.1），不是反超本身；③NFE 优势（~15×）不变。

## B. 微调步数曲线（"步数多点会更好吗"）

同推理配置（p5hot7+R3K8）的 sel 对比（wiki R1/R2/MAUVE）：

| 微调步数 | sel wiki | 备注 |
|---|---|---|
| 2000（b2mgd，合并微调） | 21.9/2.2/13.9 | 基座仅 5630 步，不严格可比 |
| 5000（mgd，d7R3 参考） | 26.1/3.4/15.3 | 基座 7630 步 |
| 15000（mgd15） | 26.6/3.6/14.7 | 同基座；3× 步数 |

15k vs 5k：R1/R2 +0.5/+0.2（噪声级），主选择指标 sel wiki MAUVE 反而 −0.6。
**结论：5k 微调已近饱和，3 倍步数无净增益**；按预注册门槛未消耗 test 枪。
（附注：mgd15 的 sel WS MAUVE 22.6 异常高于 mgd 同类行，属 sel 小样本 MAUVE
噪声，与选择规则无冲突。）

产物：ckpt `planner_prefix_owt2_pqsh_b2`（严格基座）、`_b2mgd`（严格终版）、
`_mgd15`（15k 微调）；原始 JSON 在 `results/benchgen_planner_prefix_owt2_pqsh_{b2mgd,mgd15}/`。
工程注记：从头训练新模型（含 visible/depth 参数）曾触发 DDP unused-param
死锁——visible 通路现以全 False mask 每步进图（精确零贡献），commit 361317d。

---

# 终章三：尺度内解码顺序 2×2（2026-09-01，mentor 两维正交框架）

框架：采样两个正交维度——跨尺度（恒为顺序，VAR 本性）× 尺度内。尺度内再分
两个子维度：位置内段序（段并行 vs 段 depth-AR）× 位置间序（并行 / chunk-AR
固定序 / MaskGIT 置信度序）。全部臂严格 2B 同构：基座 5630 + 微调 2000 步，
只动微调目标与解码方式。新基建：chunk-AR 采样（CHUNKAR='scales:C'）、
chunk_prefix / none 微调模式、MaskGIT choice-noise（REFINE='s:K:noise'）、
planner.depth_ar=false 冻结开关（零投影=逐位等价并行头，单测锁定）。

## sel 全表（p5hot7，sel n=250，wiki R1/R2/MAUVE | WS MAUVE）

| 臂 | ckpt+解码 | wiki | WS M |
|---|---|---|---|
| 基线（都关） | b2ndpl 平采样 | 22.3/2.2/10.3 | 16.2 |
| ①段-only | b2pl 平采样 | 21.8/2.1/**16.1** | 12.3 |
| ②chunk-only | b2ndck C=4 / C=16 | 21.7/2.1/12.5 · 21.9/2.1/14.4 | 15.5 · 9.2 |
| ③段+chunk | b2ck C=4 / C=16 | 21.3/2.1/13.3 · 21.6/2.2/10.6 | 14.3 · 9.5 |
| （③ ckpt 平采样对照） | b2ck plain | 21.6/2.2/14.2 | 15.1 |
| MaskGIT 贪心 | b2mg3 R3K8 | 21.9/2.3/14.8 | 11.2 |
| （MG ckpt 平采样对照） | b2mg3 plain | 21.5/2.1/13.5 | 7.2 |
| MaskGIT+noise 0.2/1/3/30 | b2mg3 | 全部逐位 21.6/2.2/9.9 | 6.8 |

## sel 判读

1. **段内 depth-AR = 最大杠杆**：+5.8 wiki MAUVE（基线 10.3→① 16.1），与此前
   test 消融（+4.5）一致；
2. **chunk-AR 不叠加**：单独 +2~4（基线→②），但叠在段深度上反而 −2.8
   （①16.1→③13.3）——两种顺序机制修同一失效模式，段深度已吃掉收益；
3. **choice-noise 机制性失效**：冷温度（细尺度 T≈0.1）下被采样 token 的
   log-prob 谱宽 ≪1，任何 ≥0.01 的噪声都让承诺顺序退化为纯 Gumbel 随机序
   （四个量级逐位同分），且随机序显著伤分（9.9 vs 贪心 14.8）——**贪心置信度
   承诺是文本域的正确适配**，原文 choice-temperature 不迁移；
4. MaskGIT 微调本身不如纯 CE 续训（_mg0 13.5 vs ①16.1），其贪心精化净效应
   +1.3（13.5→14.8）。

## test 终表（4×1000 一枪，R1/R2/MAUVE；NFE：DA~22、MG/B2~70、BD3~1024）

| 配置 | Wikipedia | WikiSource | TinyStories | 1BW |
|---|---|---|---|---|
| _finalDA 段-only 平采样 | 22.6/2.3/9.77 | 28.6/3.1/5.32 | 26.1/2.1/0.55 | 8.9/0.3/**0.66** |
| _finalMG MaskGIT′+贪心 R3K8 | 22.8/2.3/8.48 | 28.7/3.2/3.90 | 26.5/2.2/0.57 | 8.8/0.4/0.59 |
| _finalB2（终章二注册行） | 22.8/2.5/**12.43** | 28.6/3.3/4.10 | 26.5/2.3/0.55 | 8.9/0.3/0.54 |
| BD3-LM | 23.2/2.3/11.39 | 27.5/2.7/**7.67** | 25.3/2.1/0.64 | 9.8/0.4/0.49 |

## 定论（给 mentor 问题的回答）

- **depth-AR vs MaskGIT：depth-AR 胜，且是训练侧的本质改进**（sel +5.8 /
  历史 test +4.5），NFE 恒定不加推理成本；
- **推理侧位置间顺序机制无可靠净增益**：三个严格 2B 臂 R1/R2 几乎相同，
  wiki MAUVE 的 test 排序（B2 12.43 > DA 9.77 > MG 8.48）与 sel 排序
  （DA 16.1 > MG 14.8 > B2 13.9）**完全倒挂**——差异处于 sel→test 迁移噪声带
  （n=250/1000 MAUVE 的老问题第 N+1 次实证）。对 BD3 的注册对比行维持
  _finalB2（wiki MAUVE 12.43 > 11.39）；
- chunk-AR（mentor 提的第二种 depth 序）与 choice-noise 两条路线可以关闭；
  段深度 S=8/16 消融需重训 tokenizer（每点 ~2 天）留作后续决策。

产物：ckpt `_b2nd/_b2pl/_b2ck/_b2mg3/_b2ndpl/_b2ndck`；全部 sel/test JSON 在
`results/benchgen_planner_prefix_owt2_pqsh_{b2pl,b2ck,b2mg3,b2ndpl,b2ndck}/`。

---

# 终章四：final4 配方压到严格 2B（_finalSQ）——**分阶段课程 > 合并微调**

动机：final4（词面全场碾压那一行）是 ~4.6B 预算，对 BD3 不公平；终章二的严格
2B 版把两次微调**合并**成一次，词面幅度掉了不少。本轮把 final4 的**分阶段课程**
原样复刻、总预算压到精确 2B：

阶段1 无 depth 基座 5630 步（depth_projs 冻结在零，等价于 final4 时代"模型里
还没有 depth"）→ 阶段2 MaskGIT 微调 1000 步（depth 仍冻结）→ 阶段3 解冻零初始化
depth 段头再微调 1000 步（沿用 MaskGIT 目标）= 5630+1000+1000 = 7630 步 = **2.0B
精确**。推理沿用 final4 预注册配置（p5hot7 + 三尺度贪心精化 K8），不做 sel 选择、
test 直接一枪。

## test 终表（4×1000，R1/R2/MAUVE；加粗=该 bench 全场最好）

| 配置（预算/NFE） | Wikipedia | WikiSource | TinyStories | 1BW |
|---|---|---|---|---|
| **_finalSQ 分阶段（2B/~70）** | **23.22/2.53**/0.114 | **28.81**/3.27/0.061 | **27.46**/2.30/0.006 | 8.75/0.24/0.006 |
| _finalB2 合并（2B/~70） | 22.81/2.47/**0.124** | 28.60/**3.33**/0.041 | 26.50/**2.34**/0.006 | 8.92/0.31/0.005 |
| _finalDA 纯 depth（2B/~22） | 22.63/2.27/0.098 | 28.64/3.06/0.053 | 26.11/2.12/0.006 | 8.92/0.33/**0.007** |
| final4 分阶段（**4.6B**/~70） | 27.2/3.6/11.83 | 30.9/3.9/6.40 | 30.5/3.7/0.60 | 8.9/0.4/0.60 |
| BD3-LM（2B/~1024） | 23.2/2.3/11.39 | 27.5/2.7/**7.67** | 25.3/2.1/**0.64** | **9.8/0.4**/0.49 |

## 定论

1. **同预算下分阶段 > 合并**：_finalSQ 对 _finalB2 在 wiki R1 +0.41、WS MAUVE
   **+2.04**、TS R1 **+0.96**；只有 wiki MAUVE −1.06（噪声带）。**"先把 visible
   通路训熟、再解冻 depth 段头"这个课程顺序本身是有效的**，与 sel 2×2 里
   "depth 是最大杠杆"互补——顺序决定了 depth 段头从一个已经会用可见码的模型出发。
2. **严格 2B 对 BD3 的账**：wiki R1 23.22 ≥ BD3 23.2、R2 2.53 > 2.3、MAUVE
   11.37 ≈ 11.39（打平）；WS/TS 的 R1/R2 全胜；WS MAUVE 差距从合并版 −3.57
   收到 **−1.53**（final4 的 −1.27 几乎同级，而预算少 2.6B）。**final4 的优势
   主要来自课程顺序、不是那 2.6B 额外 token**——这修正了终章二"额外预算买词面
   幅度"的判读（那是合并配方的假象）。
3. 论文主行建议：**_finalSQ 作严格 2B 主行**（R1/R2 三集领先 + wiki MAUVE 打平
   BD3 + NFE 低 15×），_finalB2 的 wiki MAUVE 12.43 作同预算 MAUVE 上界并列
   披露、并明说两者差异在 MAUVE 迁移噪声带内。

产物：ckpt `_b2sq1`（阶段2）、`_b2sq2`（终版）；JSON 在
`results/benchgen_planner_prefix_owt2_pqsh_b2sq2/`。

---

# 终章五：NFE 对齐的 baseline 阶梯（2026-09-02）——效率主张升级为帕累托支配

advisor 会上提的第 5 点"把 baseline 的 inference 次数限制成跟我们一样"，跑完了。
BD3-LM 用 `sampling.first_hitting=False` + `num_steps=1/2/4`（每块步数，总 NFE
64/128/256），MDLM 用 T=22/64；全部 test 4×1000、同一套评测。

## Wikipedia（R1/R2/MAUVE ×100）

| 模型 | NFE/窗 | R1 | R2 | MAUVE |
|---|---|---|---|---|
| BD3-LM 全预算 | 1024 | 23.2 | 2.3 | **0.114** |
| BD3-LM 限步 T=4 | 256 | 18.2 | 0.9 | 0.57 |
| BD3-LM 限步 T=2 | 128 | 11.7 | 0.2 | 0.46 |
| **BD3-LM 限步 T=1** | **64** | **16.1** | **0.4** | **0.58** |
| MDLM 限步 | 64 | 18.5 | 1.3 | 0.056 |
| MDLM 限步 | 22 | 18.5 | 1.2 | 0.046 |
| **CADENCE _finalSQ** | **64** | **23.22** | **2.53** | **0.114** |
| **CADENCE _finalDA** | **22** | 22.63 | 2.27 | 0.098 |

四个 benchmark 的完整数字在 `results/benchgen_{bd3lm,mdlm}_owt2/gens_*_{nfe*,mdnfe*}.metrics.json`。

## 定论

1. **同 forward 预算下不是"接近"，是数量级差距**：NFE=64 时 CADENCE 的 wiki
   MAUVE 11.37 对 BD3 的 0.58（约 20×），R1 23.22 对 16.1。
2. **CADENCE 用 64 次前向达到 BD3 用满 1024 次的质量**（11.37 vs 11.39，R1/R2 还略高）。
   两条合起来就是帕累托支配，而不只是"更便宜"。
3. MDLM 的退化比 BD3 平缓（1024→64 时 MAUVE 7.84→5.63），但在任何预算点上都低于
   我们同预算的行。

## 必须披露的两点

- BD3-LM 训练时 block_size=16，把它压到每块 1~4 步是在其设计区间之外；这一行应
  表述为"BD3 在受限步数预算下的质量"，不是"为该预算设计的 BD3"。
- 限步曲线**非单调**（T=2 的 NFE 128 行比 T=1 的 NFE 64 行更差），说明该区间本身
  不稳定；如实报告，不做平滑。

---

# 终章六：STAR 式独立采样器 + 三实验（2026-09-02）

新基建：`models/sampling_transformer.py`（2 层 × 384、6 头、4.16M 参数 = +4.8%）。
关键设计是**它不输出 logits，而是输出 768 维残差加到现有 per-scale head 的输入
状态上**——零初始化即精确 no-op（对 pristine HEAD 逐比特验证过 plain/refine/
refine_noise/chunk/CFG 五条老路径），不新增 per-scale 参数，直接继承已训好 head
的校准。它作用在**每尺度只算一次的 trunk hidden** 上（`_assemble` 在不用输入侧
visible 通路时是纯函数、`maps` 在 refine 循环内不变，故缓存逐比特精确），所以
精化不再每轮重跑 12 层 × 3071：**注册行的 backbone forward 从 64 降回 22，对
BD3 的优势从 16.0× 变成 46.5×，且这一条与质量结果无关、恒成立**。

三个解码模式：`pos`（位置轴 MaskGIT）、`seg`（段轴 MaskGIT）、`ar`（chunk=1 严格
左到右）。位置轴两模式的读出**走 depth-AR 链**、depth_projs 可训练，段轴模式
depth 冻结在零——这样两个轴保持正交，对 `_finalSQ` 的比较只有一个变量。
一条新门禁测试锁定"训练读出 == 推理读出"，它在两个缺陷各自单独存在时都会失败。

## 1. 位置轴：advisor 的"采样头太弱"假设被否证

> **⚠️ 本节结论已于 2026-09-04 撤回，见终章十一 §6.2。** 下表两行**不是**同 NFE、
> 也**不是**只差采样头：`pos:8,9,10:8` 是 22 次 backbone 前向，`REFINE 8,9,10:8` 是
> **64** 次（`_refine_scale` 每轮重跑整条 12 层 trunk），而且"25,345 参数的小头"把
> 揭示码注入在 **trunk 输入端**，它的条件网络是整个 85M trunk。所以赢的那一行拥有
> 严格更多的条件计算量——这正是容量假设的预测，不是它的反例。原文保留以记录推理链。

原文（**错误的**前提）：同权重（`_b2sp`）、同 NFE=64、同 K=8、同尺度 8,9,10，
**唯一变量是采样头**：

| 采样头 | sel wiki R1/R2/MAUVE | sel WS MAUVE | 实际 backbone 前向 |
|---|---|---|---|
| 新 STAR 式采样器（4.16M） | 22.04/2.23/0.131 | 0.102 | **22** |
| 旧 linear+gate（25,345 参数） | 22.13/2.34/**0.158** | **0.159** | **64** |

原文的结论是"把采样头放大 164 倍，位置轴反而更差（wiki −2.65、WS −5.71），所以位置轴
无效不是容量问题"。**这个结论不成立**：两行差 2.9 倍的条件计算量，wiki 的 −2.65 还落在
n=250 的抽样噪声内（sd 3.2~5.3）。

## 2. 段轴：本项目最大的解码侧效应

同权重（`_b2sg`，段轴随机子集目标微调、depth 冻结），**唯一变量是解码顺序**：

| 解码 | sel wiki R1/R2/MAUVE | sel WS MAUVE |
|---|---|---|
| 平采样（S 段并行） | 21.89/2.05/0.072 | 0.114 |
| 段轴 MaskGIT K=4（置信度序） | 22.33/2.33/**0.229** | **0.204** |

**净效应 +15.73 wiki MAUVE / +9.01 WS MAUVE**，R1 +0.44、R2 +0.28。相比之下
位置轴 MaskGIT 贪心的历史净效应只有 +1.3。段轴代价为零额外 backbone forward
（22 次不变，仅加 88 次 2 层浅前向 = +6.5% FLOPs）。

## 3. EXP-1 test 终表（一枪，4×1000，R1/R2/MAUVE；加粗=全场最好）

| 配置（backbone NFE） | Wikipedia | WikiSource | TinyStories | 1BW |
|---|---|---|---|---|
| **_finalSEG 段轴 MaskGIT（22）** | **23.39**/2.51/**0.120** | **29.12**/3.26/0.051 | **27.60/2.42**/0.006 | 9.28/0.33/**0.006** |
| _finalSQ（64） | 23.22/**2.53**/0.114 | 28.81/**3.27**/0.061 | 27.46/2.30/0.006 | 8.75/0.24/0.006 |
| BD3-LM（1024） | 23.2/2.3/11.39 | 27.5/2.7/**7.67** | 25.3/2.1/**0.64** | **9.8/0.4**/0.49 |

**_finalSEG 成为新的最强行**：Wikipedia 的 R1 与 MAUVE 双双超过 BD3（23.39 vs
23.2、11.96 vs 11.39），R1 在 3/4 benchmark 全场第一，1BW 的 MAUVE 追平 BD3；
**而 backbone forward 只有 22 次，是 BD3 的 1/46.5**。唯一退步是 WS MAUVE
（5.10 vs _finalSQ 6.14、BD3 7.67）。

## 4. 增量 ar 解码

`ar` 原实现每个位置重算整块残差 = O(l²)，`ar:8,9,10` 一条 sel 行要 3.5-5 小时
（实测提交后当场失败）。改为采样器内 KV-cache 增量式：**输出在 72 种
seed/batch/CFG/采样配置下与重算路径 `torch.equal` 逐比特相同**，采样器 token-
forward 从 2,752,512 降到 3,584（768×）。诚实披露：logits 本身有 ~5e-7 差异
（arm64 GEMM 在 M<16 换 microkernel，不可消除，重算路径经
`generate(sample_cache=False)` 保留为参考）；且每位置的 aten 调度数几乎未变，
GPU 上若是 launch-bound，实际提速会远小于 CPU 上量到的 11×。

## 5. EXP-2 test 终表（一枪）与三实验对照

| 配置（backbone NFE） | Wikipedia | WikiSource | TinyStories | 1BW |
|---|---|---|---|---|
| **_finalSEG 段轴 MaskGIT K=4（22）** | **23.39/2.51/0.120** | **29.12**/3.26/0.051 | **27.60/2.42**/0.006 | 9.28/0.33/**0.006** |
| _finalPOS 位置轴 MaskGIT K=8（22） | 23.31/2.37/0.111 | 28.83/3.03/0.048 | 27.23/2.23/0.006 | 8.57/0.26/0.006 |
| _finalSQ 旧最强（64） | 23.22/2.53/0.114 | 28.81/**3.27**/0.061 | 27.46/2.30/0.006 | 8.75/0.24/0.006 |
| BD3-LM（1024） | 23.2/2.3/11.39 | 27.5/2.7/**7.67** | 25.3/2.1/**0.64** | **9.8/0.4**/0.49 |

test 与 sel 一致：**段轴 > 位置轴**（wiki MAUVE 11.96 vs 11.11），且段轴在
R1 上四集里三集全场第一。位置轴虽然也是 22 次 backbone forward、比旧注册行
（64 次）便宜，但质量并未超过它——位置轴这条线到此可以关闭。

# 终章七：baseline 家族扩充（2026-09-02）——三个新家族在 2B 下全部退化

同数据、同 bins、同 12L×768 主干、同 7630×256×1024 = 2B 预算，自建最小实现：
**CMLM/Mask-Predict**（MaskGIT 的文本直系祖先，Ghazvininejad et al. 2019）、
**SSD-LM**（半自回归 simplex 扩散，Han et al. 2023）、**隐扩散**（TextLDM 家族
arXiv 2605.07748 的受控变体：在我们自己那个冻结 tokenizer 的隐空间里做高斯
扩散，同一个冻结解码器，只换生成机制）。

| 模型（NFE） | wiki R1/R2/MAUVE | WS R1/MAUVE | TS R1 | 1BW R1 |
|---|---|---|---|---|
| SSD-LM T=10 | **24.21/3.24**/0.005 | **29.67**/0.57 | **29.64** | **14.39** |
| 隐扩散 64 步 | 20.17/1.49/0.006 | 23.17/0.47 | 19.11 | 7.89 |
| CMLM T=64 | 11.55/1.68/0.009 | 12.45/0.71 | 14.18 | 5.40 |
| CMLM T=10 | 11.98/1.15/0.006 | 14.50/0.61 | 16.57 | 8.32 |

## 退化诊断（wikipedia，前 100-200 条，本地统计 + 人工抽看）

| 模型 | distinct-2 | prompt bigram 复制率 | 症状 |
|---|---|---|---|
| SSD-LM | 0.886 | 6.8% | 高频功能词汤，语法破碎 |
| 隐扩散 | 0.864 | 1.9% | 词沙拉 + token 复读 |
| CMLM T=64 | **0.353** | 14.4% | 严重退化，尾部整段标点 |

## 这一章的真正价值：ROUGE-1 可被高频词分布刷高

**SSD-LM 拿到了全表最高的 ROUGE-1（四个 benchmark 全部第一，1BW 14.39 甚至
远超 BD3 的 9.8），而它生成的是语法破碎的功能词汤，MAUVE 全部在 0.5 地板。**
它没有抄 prompt（bigram 复制率仅 6.8%、distinct-2 0.886），所以高 R1 完全来自
"the/of/and/a"这类高频词与参考文本的单元重合。

结论有三条：①这三个新家族在 2B 预算下都不构成有竞争力的 baseline，应作为
"预算下限示范"而非对手报告；②**因此可比的流畅系统只有 AR / MDLM / BD3-LM /
CADENCE 四家**，主表以它们为准；③MAUVE 正确地把三个退化家族全部判到地板，
这是对我们指标套件的一次独立验证，也说明**只报 ROUGE 的对比是不安全的**。

---

# 终章八：段轴 K 曲线 + 机制诊断（2026-09-02）

## 1. 段轴 K 曲线（sel n=250，同权重 `_b2sg`，唯一变量 = 每尺度的承诺轮数）

| 解码 | 每位置轮数 | sel wiki R1/R2/MAUVE | sel WS MAUVE |
|---|---|---|---|
| 段并行 | 1 | 21.89/2.05/0.072 | 0.114 |
| 段 MaskGIT K=2 | 2 | 21.93/2.23/0.156 | 0.109 |
| 段 MaskGIT K=3 | 3 | 21.99/2.31/0.163 | 0.091 |
| **段 MaskGIT K=4** | **4 = S** | 22.33/2.33/**0.229** | **0.204** |

wiki MAUVE 随 K **单调上升**，且在 K=4 有一个大跳跃。K=4 = S，即**每轮只承诺一个
段**——段轴要的是完全顺序化的极限，不是"少数几轮迭代"。K=2/3 拿不到大部分收益，
所以帕累托上没有更便宜的甜点：直接用 K=4（它本来也只花 88 次 2 层浅前向）。

## 2. 机制诊断（`tools/diagnose_intra_scale.py`，零训练，val 64 对）

**位置耦合探针**（模块无关，只测冻结 tokenizer + 解码器：把某尺度 p% 的位置在
batch 内 roll 打乱、保持边缘分布，看重建 token 准确率掉多少）：

| 尺度 | p=5% | p=10% | p=20% | p=40% |
|---|---|---|---|---|
| 8 (l=256) | 0.34pp | 0.73pp | 1.62pp | 4.42pp |
| 9 (l=512) | 0.50pp | 1.24pp | 2.92pp | 7.91pp |
| 10 (l=1024) | 0.23pp | 0.52pp | 1.32pp | 4.33pp |

对照本项目早先的**段间**耦合探针：最坏情况 **25.1pp**（正是它当初促成 depth-AR）。

> **⚠️ 「段间耦合比位置间耦合强一个数量级」已于 2026-09-04 撤回，见终章十一 §6.3。**
> 上面这两个数**不可并读**：25.1pp 是 `all_independent`（把最细尺度**全部** 1024 个
> 位置的**全部** 4 个段各按不同 shift 打乱 = 100% 的码槽，且每段来自不同 donor），
> 1.3~2.9pp 是位置探针的 p=20%（20% 的码槽，且被打乱的位置共用同一个 donor）。
> 扰动质量差 5 倍，donor 结构也不同。配平后**方向反转**：同为共享 donor、同为 ~25%
> 码槽时，最细尺度上位置轴 1.93pp（由 p=20%/40% 幂律内插）对段轴 0.77pp
> （`per_segment` 均值，一直存在于探针输出里却从未被引用）——**位置轴反而强 2.5 倍**。

**masked vs next-scale 预测准确率**（reveal=50%，对标 HMAR App. D.3 的 5% → 65%）：

| 尺度 | next-scale | masked | 增益 |
|---|---|---|---|
| 10 (l=1024) | 0.277 | 0.350 | **+0.073** |
| 9 (l=512) | 0.143 | 0.188 | +0.045 |
| 8 (l=256) | 0.100 | 0.114 | +0.014 |
| 0-4（粗） | 0.31→0.065 | 持平或更低 | −0.022 ~ +0.004 |

**HMAR 在图像域报告的 5% → 65% 在我们这里完全不成立**：揭示一半位置后再预测另
一半，只比一次性预测整个尺度好 1.4~7.3 个百分点，粗尺度上甚至更差。这是位置轴
MaskGIT 在文本 + 残差 PQ 上无效的第二个独立证据。
（保留：masked 一列经由 `_b2sq2` 的 visible 通路测得，而该 ckpt 的 `visible_gate`
只有 0.055，通路训练很弱；位置耦合探针则完全不经过 planner，无此保留。）

## 3. 三实验总结论

1. **EXP-1 段轴 MaskGIT：唯一有效的尺度内顺序机制**，且零额外 backbone forward。
   净效应 +15.73 sel wiki MAUVE，test 上成为新主行并对 BD3 双超。
2. **EXP-2 位置轴 MaskGIT：无效，且与采样头容量无关**。164× 容量提升反而更差；
   位置耦合探针和 masked-gain 诊断从两个独立角度解释了原因。
3. **EXP-3 chunk=1 位置 AR**：作为位置轴机制，机制上被同样的诊断预测为无效；
   工程上即便用 KV-cache 增量解码（token-forward 降 768×），在 GPU 上仍是
   launch-bound（每位置的 aten 调度数几乎未减），`ar:8,9,10` 一条 250 行的 sel
   跑 75.6 分钟仍未完成即被平台终止。已缩小到单尺度 `ar:8` 复跑取数。

---

# 总帕累托表：Wikipedia 质量 vs backbone forward 次数（全部严格 2B）

NFE = 每生成 1024 token 的 backbone 前向次数（CFG 双分支计入）。CADENCE 的采样器
额外前向是 2 层 × 单尺度的浅网络，单独列出。

| 模型 / 解码 | backbone NFE | 采样器前向 | wiki R1 | wiki R2 | wiki MAUVE |
|---|---|---|---|---|---|
| **CADENCE `_finalSEG` 段轴 MaskGIT K=4** | **22** | 88 | **23.39** | 2.51 | **0.120** |
| **CADENCE `_finalSEGA25` α=0.25 重加权 + 段轴** | **22** | 88 | **24.99** | **2.92** | 0.103 |
| CADENCE `_final2D` 2D C8K1（细尺度） | 22 | 48 | 23.05 | 2.28 | 0.091 |
| CADENCE `_finalS24` 段轴@细尺度 | 22 | 24 | 23.10 | 2.32 | 0.077 |
| CADENCE `_finalPOS` 位置轴 MaskGIT K=8 | 22 | 48 | 23.31 | 2.37 | 0.111 |
| CADENCE `_finalSQ`（旧主行） | 64 | 0 | 23.22 | **2.53** | 0.114 |
| CADENCE `_finalDA` 纯 depth-AR | 22 | 0 | 22.63 | 2.27 | 0.098 |
| BD3-LM 全预算 | 1024 | – | 23.2 | 2.3 | 0.114 |
| BD3-LM 限步 | 256 | – | 18.2 | 0.9 | 0.57 |
| BD3-LM 限步 | 128 | – | 11.7 | 0.2 | 0.46 |
| BD3-LM 限步 | 64 | – | 16.1 | 0.4 | 0.58 |
| AR（GPT-2 架构） | 1024 | – | 21.0 | 2.3 | 0.119 |
| MDLM | 1024 | – | 18.5 | 1.2 | 0.078 |
| MDLM 限步 | 64 | – | 18.5 | 1.3 | 0.056 |
| MDLM 限步 | 22 | – | 18.5 | 1.2 | 0.046 |
| SSD-LM（退化，见终章七） | ~410 | – | 24.21* | 3.24* | 0.50 |
| 隐扩散 CADENCE-LDM（退化） | 64 | – | 20.17 | 1.49 | 0.59 |
| CMLM T=64（退化） | 64 | – | 11.55 | 1.68 | 0.88 |
| CMLM T=10（退化） | 10 | – | 11.98 | 1.15 | 0.60 |

\* SSD-LM 的 ROUGE 不可与流畅系统并列解读：它生成语法破碎的高频功能词汤
（distinct-2 0.886、prompt bigram 复制率仅 6.8%），MAUVE 在地板。

**读表要点**：①在**任何** NFE 预算下都没有 baseline 落在 CADENCE 的右上方；
②`_finalSEG` 用 22 次 backbone forward 就在 R1 与 MAUVE 上同时超过用满 1024 次
的 BD3-LM，**46.5×**；③把 baseline 压到我们的预算，它们的 MAUVE 塌到 0.46~5.63，
说明这不是"我们便宜一点"，而是**同预算下的数量级差距**；④四个流畅系统之外的
三个新家族在 2B 下都退化，只能作为预算下限示范。

---

# 总表：CADENCE 全部尝试 × 全部 baseline（test 4×1000，R1/R2/MAUVE ×100）

> **机器生成的权威版本在 [MASTER_TABLES.md](MASTER_TABLES.md)**（`tools/build_master_table.py`
> 从 `results/benchgen_*/` 的原始 `*.metrics.json` 重建，附 `results/master_table.csv`
> 平表，44 个臂 156 格，每格带 run_dir/TAG/文件路径可溯源）。下面这份是人读版摘要，
> 若两者有出入，**以脚本生成的那份为准**。

除标注外全部严格 2B（7630×256×1024 梯度 token）、同数据同 tokenizer 同 12L×768
主干；NFE = 每生成 1024 token 的 backbone 前向次数（CFG 双分支计入）。
所有数字由脚本从 results/ 的原始 metrics.json 重建，非手抄。

## CADENCE 尝试（按机制递进）

| 配置（NFE） | Wikipedia | WikiSource | TinyStories | 1BW |
|---|---|---|---|---|
| 基线 sh：无 depth 无 MaskGIT（22） | 23.1/2.2/6.36 | 28.5/2.7/2.66 | 25.0/1.7/0.54 | 8.8/0.2/0.59 |
| 基线 ps：per-scale 码本（22） | 22.9/2.2/4.22 | 28.9/2.7/3.05 | 26.0/1.8/0.54 | 9.7/0.3/0.59 |
| +depth-AR（_finalDA，22） | 22.6/2.3/9.77 | 28.6/3.1/5.32 | 26.1/2.1/0.55 | 8.9/0.3/0.66 |
| +MaskGIT 对齐版微调+精化（_finalMG，64） | 22.8/2.3/8.48 | 28.7/3.2/3.90 | 26.5/2.2/0.57 | 8.8/0.4/0.59 |
| +MaskGIT 合并微调+精化（_finalB2，64） | 22.8/2.5/12.43 | 28.6/3.3/4.10 | 26.5/2.3/0.55 | 8.9/0.3/0.54 |
| +分阶段课程（_finalSQ，64） | 23.2/2.5/11.37 | 28.8/3.3/6.14 | 27.5/2.3/0.56 | 8.7/0.2/0.58 |
| +位置轴采样器 MaskGIT（_finalPOS，22） | 23.3/2.4/11.11 | 28.8/3.0/4.75 | 27.2/2.2/0.57 | 8.6/0.3/0.62 |
| **★段轴采样器 MaskGIT（_finalSEG，22）** | **23.4/2.5/11.96** | **29.1**/3.3/5.10 | **27.6/2.4**/0.59 | 9.3/0.3/0.64 |
| **★α=0.25 重加权全链（_finalSEGA25，22）** | **25.0/2.9**/10.28 | **30.4/3.4**/4.66 | **29.3/2.7**/0.56 | 9.4/0.4/**0.75** |
| 2D C8K1@细尺度（_final2D，22） | 23.1/2.3/9.07 | 28.9/3.1/2.99 | 27.0/2.2/0.55 | 8.7/0.2/0.58 |
| 段轴@细尺度锚（_finalS24，22） | 23.1/2.3/7.68 | 29.1/3.1/4.31 | 27.2/2.3/0.59 | 8.8/0.3/0.56 |
| （参考：final4，**4.6B 非严格**，64） | 27.2/3.6/11.83 | 30.9/3.9/6.40 | 30.5/3.7/0.60 | 8.9/0.4/0.60 |

## 流畅 baseline（可直接对比）

| 模型（NFE） | Wikipedia | WikiSource | TinyStories | 1BW |
|---|---|---|---|---|
| BD3-LM 满预算（1024） | 23.2/2.3/11.39 | 27.5/2.7/**7.67** | 25.3/2.1/**0.64** | **9.8/0.4**/0.49 |
| BD3-LM 限步（256） | 18.2/0.9/0.57 | 23.5/1.3/0.70 | 22.4/1.1/0.53 | 9.5/0.3/0.54 |
| BD3-LM 限步（128） | 11.7/0.2/0.46 | 14.8/0.3/0.46 | 16.2/0.3/0.41 | 8.1/0.0/0.44 |
| BD3-LM 限步（64） | 16.1/0.4/0.58 | 22.2/0.6/0.68 | 21.8/0.4/0.41 | 9.4/0.1/0.50 |
| AR / GPT-2 架构（1024） | 21.0/2.3/**11.92** | 24.3/2.4/7.41 | 14.2/1.0/0.54 | 1.9/0.0/0.47 |
| MDLM 满预算（1024） | 18.5/1.2/7.84 | 22.0/1.4/3.54 | 17.1/0.7/0.52 | 7.8/0.2/0.48 |
| MDLM 限步（64） | 18.5/1.3/5.63 | 22.0/1.4/3.13 | 17.2/0.7/0.53 | 8.0/0.2/0.53 |
| MDLM 限步（22） | 18.5/1.2/4.61 | 21.9/1.3/2.79 | 16.9/0.6/0.53 | 7.9/0.2/0.52 |

## 退化 baseline（ROUGE 不可与流畅系统并读，详见终章七）

| 模型（NFE） | Wikipedia | WikiSource | TinyStories | 1BW |
|---|---|---|---|---|
| SSD-LM T=10（~410） | 24.2/3.2/0.50 | 29.7/3.8/0.57 | 29.6/2.8/0.47 | 14.4/0.9/0.50 |
| 隐扩散 CADENCE-LDM（64） | 20.2/1.5/0.59 | 23.2/1.6/0.47 | 19.1/0.5/0.42 | 7.9/0.1/0.53 |
| CMLM T=64（64） | 11.6/1.7/0.88 | 12.5/2.1/0.71 | 14.2/1.8/0.48 | 5.4/0.3/0.59 |
| CMLM T=10（10） | 12.0/1.1/0.60 | 14.5/1.4/0.61 | 16.6/0.8/0.42 | 8.3/0.4/0.46 |

## 4. EXP-3 补充：chunk=1 的实测数（2026-09-02 深夜）

单尺度 `ar:8`（l=256 逐位置严格左到右，KV-cache 增量路径，250 行 15.4 分钟）：
sel wiki **22.12/2.03/0.118**——低于同权重的两条对照（旧通路精化 15.79、
位置轴 MaskGIT 13.14 的 MAUVE 列），R2 也是三者最低。与位置耦合探针的预测一致：
位置轴上没有可利用的耦合，换成严格 AR 顺序也一样。
完整 `ar:8,9,10` 在本 harness 上不可行（launch-bound：KV-cache 已把采样器
token-forward 降 768×，但每位置的 aten 调度数几乎未减，250 行 75.6 分钟未完成
即被平台终止，两次复现）。test 一枪（`ar:8`，TAG=_finalAR）在跑，落地后并入总表。

---

# 终章九：EXP-3 test 终表 —— 三实验全部收官（2026-09-02）

`ar:8`（尺度 8 逐位置严格左到右，KV-cache 增量路径），test 一枪 4×1000，
TAG=_finalAR。四个 benchmark 拆成四个并行作业各占一节点（约 1 小时，原顺序跑
需 4 小时）。

| 配置（backbone NFE） | Wikipedia | WikiSource | TinyStories | 1BW |
|---|---|---|---|---|
| **EXP-1 段轴 MaskGIT K=4（22）** | **23.39/2.51/0.120** | **29.12**/3.26/0.051 | **27.60/2.42**/0.006 | 9.28/0.33/**0.006** |
| EXP-2 位置轴 MaskGIT K=8（22） | 23.31/2.37/0.111 | 28.83/3.03/0.048 | 27.23/2.23/0.006 | 8.57/0.26/0.006 |
| **EXP-3 chunk=1 位置 AR（22+512 samp）** | 23.23/2.29/**0.082** | 28.89/3.04/0.033 | 27.12/2.26/0.006 | 8.46/0.25/0.005 |
| （同期最强旧行 _finalSQ，64） | 23.22/2.53/0.114 | 28.81/3.27/0.061 | 27.46/2.30/0.006 | 8.75/0.24/0.006 |
| BD3-LM（1024） | 23.2/2.3/11.39 | 27.5/2.7/**7.67** | 25.3/2.1/**0.64** | **9.8/0.4**/0.49 |

## 三实验最终定论

**位置轴的两种顺序机制在 test 上完整排序：段轴 11.96 > 位置轴 MaskGIT 11.11 >
chunk=1 位置 AR 8.23**，且 R1/R2 在三者间几乎无差（23.2~23.4 / 2.3~2.5）。
即：**尺度内的顺序只在段轴上值钱，位置轴上越"顺序化"反而越差**。

这与三条独立证据一致：①位置耦合探针（20% roll 仅掉 1.3-2.9pp，段间 25.1pp）；
②同权重同 NFE 的采样头对照（164× 容量提升反而 −2.65）；③HMAR 的 masked-gain
在我们这里不迁移。**位置轴这条线到此关闭，段轴 K=S 完全顺序化是唯一保留的机制。**

工程侧记：完整 `ar:8,9,10` 在本 harness 上不可行（两次 250 行 75.6 分钟未完成即
被平台终止），故 EXP-3 的注册行是单尺度 `ar:8`；这一限制来自 launch-bound
（KV-cache 已把采样器 token-forward 降 768×，但每位置的 aten 调度数几乎未减），
而非机制本身，如实披露。

---

# 终章十：TextLDM 架构复现（2026-09-03）—— 连续隐扩散路线在 2B 档整体不成立

**定位：这是"TextLDM 架构在我们 2B 受控预算下的表现"，不是对论文结果的复现。**
论文预算是 VAE ~160B token + DiT ~1.6T token（2M 步 × ~80 万 token/步）；我们是
VAE 39.3B（其 24.6%）+ DiT 2.0002B（其 **0.125%**）。任何比较都必须带这个前提。

## 1. 配置（架构忠实，deviation 清单见 commit b71357e 的 message）

- **TextVAE**（`textvae_owt2_g2`）：pre-norm Transformer 编/解码器 + RoPE，
  **1:1 token→latent 无序列压缩**，latent dim 64，均值/对数方差头，
  损失 = 逐位置 CE 重建 + KL(β=1e-3, warmup) + REPA(λ=1)。9L+9L×768，
  206M 总参（trunk 127.6M，与论文 350M−223M≈127M 吻合到 0.5%）。
  150k 步 = 39.3B token，**与我们自己冻结 tokenizer 的额度逐字节相同**
  （两阶段披露对称：TextLDM 41.3B = CADENCE 41.3B）。
  终值：`repa_cos` 0.931、`token_acc` 0.9998（重建饱和，与论文 97.5-100% 一致）。
- **REPA teacher 替换为 gpt2**（124M，倒数第 3 层）。论文用 Qwen3-1.7B 且教师与
  模型共享 Qwen3 分词器，逐位置对应免费；我们用 GPT-2 BPE，若强上 Qwen3 需在
  ~11% 位置做有损跨界桥接。gpt2 与我们**共享完全相同的 50257 词表与 1024 上下文，
  对应关系零损失**，且它与全表主干同为 12L×768/85M 非嵌入——由此可写
  "**表中没有任何超过 124M 的外部预训练权重进入对比**"。半程的 gpt2-xl 版
  （`textvae_owt2`@13400 步）保留作 teacher 规模消融。
  附加偏差：教师特征逐维标准化（实测 std 跨 [1.96, 512.97]，不标准化则 cosine
  被少数维支配），标准化前后两个 cosine 均有记录。
- **TextDiT**（`textldm_dit_owt2`）：标准 DiT 于 12L×768（85M trunk，与全家族相同），
  **上下文 latent 与噪声 latent 沿序列维拼接**，**不注入 timestep**，
  timestep 取 logit-normal(std 1.5)，流匹配（噪声到目标线性插值 + 速度场 L2），
  CFG p_uncond=0.1 / w=7，50 步 Euler。精确 7630×256×1024 = 2.0002B。

## 2. GO/NO-GO 门：无 timestep 注入**确实识别出了 t**，但中段拟合不足

`Var(v_target)` = 2.000，故 mse=2.0 等价于"什么都没学到"。

| step | mse_mean | t∈[0,.25) | [.25,.5) | [.5,.75) | [.75,1] |
|---|---|---|---|---|---|
| 500 | 2.0007 | 2.010 | 2.003 | 1.998 | 1.993 |
| 1000 | 1.8546 | 2.201 | 2.033 | 1.872 | 1.312 |
| 2000 | 1.5763 | 1.286 | 1.870 | 1.865 | 1.285 |
| 7500 | 1.5604 | 1.267 | 1.844 | 1.848 | 1.282 |

**正面证据**：四个 t 桶从齐平（step 500 全部 ≈2.0）走到显著分化，说明论文那个
"不给 t、让速度场从 z_t 的统计量隐式识别 t"的设计**在我们的 latent 上是成立的**
——值得单独记一笔，因为论文引 LLaDA/RADD 作依据其实是错的类比（那是吸收态离散
扩散的定理，不适用于高斯流匹配，此处只是经验上有效）。
**负面证据**：**中段两桶自 step 2000 起就锁死在 1.85**，离平凡解只差 8%，而轨迹
中段正是 ODE 积分最吃紧处。此处即预判生成会退化，并在结果产出前写入记录。

## 3. CFG 选择（sel 250×2）：三个权重全在 MAUVE 地板，选择无区分力

| CFG w | sel wiki R1/R2/MAUVE | sel WS MAUVE |
|---|---|---|
| 7（论文取值，胜出） | 10.46/0.26/**0.005** | 0.56 |
| 3 | 11.17/0.25/0.004 | 0.53 |
| 1 | **11.50**/0.20/0.004 | 0.50 |

按预注册规则（sel_wikipedia MAUVE > R1 > sel_wikisource MAUVE）选 w=7，恰与论文
一致。但如实记录：**三者差异 0.41→0.46 远在 n=250 的噪声带内**（预注册门槛
|Δ|<2.5 视为无信息），且 **R1 排序相反**（w=1 最高）——这次"选择"实际上没有
区分力，只是按规则走完流程。

## 4. test 终表（一枪，4×1000，w=7，50 步 Euler）

| bench | R1 | R2 | MAUVE |
|---|---|---|---|
| Wikipedia | 10.61 | 0.23 | 0.42 |
| WikiSource | 13.94 | 0.34 | 0.46 |
| TinyStories | 11.35 | 0.12 | 0.41 |
| 1BW | 6.18 | 0.02 | 0.45 |

退化诊断（wikipedia 前 150 条）：**distinct-2 0.985、prompt bigram 复制率 0.001**
——不复读也不抄 prompt，而是**高熵词碎片**。样例：
> "the the the silver,View8 the's Getty 8 Lives Cal's Just4 ab onSupp Est, b- Chart
> the Prom that'skel r Dream WhatO thesp the2 Seek2 is the) andff © the's…"

与 SSD-LM 的"功能词汤"（distinct-2 0.886）和 CMLM 的"复读退化"（0.353）是第三种
退化形态：几乎不重复的高熵碎片，因此 R2 极低（0.02~0.34），R1 也只有 6~14。

## 5. 结论：这个预算档上连续隐扩散路线整体不成立

我们现在有**两个独立的连续隐扩散实现**，都在严格 2B、同数据、同主干下：

| 实现 | 隐空间 | wiki R1/R2/MAUVE |
|---|---|---|
| CADENCE-LDM | 我们自己的**冻结 PQ** 隐空间 | 20.17/1.49/0.006 |
| TextLDM 架构复现 | **自学的连续 Transformer VAE**（+REPA） | 10.61/0.23/0.004 |
| （参照）CADENCE `_finalSEG` 离散下一尺度预测 | 同一个冻结 PQ 隐空间 | **23.39/2.51/0.120** |

两者隐空间来源完全不同（一个借用、一个自学并带 REPA 蒸馏），**却都退化**；而在
**同一个冻结 PQ 隐空间上换成离散的下一尺度自回归预测，MAUVE 从 0.59 跳到 11.96**。
这说明退化不是某个实现的缺陷，而是**连续隐扩散在 2B 预算档上的路线性问题**——
这比单独报一行数字更有价值。

必须同时承认的另一面：**论文的 DiT 预算是我们的 ~800 倍**（1.6T vs 2.0B），所以
本节只能支持"在 2B 档不成立"，**完全不能推论"TextLDM 方法不行"**。REPA 本身工作
正常（`repa_cos` 0.931），VAE 重建也饱和，失效点定位在**扩散阶段的中段速度场拟合**
（§2 的 1.85 平台）——若要在这个预算档救活它，方向应是给 t 一个显式条件而不是加
预算，因为中段的平台从 step 2000 起就不再改善。

## 总帕累托表追加行（2026-09-03）

| 模型 / 解码 | backbone NFE | wiki R1 | wiki R2 | wiki MAUVE |
|---|---|---|---|---|
| TextLDM 架构复现（50 步 Euler × CFG 双分支） | 100 | 10.61 | 0.23 | 0.42 |

仍然：**在任何 NFE 预算点上都没有 baseline 落在 CADENCE 的右上方**。

---

# 终章十一：P0 约束式左到右解码 + P1 HMAR 尺度重加权（2026-09-03）

这一章两组实验都是**负面结果**，但两者的负面性质完全不同，必须分开陈述：P0 是
**测不出效应**（换了解码顺序，模型没变；修好训练不匹配、并在正确的臂上重做 chunk 数
扫描之后，两个 sel 集给出反相关的排序 ρ=−0.90——不是「更差」，是「没有可测的效应」），
P1 是**机制有效但代价放错了地方**（损失重加权确实按设计改变了各尺度的建模精度，只是
这笔交易在文本上不划算）。

两组实验都出现了 **sel 选出的赢家在 test 上反转**。这本身是本章第三个、也是方法学
上最值钱的产出，单列在 §3。

## 1. P0：位置轴上的约束式左到右 MaskGIT

### 1.1 设计

在一个尺度的 l 个位置上，把「一次性全并行」和「严格逐位置左到右」之间插值：
把 l 个位置切成 **C 个 chunk 从左到右依次承诺**，chunk 内部再跑 **K 轮 MaskGIT
置信度序**。C=1,K=1 退化为全并行；C=l 退化为严格位置 AR（终章九的 EXP-3）。
实现见 `models/prefix_planner.py::_sampler_lr_scale`，解码接口
`--sample_mode lr:<scales>:<C>:<K>`。按要求 **chunk 数不取过细**，扫 C∈{2,4,8,16}
× K∈{1,4}，且**不与段轴叠加**——先确认位置轴本身是否有救。

**第一轮全部 8 格共用同一个 checkpoint `_b2sp`**，训练完全相同，唯一变量是解码调度。

> **必须先读的更正（§1.5）**：第一轮这个设计是**错的**。`_b2sp` 的采样器是用
> `sampler_pos`（均匀随机揭示）训的，而 `lr` 解码访问的是「左侧整块已承诺 + 当前
> chunk 部分揭示」这类状态——实测在 q512/q1024 上，均匀随机揭示里**一个**这样的
> 状态都不出现（0/48174、0/48810）。所以 §1.2~§1.4 的数字全部带训练/推理不匹配的
> 污染，不能当机制结论读。补做的匹配训练臂与 2×2 交叉在 §1.5，**在正确训练的臂上
> 重做的 chunk 数扫描（需求明写的那一项）在 §1.6，P0 的最终定论也在那里**。

### 1.2 sel 扫描（n=250，p5hot7，scales {8,9,10}）

| C \ K | K=1（wiki｜WS MAUVE） | K=4（wiki｜WS MAUVE） |
|---|---|---|
| C=2 | 14.17｜5.86 | 15.55｜8.27 |
| C=4 | 10.67｜11.01 | 15.03｜12.38 |
| C=8 | 12.36｜16.75 | 12.71｜10.86 |
| C=16 | 13.77｜8.96 | **16.78｜16.03** |

同权重参照行：位置轴 MaskGIT K=8 = 13.14｜10.22；旧 linear+gate 通路 = 15.79｜15.93；
单尺度 `ar:8` = 11.81。

K=4 在 4 个 C 上有 3 个优于 K=1，C 则完全不单调。sel 胜者 C=16,K=4 比位置轴
MaskGIT 高 **+3.64**，过了预注册的 2.5 门槛。

### 1.3 test 一枪（4×1000）—— 完全反转

| 解码（同一 checkpoint `_b2sp`） | wiki R1/R2/MAUVE | WS | TS | 1BW |
|---|---|---|---|---|
| 位置轴 MaskGIT K=8（`_finalPOS`） | 23.31/2.37/**0.111** | 28.83/3.03/0.048 | 27.23/2.23/0.006 | 8.57/0.26/0.006 |
| 约束左到右 C=16,K=4（`_finalLR`） | 23.12/2.35/**0.084** | 28.67/3.05/0.036 | 27.22/2.22/0.006 | 8.92/0.28/0.005 |

sel 的 +3.64 在 test 上变成 **−2.66**。R1/R2 三位小数内几乎不动，四个 benchmark
里 wiki 和 WS 的 MAUVE 都是左到右更差。

而且左到右**贵得多**：每尺度采样器前向 2·min(C,l)·K = 128 次，位置轴 MaskGIT
只要 2·K = 16 次，**8 倍代价换来更差的分布匹配**。

### 1.4 第一轮的（错误）结论——保留以记录推理链，实际判读见 §1.5

**必须先说清一件事：上表 MAUVE 差值的符号取决于 MAUVE 自身的一个超参数。** §3.1
的直接测量表明，把**同一份 1000 行 test 生成**改用 sel 的簇数（25 而非 100）重算，
POS 与 LR 的排序会**翻过来**（POS 16.65 vs LR 18.36）。所以「按 MAUVE 判定左到右更
差」这个说法**不成立**——MAUVE 在这一对系统上没有给出稳定答案。

去掉 MAUVE 之后，仍然站得住的证据有三条，而且都不依赖这个指标：

1. **词面指标上完全没有收益**：R1/R2 在四个 benchmark 上的差都在 0.2 以内
   （wiki 23.12 vs 23.31、WS 28.67 vs 28.83、TS 27.22 vs 27.23、1BW 8.92 vs 8.57）。
2. **代价高 8 倍**：每尺度采样器前向 2·min(C,l)·K = 128 次，纯置信度序只要 2·K = 16。
3. **机制上没有可利用的东西**：终章八的模块无关耦合探针给出位置间耦合只有
   1.3~2.9pp，而段间是 25.1pp——位置轴上本就几乎没有顺序信息可挖。
   （**2026-09-04 撤回**：这两个数扰动质量差 5 倍，不可并读，见 §6.3。）

据此**关闭这个方向**：不是因为它更差，而是因为**在 8 倍代价下测不出任何收益，而机
制上也不该有**。这是一个比原先写法更弱、但站得住的结论。

同时必须把之前那句强断言收回：终章九曾据 test MAUVE 排出「段轴 11.96 > 纯 MaskGIT
11.11 > 严格 AR 8.23」并推论「越顺序化越差」。**位置轴内部这三档的排序现在应视为
未定**（都落在同一个估计量不稳定区间内）；仍然稳固的只有**段轴 vs 位置轴的差别**
——那个差距（22.89 vs 13.14 sel、11.96 vs 11.11 test）有独立的机制证据（耦合探针
差一个数量级——**此条 2026-09-04 撤回，见 §6.3**）和 K 曲线单调性支撑，
不是靠单一 MAUVE 数字。

### 1.5 更正轮：补上匹配的训练臂，做 2×2 交叉

**问题定位。** `lr` 解码在 C<l 时走采样器的**非因果 position 模式**，输入状态是
「chunks<c 全部承诺 + chunk c 内部分揭示 + chunks>c 全遮蔽」。而 `_b2sp` 的采样器
（**更正**：实际训练模式是 `sampler_mix`，不是 `sampler_pos`，见 §6.1）有一半的
micro-batch 走 `sampler_pos` 的**均匀随机揭示**。这个分布与 `lr` 状态几乎不相交
（排除全揭示/全遮蔽的退化样本后）：

| 尺度 | 均匀随机揭示中恰好是连续左前缀的比例 |
|---|---|
| q256 | 1 / 47404 |
| q512 | 0 / 48174 |
| q1024 | 0 / 48810 |

代码里原本也没有匹配的训练模式：`chunk_prefix` 走的是**旧的 input-side visible
通路**（不是新采样器），`sampler_causal` 是严格下三角、只对应 C=l。所以新增
`--mask_mode sampler_lr`（`finetune_prefix_maskgit.py`）：揭示左侧整块 + 当前 chunk
内 cosine 随机子集，chunks>c 保持遮蔽。

**监督范围是第二个坑。** `_sampler_lr_scale` 只读出**当前 chunk**（`st_c[:, sl]`），
所以严格匹配的损失只应落在当前 chunk 的被遮蔽位置上。第一版按 `sampler_pos` 的惯例
监督了**所有**被遮蔽位置，等于额外训练「chunk c 还没定就去预测 chunk c+5」这个推理
从不执行的条件。两种范围都保留为 `--lr_supervise {chunk,all}`，`chunk` 为默认。

**新臂的位置，以及一个必须写清的边界。** 主线的树是
`b2nd`(5630) → `b2sq1`(+1000 MaskGIT 微调) → { `b2sg`(+1000 段轴)=**主线** /
`b2sp`(+1000 随机揭示) / `b2slr`,`b2slr2`(+1000 左到右揭示) }。四个第三阶段臂同父、
同 1000 步、同为精确 7630 步 = 2.0002B，**但它们不是全都只差一个变量**：

`planner.depth_ar` 不改架构也不改前向（`_depth_states` 无条件应用），它**只控制
`depth_projs` 是否训练**——`false` 即冻在零初始化上，于是 `state_s == h`，等价于纯
并行段头。而冻结规则是
`if not cfg.planner.depth_ar or args.mask_mode == "sampler_seg"`
（`finetune_prefix_maskgit.py:153`），所以：

| 臂 | mask_mode | depth_projs | 说明 |
|---|---|---|---|
| `b2sq1`（父） | bernoulli（旧通路） | 冻结在零 | 传下零权重 |
| `b2sp` | **`sampler_mix`**（**更正**，见 §6.1） | **可训** | 每 micro-batch 50/50 抛硬币在 `sampler_causal` / `sampler_pos` 之间 |
| `b2slr` / `b2slr2` | sampler_lr | **可训** | `lr` 同样经 `_depth_draw` 读出 |
| `b2sg`（主线） | sampler_seg | 冻结在零 | 刻意：让采样器成为唯一段间耦合机制 |

因此：
- ~~**`b2sp` / `b2slr` / `b2slr2` 三者只差 mask_mode**~~ **这句话是错的**（2026-09-04
  更正，见 §6.1）：`b2sp` 训的是 `sampler_mix`，只有约一半的 micro-batch 是
  `sampler_pos`，所以在各自的解码约定上它是 50% 预算、两个 `lr` 臂是 100% 预算，
  **不是预算匹配的对照**。偏向对 `lr` 臂不利的一侧，所以下表 `lr` 的劣势若有也被高估
  ——"测不出收益"这个结论不受影响，但"干净单变量"这个说法不成立。§1.6 的
  `b2spf`（纯 `sampler_pos`）才是干净对照。
- **`b2sg` 与它们差两件事**（mask_mode + 深度头冻结），所以 **`lr` 的数字不可与主线
  的段轴数字直接并读**。本节的判读全部只用 `lr` vs `pos` 的对角线，不涉及主线。

**交叉表（sel n=250，p5hot7，scales {8,9,10}，R1/R2/MAUVE）：**

| 训练臂 \ 解码 | `pos`（随机序 K=8） | `lr`（C=16,K=4） |
|---|---|---|
| `b2sp`（**`sampler_mix`**） | ~匹配一半 22.04/2.23/**0.131**｜28.00/3.31/0.102 | ✗不匹配 21.90/1.98/0.168｜28.36/3.22/0.160 |
| `b2slr`（sampler_lr, 监督=all） | ✗不匹配 22.20/2.10/0.081｜28.15/3.34/0.106 | ✓匹配 —/—/**12.25**｜—/—/13.31 |
| `b2slr2`（sampler_lr, 监督=chunk） | 未跑 | ✓匹配 21.93/2.06/**0.118**｜28.19/3.33/0.169 |

（左栏 = sel_wikipedia，右栏 = sel_wikisource。）

按预注册规则（sel_wikipedia MAUVE 优先，|Δ|<2.5 = 无信息）读**对角线**——即每个臂
用它自己训练过的解码：

| 各自匹配的配置 | sel_wiki MAUVE | 对 `pos` 的 Δ | 判读 |
|---|---|---|---|
| `pos` K=8（对照） | 0.131 | — | — |
| `lr` C16K4（监督=all） | 0.122 | −0.89 | 门槛内，无信息 |
| `lr` C16K4（监督=chunk） | 0.118 | −1.35 | 门槛内，无信息 |

读法四点：

1. **修好训练不匹配之后，对角线上测不出任何差异。** 三个配置全落在 ±2.5 无信息带
   内，而实测的 n=250 bootstrap 标准差是 3.2~6.0（§3.3）——这三个数根本不可分辨。
2. **监督范围这个假设被否证了。** 严格匹配的 `chunk` 监督（11.79）与 `all` 监督
   （12.25）差 0.46，深在噪声里。所以 `b2slr` 整体偏弱**不是**由"多训了推理不执行的
   条件"造成的；这个检查该做，但答案是它不重要。
3. **整张表的最高格仍是不匹配的那格（16.78）**，全表全距 8.65，与实测噪声同量级
   ——**MAUVE 在 n=250 上分辨不了这几个配置**，正是 §3 那条协议缺陷在咬人。
4. **sel_wikisource 上顺序相反**（`lr`+chunk 16.95 > `pos` 10.22），这是三级判据、
   且它的离散度更大（§3.3 实测 sd 更高）。两个 sel 集给出相反顺序，本身就是"该指标
   在这个分辨率上没有信息"的又一个旁证（与 §3.1 的 8 格扫描同型）。

**P0 的最终结论**：把训练不匹配修掉、并做了估计量稳健性检查之后，**约束式左到右在
位置轴上没有可测的收益**——三个匹配配置在主判据上互相不可分辨——而它的采样代价是
纯置信度序的 **8 倍**（每尺度 2·min(C,l)·K = 128 次 vs 2·K = 16 次采样器前向）。据此
关闭该方向：**理由是「同预算下测不出收益 + 代价 8 倍」，而不是「MAUVE 更差」**
（那个符号不稳定）。~~机制上位置间耦合仅 1.3~2.9pp（段间 25.1pp）~~ 这条理由
**已于 2026-09-04 撤回**（§6.3：两个探针扰动质量差 5 倍，配平后方向反转）。

必须与第一轮的错误结论区分开：第一轮说的是"左到右**更差**"，那是训练不匹配的产物；
本轮说的是"**测不出差异**"。两者的可写强度完全不同。

同时收回终章九那句强断言：位置轴内部三档（纯 MaskGIT / 约束左到右 / 严格 AR）的排序
应视为**未定**。~~稳固的只有段轴 vs 位置轴的差别，它有耦合探针（差一个数量级）与
段轴 K 曲线单调性两条独立支撑。~~ **2026-09-04 更正**：耦合探针这条支撑已撤回
（§6.3），且 `ar:8` 的撤回理由本身是反的（§6.1：`sampler_causal` 恰是 `b2sp` 训练的
一半，所以 `ar:8` 是位置轴两行里**匹配得更好**的那行）。段轴 vs 位置轴目前只剩
**段轴 K 曲线单调性**与 §1.6 的**两个 sel 集一致性**两条支撑。

### 1.6 chunk 数扫描（补做，2026-09-04）—— 需求明写的一项，之前只在作废数据上做过

**为什么要重做。** 需求原文是「Please sweep several possible values for the chunk
number」。§1.3 那次 C×K 八格扫描跑在 `b2sp` 上，训练/推理不匹配，**整批作废**；而
§1.5 的更正轮只跑了 **C=16,K=4 一个点**，且这个 C 值本身还是从作废的扫描里挑出来的
——等于把无效数据上的选择带进了修正后的实验。所以扫描必须在正确训练的臂上重做。

**跑在哪条臂上，以及为什么换成 depth 冻结的臂。** 本节全部用 `b2slr3`
（`sampler_lr` + `--lr_supervise chunk` + `depth_ar=false`）与其对照 `b2spf`
（`sampler_pos` + `depth_ar=false`）。相比 §1.5 用的 `depth_ar=true` 臂，这么换有
两个好处：

1. **最严格地满足「不要叠段轴」这条要求**。`depth_ar=false` 把 `depth_projs` 冻在零
   初始化上，于是 `state_s == h`，段头是纯并行的——不但没有段轴 MaskGIT，连固定序的
   depth-AR 链也关掉了。这一臂上**唯一**的结构就是 chunk 轴的左到右约束。
2. **与主线 `b2sg` 变成单变量可比**。两者都 depth 冻结、同父、同 1000 步，差别只剩
   mask_mode。§1.5 那句「`lr` 的数字不可与主线并读」在这一节不再适用。

**为什么 K 固定为 1。** 训练用 `--chunks 16`，揭示边界落在 l/16 的倍数上。K=1 时
chunk 内不做部分揭示，状态就是「左侧整块承诺 + 其余全遮蔽」，**C∈{2,4,8,16} 全部落在
训练分布内**。而 K>1 会在宽度 l/C 的 chunk 内部分揭示，训练只见过 l/16 宽——**C<16 配
K>1 就是又一次训练不匹配**。所以 K=4 只在 C=16 上报（`lr3`）；其余 C 想上 K>1，得各
自训一个 `--chunks C` 的臂才合法。K=1 也正是转写里 mentor 描述的形态（chunk 轴强制
左到右、chunk 内并行、不按 confidence）。

**扫描结果（sel n=250，p5hot7，scales {8,9,10}，MAUVE/R1 ×100）：**

| 解码 | sel_wiki MAUVE | 排名 | sel_WS MAUVE | 排名 | wiki R1 | wiki RL | wiki BERT |
|---|---|---|---|---|---|---|---|
| `lr` C=2 K=1 | 0.169 | 2 | 0.146 | 3 | 21.90 | 11.40 | 77.64 |
| `lr` C=4 K=1 | 0.093 | 5 | **0.224** | 1 | 21.71 | 11.45 | 77.64 |
| `lr` C=8 K=1 | **0.219** | 1 | 0.098 | 5 | 21.88 | 11.43 | 77.61 |
| `lr` C=16 K=1 | 0.162 | 3 | 0.124 | 4 | 21.76 | 11.38 | 77.65 |
| `lr` C=16 K=4 | 0.106 | 4 | 0.152 | 2 | 21.93 | 11.42 | 77.63 |
| `pos` K=8（对照，`b2spf`） | 0.184 | — | 0.129 | — | **22.15** | **11.55** | **77.73** |

三条读法，都指向同一个结论：

1. **两个 sel 集把 chunk 数排成了几乎相反的顺序：Spearman ρ = −0.90。** wiki 说 C=8
   最好（21.88）、C=4 最差（9.35）；wikisource 说 C=4 最好（22.40）、C=8 最差
   （9.76）。如果 chunk 数上真有结构，两个不相交的 250 行子集不会给出反号的排序。
   这不是「哪个 C 更好」的问题，是**没有可测的 chunk 数效应**。
2. **表面上的赢家完全由「取 5 个噪声抽样的最大值」解释。** 5 个 lr 格的 wiki MAUVE
   均值 14.97、标准差 5.10（与 §3.3 实测的 n=250 bootstrap sd 3.2~5.3 完全同量级），
   E[max of 5] = 均值 + 1.163·sd = **20.90**，实测最大值 **21.88**。对照 `pos` 的
   +3.53 领先，正好落在这个量级里。
3. **在方差小得多的指标上，对照 `pos` 反而是最好的。** wiki 的 R1 / R2 / RL /
   BERTScore / distinct-2 五项里，`pos` 全部排第一，**五个 lr 格无一超过它**
   （R1 22.15 vs 最高 21.93；RL 11.55 vs 11.45；BERT 77.73 vs 77.65）。这些指标的抽样
   误差比 MAUVE 小一个量级，方向一致。（sel_WS 的 R1 上有两格略高于对照，差 0.07~0.15，
   同样不可分辨；此处不做相反方向的解读。）

**与主线段轴的对照——这才是这张表最有信息量的部分。** `b2slr3` 与主线 `b2sg` 现在只
差 mask_mode，可以直接并读：

| 配置（都 depth 冻结、同父、7630 步） | sel_wiki MAUVE | sel_WS MAUVE | wiki R1 | wiki RL | wiki BERT |
|---|---|---|---|---|---|
| **段轴** `b2sg` + `seg:all:4` | **0.229** | **0.204** | **22.33** | **11.87** | **77.79** |
| 位置轴 `b2spf` + `pos:8` | 0.184 | 0.129 | 22.15 | 11.55 | 77.73 |
| chunk 轴 `b2slr3` + `lr` 最好格 | 21.88（C=8） | 22.40（C=4，**不同格**） | 21.88 | 11.43 | 77.61 |

段轴在**两个 sel 集上同时**是最高的（22.89 与 20.42），R1/RL/BERT 也同时最高；chunk 轴
的「最好」在两个集上是**两个互不相同的 C**，而且按 wiki 选出的 C=8 在 WS 上只有 9.76。
**两个集一致 = 信号，两个集反号 = 噪声**，这条判据在同一批数据上把段轴和 chunk 轴分得
很清楚。

**P0 定论：chunk 级/位置级的约束式左到右没有被确认有效。** 依据是三条相互独立的证据：
训练匹配后对角线不可分辨（§1.5）、chunk 数扫描的两个 sel 集反相关 ρ=−0.90（本节）、
低方差指标一致不支持（本节）。而其采样代价是纯置信度序的 8 倍。

（**注意**：这三条**都是测量证据**，不依赖耦合探针。原本还想引「位置间耦合只有
1.3~2.9pp」作第四条机制解释，但该对比已于 §6.3 撤回——配平扰动质量后方向反转。
所以 P0 关闭的理由现在**纯粹是"在这个分辨率上测不出收益、且贵 8 倍"**，不再声称
"位置轴上本就没有顺序信息可挖"。后者现在是一个**开放问题**。）

按需求原文「**Please do not combine the segment-level maskgit before we can confirm
that the token/position/chunk-level maskgit works**」——**没有确认，因此 2D 嵌套设计
（chunk 轴左到右 × chunk 内段轴置信度）不启动**。若日后要启动，正确形态仍是把
`_sampler_lr_scale` 的段轴读出换成 `_sampler_seg_scale` 的置信度序，并配一个
`sampler_lrseg` 训练模式。

**test 一枪（按预注册规则触发）。** 按 sel_wikipedia MAUVE 优先，选中 `lr` C=8 K=1
（21.88，次高 16.86，Δ=5.02>2.5，主判据即可定），在 `b2slr3` 上打 `_finalLR3`；同时
在 `b2spf` 上打匹配对照 `_finalPOSF`（`pos:8`）。两者与主线 `_finalSEG` 同为 depth
冻结、同 4×1000 benchmark，构成「段轴 / 位置轴 / chunk 轴」的三方单变量终表。**必须
连带写明：这一枪的选择依据本身是不可靠的**（ρ=−0.90），所以它的作用是给出一个诚实的
test 数，而不是支持「C=8 是好参数」。

## 2. P1：HMAR §4.3 尺度重加权 —— 机制有效，但代价放错了地方

### 2.1 难度测量（对应 HMAR Fig. 12）

按 HMAR §4.3 的做法，用**逐尺度的最小交叉熵**度量学习难度
（`tools/scale_difficulty.py`，单位 bits/segment）：

| 尺度 | q1 | q2 | q4 | q8 | q16 | **q32** | q64 | q128 | q256 | q512 | q1024 |
|---|---|---|---|---|---|---|---|---|---|---|---|
| minCE | 3.372 | 4.006 | 4.517 | 5.115 | 5.553 | **5.739** | 5.642 | 5.396 | 5.217 | 4.782 | 3.606 |

是一条**驼峰曲线**，最难点在 q32，两端都容易——形状与 HMAR 在图像上报告的一致。
最小二乘拟合 log-normal 得 **μ=1.98、σ=0.50**（尺度序号 k=1..11）。

对应 HMAR Fig. 12 的图：**[docs/figures/scale_difficulty.svg](../figures/scale_difficulty.svg)**
（左轴 = 逐尺度最小 CE 的驼峰，右轴 = 三条权重函数；生成脚本
`tools/plot_scale_difficulty.py`，拟合产物 `results/scale_weights_pqsh.json`）。HMAR 的
方法是**看着这条曲线**去选权重函数，所以它必须是图而不只是表。

> **一处对 HMAR 措辞的刻意偏离，必须写明。** HMAR 说的是「**test** cross-entropy」；
> 我们用的是 **validation**——`train_prefix_planner.py` 的 `eval.jsonl` 读的是
> `bin_dir / "val.bin"`。理由是我们自己的预注册协议：test benchmark 每配置只打一枪、
> 从不参与设计，拿 test split 去拟合损失权重恰好就是它禁止的测试集泄漏。所以这里的
> 设计量是 **min-VAL CE**。`tools/scale_difficulty.py` 的 `--eval_jsonl` 留了口子，
> 可以另算一条真 test split 的曲线**仅用于呈现**，但不得回喂训练。

三种权重（Σw=1）：

| 尺度 | q1 | q2 | q4 | q8 | q16 | q32 | q64 | q128 | q256 | q512 | q1024 | 与 minCE 相关 |
|---|---|---|---|---|---|---|---|---|---|---|---|---|
| token（默认 CE） | 0.05% | 0.10% | 0.20% | 0.39% | 0.78% | 1.56% | 3.13% | 6.25% | 12.51% | 25.01% | **50.02%** | **−0.369** |
| equal | 9.09% | 9.09% | 9.09% | 9.09% | 9.09% | 9.09% | 9.09% | 9.09% | 9.09% | 9.09% | 9.09% | 0 |
| lognormal | 0.04% | 1.77% | 6.83% | 11.98% | 14.73% | **15.05%** | 13.82% | 11.88% | 9.80% | 7.87% | 6.22% | **+0.935** |

值得单独强调：**默认的按 token 平均的交叉熵，其隐含的尺度权重与难度是反相关的
（−0.369）**——它把一半的损失质量花在最容易的 q1024 上，只给最难的 q32 1.56%。
按 HMAR 的论证，这正是应该修的。log-normal 把峰值精确放在 q32，与难度相关 +0.935。

实现：`train_prefix_planner.py::scale_weight_vector` / `scale_weighted_ce`，
损失 = Σ_k w(k)·(尺度 k 的平均 CE)。`per_scale_seg_bits` 在所有臂里**保持不加权**，
以保证难度测量本身跨臂可比。

### 2.2 主线答案（这一节才是 P1 的结论，先读它）

CADENCE 的主行是**完整课程链 + 段轴解码**：`b2nd`(5630) → `b2sq1`(+1000 MaskGIT
微调) → `b2sg`(+1000 段轴采样器) = 7630 步，解码 `seg:all:4` @p5hot7。P1 的问题
「重加权能不能让 CADENCE 变好」只有在这条链上问才有意义，所以波 B 复刻了整条链、
逐段对齐（同 src、同 1000 步、同解码；三阶段的 gate 值 0.0286 vs 主线 0.0287），
唯一变量是损失权重（**精确说**：`train.scale_weight` 只被 `train_prefix_planner.py`
读取，两个 MaskGIT 微调阶段的 `finetune_prefix_maskgit.py` 从不读它，所以重加权实际
覆盖 5630/7630 步。这是**共模**的——主线 token 对照走同一脚本、同样那 2000 步不加权
——所以对比仍有效，见 §6.4）。

**test 一枪（4×1000，`seg:all:4` @p5hot7）：**

| benchmark | 主线 token（`_finalSEG`） | lognormal 全链（`_finalSEGHWLN`） | Δ |
|---|---|---|---|
| wikipedia | 23.39/2.51/**0.120** | 25.01/2.77/**0.068** | R1 +1.62、MAUVE **−5.14** |
| wikisource | 29.12/3.26/**0.051** | 30.32/3.37/**0.030** | R1 +1.20、MAUVE **−2.11** |
| tinystories | 27.60/2.42/0.006 | 28.46/2.41/0.006 | R1 +0.86、MAUVE −0.04 |
| 1BW | 9.28/0.33/0.006 | 9.49/0.41/0.006 | R1 +0.21、MAUVE −0.02 |

**四个 benchmark 的 R1/R2 全部上涨，四个 benchmark 的 MAUVE 全部下降**，wiki 的
MAUVE 被砍掉 43%。这与 §2.4 的 ladder 总描述长度恶化 10~13% 同向，也与终章七
SSD-LM 的形态同源（词面重叠可涨、MAUVE 才判别退化）。

sel（n=250，同解码）方向一致且幅度更大：token 22.89｜20.42、lognormal
10.52｜5.65、equal 4.20｜4.62。equal 差到远超任何门槛，按预注册规则未开 test 枪。

**估计量稳健性检查（按 §3.4 第 5 条的新纪律，`tools/mauve_variance.py`）**——把同一
份 1000 行 wikipedia test 生成换成 sel 的簇数重算：

| 打分方式 | 主线 token | lognormal | 差距 |
|---|---|---|---|
| n=1000 @100 簇（上报值） | 11.96 | 6.82 | **+5.14** |
| n=1000 @**25** 簇 | 17.77 | 11.61 | **+6.16** |
| 4 个不重叠 250 行块 @25 簇（均值） | 19.13 | 12.74 | +6.39 |

**两个估计量下方向一致、幅度相当**（逐块看 4 块里 3 块favor token、1 块基本打平）。
这与 §3.1 的 P0 形成对照：P0 换簇数会翻转，P1 不会。加上零方差的 ladder 总描述长度
信号同向，**P1 的结论是稳的**。

（同时也再次量到 n=250 的噪声之大：这两个配置的 bootstrap 标准差分别是 6.01 和
4.00 MAUVE 分。）

**结论：在 CADENCE 主线上，HMAR 式尺度重加权不成立。**

下面 §2.3~§2.6 是支撑这个结论的隔离实验与机制，§2.3 的波 A 是一个**没有接
MaskGIT 的朴素基座**（`sampler=false`，普通解码 @p5hot3），它回答的是"重加权对朴素
基座有没有用"，**不是**本节的问题——它的数字与主线不在同一 regime，不可并读。

### 2.3 波 A：单跑基座三臂（唯一变量 = 损失权重，隔离用，非主线）

7630 步、depth_ar=true、同数据同主干同种子，只改 `train.scale_weight`。

sel（n=250，p5hot3）：

| 臂 | sel_wiki R1/R2/MAUVE | sel_WS R1/R2/MAUVE |
|---|---|---|
| hw76tk（token，对照） | 22.88/2.32/0.087 | 28.77/3.28/0.116 |
| hw76eq（equal） | 22.63/2.28/0.092 | 28.61/3.19/0.066 |
| hw76ln（lognormal） | 22.33/2.23/**0.140** | 28.66/3.31/**0.175** |

按预注册规则 lognormal 胜出（+5.29，过门槛），equal 无信息（+0.47）。

test 一枪（4×1000，p5hot3）：

| 臂 | wiki R1/R2/MAUVE | WS | TS | 1BW |
|---|---|---|---|---|
| hw76tk（token） | 24.23/2.55/**0.082** | 29.12/3.00/0.029 | 26.73/2.13/0.006 | 8.85/0.32/0.006 |
| hw76eq（equal） | 24.04/2.51/0.052 | 29.63/3.09/0.024 | 27.02/2.10/0.005 | 9.41/0.34/0.006 |
| hw76ln（lognormal） | 24.06/2.49/0.056 | 29.69/3.14/0.036 | 27.15/2.14/0.006 | 9.36/0.29/0.007 |

**又一次反转**：sel 上 ln 领先 tk +5.29，test 上 tk 领先 ln +2.60。

（附带一个真实但次要的观察：两个重加权臂在 WS/TS/1BW 三个集上的 R1 都高于对照，
只在 wiki 上略低。R1 的方向和 MAUVE 相反，这与终章七的结论一致——R1 单独不可读。）

### 2.4 排除优化器混淆

重加权臂的梯度范数明显更小（中位数 tk 0.461、eq 0.161、ln 0.125，相差 3.7 倍），
需要确认这不是「同一个学习率下有效步长不同」造成的假象。两条证据说明不是：

1. 优化器是 **AdamW**，更新量 ≈ lr·m/√v，对梯度的整体缩放不敏感；
2. 梯度裁剪阈值 `grad_clip=1.0`，而三臂**全程最大梯度范数分别只有 0.600 / 0.399
   / 0.152，裁剪一次都没有触发**。

所以三臂是在可比的有效步长下训练的，比较是公平的。

### 2.5 机制：重加权确实生效了，只是这笔交易在文本上亏本

关键证据是**不加权**的逐尺度 seg_bits（单位 bits/segment，越低越好）。注意
`per_scale_seg_bits` 每个 log 点只取一个 micro-batch 的快照，所以下表用**最后 10 个
log 点（step 7150–7600）的均值**，并在总账上给出批间抖动：

| 臂 | q1 | q2 | q4 | q8 | q16 | q32 | q64 | q128 | q256 | q512 | q1024 |
|---|---|---|---|---|---|---|---|---|---|---|---|
| hw76tk | 2.728 | 3.942 | 4.468 | 5.151 | 5.521 | 5.733 | 5.620 | 5.392 | 5.192 | **4.752** | **3.600** |
| hw76eq | **2.548** | 3.644 | 4.327 | 5.042 | 5.492 | 5.671 | 5.577 | 5.369 | 5.251 | 5.120 | 4.521 |
| hw76ln | 2.633 | **3.649** | **4.314** | **5.018** | **5.481** | **5.655** | **5.549** | **5.329** | **5.185** | 5.013 | 4.390 |

lognormal 相对对照的逐尺度变化（负=更好）：

`q1 −0.095｜q2 −0.293｜q4 −0.154｜q8 −0.133｜q16 −0.040｜q32 −0.078｜q64 −0.071｜q128 −0.063｜q256 −0.008｜q512 +0.260｜q1024 +0.790`

**HMAR 的机制完全兑现了**：从 q1 一路到 q256，**11 个尺度里有 9 个变好**，包括最难
的 q32（−0.078 bits）和 q64（−0.071 bits）。问题只在最后两列——q512 退化 0.260、
**q1024 退化 0.790**。

把每个尺度的变化按它实际占的位置数加权（l_k × S × Δ），得到整条 ladder 的总描述
长度变化（bits，负=更好）：

| 臂 | q32 | q64 | q128 | q256 | q512 | q1024 | **总计** | ladder 总 bits |
|---|---|---|---|---|---|---|---|---|
| hw76tk（对照） | — | — | — | — | — | — | — | **35362 ± 776** |
| hw76eq | −8 | −11 | −12 | +60 | +752 | **+3772** | **+4543** | 39905 ± 568 |
| hw76ln | −10 | −18 | −32 | −8 | +533 | **+3235** | **+3688** | 39050 ± 632 |

差异是批间抖动的 **4.7σ（eq）与 3.7σ（ln）**，不是噪声。

**中段省下的几十 bits，被最细尺度多花的三千多 bits 淹没了。** 原因是纯粹的算术：
q1024 一个尺度就占 ladder 2047 个位置里的 1024 个，q32 只占 32 个。**难度最高的尺度
恰恰是位置最少的尺度**，而按难度加权正好把权重从「多而易」搬到「少而难」。

结果是重加权模型对整条隐序列的建模**整体变差 10.4%（ln）到 12.8%（eq）**。

### 2.6 波 B 的基座层面账（与 §2.2 同一条链）

波 B 复刻产出主行的完整课程（5630 步基座 depth_ar=false → +1000 步 MaskGIT 微调
→ +1000 步段轴采样器微调 = 7630 步 = 2.0002B），权重贯穿第一阶段的 5630 步
（后 2000 步两臂都不加权，共模，见 §6.4），解码
`seg:all:4`（p5hot7）。

基座层面的 ladder 总 bits 变化（vs token 对照 `b2nd`，同为最后 10 个 log 点均值）：

| 臂 | q512 | q1024 | 总计 | ladder 总 bits | 相对批间抖动 |
|---|---|---|---|---|---|
| b2nd（token） | — | — | — | **36630 ± 937** | — |
| hw56eq | +930 | **+3839** | **+4871** | 41501 ± 690 | 4.2σ |
| hw56ln | +599 | **+3094** | **+3665** | 40295 ± 756 | 3.0σ |

同一个签名：q1..q256 全线改善或持平，全部损失压在 q512/q1024 两列。

下游 sel（n=250，seg:all:4）：

| 臂 | sel_wiki R1/R2/MAUVE | sel_WS MAUVE |
|---|---|---|
| b2sg（token 全链，对照） | 22.33/2.33/**0.229** | **0.204** |
| sg56ln（lognormal 全链） | 23.91/2.49/0.105 | 0.057 |
| sg56eq（equal 全链） | 23.19/2.48/0.042 | 0.046 |

在这里效应大到 sel 也测得出来：重加权把段轴主行的 MAUVE 从 22.89 打到 10.52 / 4.20。
`sg56eq` 远在门槛下方，按预注册规则不开 test 枪；**`sg56ln` 作为「重加权在
CADENCE 主线上到底是什么数」这一问的正式答案，开了一枪，结果见 §2.2**
（wiki 25.01/2.77/0.068 vs 主线 23.39/2.51/0.120）。

再一次出现「R1 涨、MAUVE 崩」，sel 与 test 两级同向（sel R1 23.91 vs 22.33、
test R1 25.01 vs 23.39；MAUVE 分别只有对照的 46% 和 57%），与终章七 SSD-LM 的现象
同源。

### 2.7 六个配置上的一条完整解释链

把两波合起来，**ladder 总描述长度对 MAUVE 排序的预测是完美的**：

| 波 | 配置 | ladder 总 bits（低=好） | 对应 MAUVE 排序 |
|---|---|---|---|
| A（test，n=1000） | tk / ln / eq | 35362 < 39050 < 39905 | 8.19 > 5.59 > 5.24 ✓ |
| B（sel，n=250） | b2nd / ln / eq | 36630 < 40295 < 41501 | 22.89 > 10.52 > 4.20 ✓ |
| **B（test，n=1000）** | b2nd / ln | 36630 < 40295 | **11.96 > 6.82** ✓ |

**两波、七行、六个配置，秩序完全一致**（波 B 的 test 只有 token 与 lognormal 两臂，
equal 未开枪）。唯一与之矛盾的是波 A 的 sel（n=250），它把 ln 排到了第一——而波 B
的 sel/test 之所以都没矛盾，是因为那里的效应大到远超噪声。

这直接给出本章的操作性结论：**对这类比较，ladder 总描述长度是比 n=250 的 MAUVE
可靠得多的选择信号，而且它零方差、零额外算力、训练日志里现成就有。**

**但这条规律有一个必须写明的边界：它只在采样分解方式固定时才有分辨力。** 手边就有
一个例子——朴素 sh 基座与 hw76tk 只差 `depth_ar` 一项（已逐行 diff 确认，token 权重
即旧默认损失）：

| 配置（均 7630 步） | ladder 总 bits | test wiki MAUVE |
|---|---|---|
| sh 朴素基座（depth_ar=off） | 34960 ± 912 | 0.064 |
| hw76tk（depth_ar=on，token） | 35362 ± 776 | **0.082** |

两者的 ladder bits 差 +402，而合并批间抖动是 1197——**0.34σ，统计上不可分辨**；
MAUVE 却差了 1.83。也就是说在这个对比上 ladder bits **没有分辨力**，它不会给出错误
的排序，但也给不出排序。

原因是这两个量测的不是一回事：`per_scale_seg_bits` 取自模型自身前向的 logits，是
**教师强制下的密度**；而 depth_ar 改变的主要是**采样时的分解方式**——不带 depth-AR
时，一个位置上的 4 个段在生成时只能各自独立地从同一个 h 采出来，边缘之积不等于联
合；depth-AR 按链式法则顺序采样，分解本身是对的。密度几乎没变而样本变好，正是这个
差别的表现。

本章的两波比较全部满足「分解方式相同」这个前提（波 A 三臂全是 depth_ar=true，波 B
三臂全是 depth_ar=false，解码模式各自统一），所以 §2.7 的结论不受影响；但把这条规
律推广到跨架构比较时会失效。

### 2.8 P1 结论

在 2B 预算的文本上，**HMAR §4.3 的尺度重加权在 CADENCE 主线上不成立**，而且失败
原因不是实现问题：

- **主线 test 一枪（§2.2）**：wiki MAUVE 11.96 → 6.82，四个 benchmark 的 MAUVE 全跌、
  R1/R2 全涨；
- 难度曲线复现出了 HMAR 报告的驼峰形状；
- 拟合出的 log-normal 与难度相关 +0.935，默认 CE 反而是 −0.369；
- 重加权**确实**改善了 11 个尺度里的 9 个，含最难的 q32（−0.078 bits）；
- 但最细尺度独占一半 ladder 位置，它退化 0.790 bits 就把总账拖成 +10.4%；
- 下游 MAUVE 在两波、七行、六个配置上都跟着总账走。

可以给出的（有待验证的）跨模态解释：图像 VAR 的最细尺度主要是纹理精修，那里的
误差不破坏样本；而在文本里最细尺度**就是词本身**，用它换粗尺度的精度是亏本买卖。
这只是假说，本章的数据支持不到这一步，但它是一个可以直接设计实验去证伪的说法。


## 3. 方法学：两次 sel→test 反转的真正原因（本波最值钱的产出）

一天之内两组独立实验都出现「sel 选出的赢家在 test 上反转」，我没有停在"噪声"这个
解释上，而是用 `tools/mauve_variance.py` 直接去量。**结论和我最初的猜测不同：两次
反转的成因不一样，而且 P0 那次根本不是噪声，是指标本身的问题。**

测量方式：不重新生成任何文本，只把**已有的 1000 行 test 生成**反复重新打分——
(a) 全量 1000 行、默认簇数（=100），即我们上报的 test 数字；(b) **同一份 1000 行**
改用 sel 的簇数（25），单独隔离"估计量"这一个变量；(c) 4 个不重叠的 250 行块、
25 簇，模拟 sel；(d) 20 次有放回重采样 250 行，给出 sel 的抽样标准差。

| 配置 | (a) n=1000, 100 簇 | (b) 同数据, 25 簇 | (c) 250 行块 ×4（25 簇） | 块间 sd | bootstrap sd |
|---|---|---|---|---|---|
| P0 位置轴 MaskGIT | **11.11** | 16.65 | 24.57 / 15.51 / 13.23 / 8.87 | 6.62 | 5.34 |
| P0 约束左到右 | 8.45 | **18.36** | 17.67 / 20.80 / 11.77 / 15.40 | 3.80 | 4.21 |
| P1 hw76tk（token） | **8.19** | **11.56** | 9.42 / 10.79 / 12.73 / 8.86 | 1.72 | 3.77 |
| P1 hw76ln（lognormal） | 5.59 | 7.21 | 10.62 / 12.36 / 9.52 / 12.95 | 1.58 | 3.17 |
| P1 hw76eq（equal） | 5.24 | 7.44 | 10.47 / 12.30 / 9.69 / 9.32 | 1.33 | — |

### 3.1 P0 的反转是**估计量**造成的，不是噪声——MAUVE 的簇数不保序

看表里的 (a) 和 (b) 两列，**数据完全相同、只改了 MAUVE 的簇数**：

- 100 簇：位置轴 11.11 **>** 左到右 8.45（位置轴赢 2.66）
- **25 簇：位置轴 16.65 < 左到右 18.36（左到右赢 1.71）**

**排序翻转，而数据一行没变。** 原因是粗化带来的抬升是**依臂而异**的：左到右被抬高
9.91 分，位置轴只被抬高 5.54 分。粗化只能让 KL 变小、MAUVE 变大（数据处理不等式），
这部分是共同的；但**抬升幅度不同，所以这个变换不保序**。

这一点很重要，因为它否证了一个看起来很合理的辩护："簇数只是给所有臂加了同一个偏
移，因此不影响排序"。**实测不是。**

顺带说明：P0 的 sel 结果其实是**可复现的**。sel 上左到右 16.78 > 位置轴 13.14；把
sel 的估计量（250 行 + 25 簇）用到 test 数据上，左到右的四个块是
17.67/20.80/0.118/15.40（均值 16.41），位置轴是 24.57/15.51/0.132/8.87（均值 15.55）
——同一方向。也就是说 sel **没有测错**，它忠实地测量了"在 25 簇下左到右更高"这件事；
换成 100 簇，答案就反了。

### 3.2 P1 的反转是噪声，而且结论在两个估计量下都成立

同一张表的 P1 三行给出完全不同的图景：**hw76tk 在 (a) 和 (b) 两列下都赢**
（100 簇 8.19 vs 5.59/5.24；25 簇 11.56 vs 7.21/7.44），差距还都在 2.4~4.3 分。
所以 **P1 的结论对估计量是稳健的**。

那 sel 上 ln 领先 tk 的 +5.29 是怎么来的？用 sel 的估计量在 test 数据上看：
ln 的四个块均值 11.36、tk 10.45，只差 **+0.9**，而 bootstrap sd 是 3.2~3.8。
**+5.29 是一次运气抽样**，而 +0.9 这个真实方向差远在噪声以内。

### 3.3 n=250 上 MAUVE 的抽样噪声是 3.2~5.3 分，我们的判读门槛只有 0.5~0.8σ

bootstrap（有放回重采样 250 行，25 簇）给出的标准差是 **3.17 / 3.77 / 4.21 / 5.34**
分。我们预注册的判读门槛是 **|Δ| < 2.5 = 无信息**——换算下来只有 **0.5~0.8σ**。

也就是说这个门槛从来就没有起到它想起的作用。作为对照，一个有用的分解是：

- **同一份 sel 上比较不同臂**（配对）：P0 八格共用同一个 checkpoint、同一份 250 行，
  格间标准差只有 **1.95**；
- **换一份 250 行**（非配对）：块间/bootstrap 标准差 **3.2~6.6**。

差别在于 prompt 层面的方差在配对比较里被消掉了。这直接说明**配对比较比"另找 250
条做 sel"强得多**，也说明 §3.4 第 4 条的做法是对的。

顺便，8 格取最大这件事在配对口径下仍然成立：σ=1.95 时 8 次 iid 抽样的期望最大值
= 均值 + 1.42σ = **16.65**，实测最大值 **16.78**；且**两个 sel 集选出的不是同一格**
（wiki→C16K4，WS→C8K1）。所以"最好的一格"这个观测量不需要任何真实 C×K 效应。

### 3.4 应当怎么改（本波的操作性产出）

1. **显式固定 `num_buckets`**，不让它随 n 漂。这是一行改动，且是上面所有问题里唯一
   能被"改代码"解决的。`eval_generation.py:55` 目前把它留给库的 `'auto'` = n/10。
   注意：历史上已注册的 test 行都是在 100 簇下测的，改动后新旧行不可混读。
2. **纯解码顺序的实验根本不需要 sel**：这类实验共用 checkpoint，应该直接在 test 上
   做**同 prompt、同种子的逐行配对比较**——配对消掉 prompt 方差（1.95 vs 3.2~6.6），
   而且不消耗一次 test 机会去测一个本来可以配对测的量。
3. **训练侧的比较改用 ladder 总描述长度做选择信号**（§2.6）。零方差、零算力、日志
   现成，**前提是各臂采样分解方式相同**。
4. **扫描不做选择**：多格扫描只看趋势，真正的两臂判读单独跑，门槛按比较次数放宽。
5. **凡是 MAUVE 结论，都要报一个估计量稳健性检查**（换簇数重算一遍）。本章的
   `tools/mauve_variance.py` 就是为此写的，跑一次 4 个配置约 20 分钟、单节点。

### 3.5 已排除的两个替代解释（都用日志直接证伪）

审计（4 路并行 + 每条发现单独找人反驳，见 §5）提出了两个足以推翻 P1 结论的替代解
释，两条都被直接证据排除：

- **"benchgen 取 Volume 上最新的 checkpoint，且不检查训练是否完成，所以 sel 可能量
  的是中间 checkpoint"**。这是个真实的设计缺口（`jobs/benchgen_entry.sh:84-97`），
  但没有发生：六次 P1 评测（3 次 sel + 3 次 test）的日志里加载的都是
  **`ckpt_step7630.pt`**（外加冻结 tokenizer 的 `ckpt_step150000.pt`），无一例外。
- **"hw76tk 用 16 张卡、eq/ln 用 32 张卡，所谓'其他完全相同'不成立"**。GPU 数确实
  不同（配额原因），但不改变优化：`train_prefix_planner.py:256-257` 用
  `n_accum = batch_size // (micro * world)` 并**断言** `n_accum*micro*world ==
  batch_size`，所以 world=16 时 n_accum=4、world=32 时 n_accum=2，**每步的全局 batch
  都精确是 256 条序列**，梯度 token 数同为 2.0002B。

审计的另一个真实缺口也记在这里备查（本波未触发）：`--resume auto` 不校验
checkpoint 里存的 `train.scale_weight` 与当前命令行是否一致，而平台的推荐做法是
"失败就重投以续训"——一旦某个臂在续训时漏掉 `--set train.scale_weight`，目标函数会
静默改变且不留痕迹。本波 hw76ln 虽然失败重投过一次，但它失败在 rendezvous 阶段、
checkpoint 目录当时是空的，因此是从零开始训的，没有踩到。

## 4. 由机制直接推出的下一步（本波未做，留给下一波）

P1 的失效点定位得很干净——不是重加权不管用，而是**权重函数忽略了各尺度的位置数**。
由此有两个可直接证伪的设计：

- **难度 × 位置数的折中权重**：w(k) ∝ (l_k/Σl)^α · lognormal(k)^(1−α)，α=1 退化为
  现在的 token 加权，α=0 退化为纯 lognormal。§2.5 的账表明最优 α 应当离 0 很远。
  这是一个一维扫描，且**有 ladder 总 bits 这个零方差的选择信号**可用，不必再靠
  MAUVE 选。
- **只保护最细尺度的下限**：给 q512/q1024 设权重地板，把 lognormal 的形状只施加在
  q1..q256 上。§2.5 显示中段的收益是真实的（q32 −0.078 bits），只要不拿最细尺度
  去换就行。

P0 方向**建议关闭**，但理由要按 §1.5/§1.6 修正后的版本讲：不是"MAUVE 更差"（那个
符号不稳定），而是**在 8 倍采样代价下，两个 sel 集给出反相关的 chunk 数排序
（ρ=−0.90）、低方差指标一致不支持**。~~且机制上位置间耦合只有 1.3~2.9pp（段间
25.1pp），本就没有顺序信息可挖~~ ——**这条理由 2026-09-04 撤回，见 §6.3**。

另外 §3 的发现产生了一个**独立于今天两组实验的待办**，优先级不低：位置轴内部若还
要下结论（纯 MaskGIT vs 约束左到右 vs 严格 AR），应当改用**同 prompt 同种子的逐行
配对比较**重做一遍——它们共用 checkpoint，本来就该配对测，而配对能把标准差从
3.2~6.6 压到 1.95 量级。终章九里那句"越顺序化越差"应按 §1.4 视为未定。


## 5. 审计记录（4 路并行 + 逐条对抗性反驳）

因为一天内出现两次反转，我没有直接采信"噪声"这个解释，而是开了一轮并行审计：四个
独立视角（评测路径 sel/test 不对称、sel 数据是否有偏、重加权实现本身、上报数字的
provenance），每条发现再单独派人**尝试推翻**，默认"存疑即推翻"。

**结果：16 条候选发现全部被推翻，0 条确认。** 其中最有价值的产出不是"没有 bug"，
而是三条：

1. **重加权实现被逐行验证正确**：权重下标 ↔ 尺度 ↔ logits/codes 的 ladder 分块三者
   对齐精确；`token` 模式就是原来那个未改动的 flat CE（所以对照臂确实复现历史目标
   函数）；`scale_weighted_ce` 确实是 Σ_k w_k·mean CE；`per_scale_seg_bits` 在每个臂
   里都不加权。**所以"lognormal 在 test 上输"不是一句关于 bug 的话。**
2. **两个足以推翻 P1 的替代解释被日志直接排除**（见 §3.5：checkpoint 全部是
   `ckpt_step7630.pt`；16 卡/32 卡下全局 batch 都精确是 256）。
3. **一条被"推翻"的发现其实是对的，但对错方向**：审计提出"MAUVE 的 `num_buckets`
   随 n 漂"，反驳者的核心论点是"粗化只是给所有臂加同一个偏移，因此保序，不能解释
   反转"。**这个反驳听起来严密，但被 §3.1 的直接测量证伪了**——同一份数据换簇数，
   排序确实翻转，因为抬升幅度依臂而异（+9.91 vs +5.54）。这也是本波的一个方法学
   教训：**能测的东西不要用推理定案。**

审计另外记录了一批真实但本波未触发的缺口，留作备查：`--resume auto` 不校验
checkpoint 里的 `train.scale_weight`；`benchgen` 不检查训练完成即取最新 checkpoint；
`Path.with_suffix` 会让含点的 TAG 撞名；sel_* 基准文件的构建命令（`--skip/--n`）没有
记录在 repo 里，因此"与 test 不重叠"这条只有文字记录、无法从 repo 复核。

## 6. 第二轮审计（2026-09-04，27 agent）：三条确认发现，其中两条撤回既有结论

§5 那轮审计问的是"P1 的反转是不是 bug"，答案是否。这一轮换了问题：**"今晚跑的每一条
臂，是不是在测它声称在测的东西？"** 4 路视角（训练/推理匹配、对照匹配、解码-checkpoint
配对、第三类错误）共 27 个 agent，每条候选发现再单独派人尝试推翻。**23 条候选里
8 条确认（去重后 3 个独立问题）、15 条推翻。** 三条确认的我都亲自复核了代码，不采信
转述。

### 6.1 `_b2sp` 训的是 `sampler_mix`，不是 `sampler_pos`（4 个 agent 各自独立查到）

`.job-cadence-planner-b2spo2.yaml:18` 的 `EXTRA_ARGS` 是
`--mask_mode sampler_mix`，而 `finetune_prefix_maskgit.py:263-268` 把它按 **micro-batch**
抛硬币解析成 `sampler_causal` 或 `sampler_pos`。且 `grep -l` 确认 b2spo2 是**唯一**产出
`planner_prefix_owt2_pqsh_b2sp` 的 planner 作业。

两种模式是**同一组 4.16M 采样器参数上互不相容的输入约定**：`sampling_transformer.py:139-150`
在 causal 模式下把 code/flag 流整体右移一位并加下三角掩码，而 token 里没有任何标记
（`mask_tok`/`flag_emb`/`seg_emb`/`scale_emb` 都与约定无关）区分现在是哪种。所以
`_b2sp` 只有约一半的采样器梯度落在 `pos` 解码实际运行的约定上，而 `b2sg` / `b2slr` /
`b2slr2` 都是 1000 步纯单模式。

**影响，逐条说清：**

| 受影响的东西 | 判定 |
|---|---|
| §1.5 「`b2sp`/`b2slr`/`b2slr2` 三者只差 mask_mode，干净单变量」 | **假**。它们在各自解码约定上是 50% vs 100% 预算 |
| §1.5 交叉表的 `lr` 劣势（−0.89 / −1.35） | 方向偏向**不利于 `lr`**，所以劣势若有也被高估；"测不出收益"的结论**不变** |
| 终章九对 `ar:8` 的撤回理由 | **反了**。`sampler_causal` 恰是 `b2sp` 训练的一半，`_sampler_ar_cached` 精确复现该右移+下三角约定，所以 `ar:8` 是位置轴两行里**匹配得更好**的一行，`pos:8,9,10:8` 才是半匹配的 |
| §1.5 E1 诊断的量化证据（0/48174） | 测的是错的训练边缘分布（只统计了 `sampler_pos` 那一半）。但**结论仍成立**：causal 那一半是右移+下三角，而 `lr` 在 C=16<l 时 `causal=False`，两者仍不相同 |
| **§1.6 与 test 终表** | **不受影响**——它们用的是 `b2spf`（纯 `sampler_pos`）和 `b2slr3`，不是 `b2sp` |

最后一行是运气：我换成 depth 冻结臂的理由本来是"与主线单变量可比"，副作用恰好绕开了
这个缺陷。但这不是设计出来的，必须写明。

### 6.2 EXP-2「采样头容量不是原因」是 22 次对 64 次 backbone 前向（3 个 agent）

已在终章六 §1 就地加了撤回框。核实过的机制：`_refine_scale`（`models/prefix_planner.py:677`）
在 `for j in range(K)` **循环内**调用 `_sample_block` → `_block_hidden` → 完整 12 层
`_trunk`；而 `_sampler_pos_scale`（:804）的 `_cached_hidden` 在循环**外**，每尺度只算
一次。p5hot7 下每尺度两条 CFG 分支，于是 `REFINE 8,9,10:8` = 8×2 + 3×8×2 = **64**，
`pos:8,9,10:8` = 11×2 = **22**。

更要命的是参数量这个框架本身是反的：那个"25,345 参数的小头"（`visible_proj`）把揭示码
注入在 **trunk 输入端**（`_add_visible`），它的条件网络是整个 85M trunk、且揭示信息能
通过 12 层注意力与 prompt 和所有更粗尺度交互；而 4.16M 采样器只是冻结 hidden 上的一个
残差，注意力从不离开本尺度。**所以标着"容量小 164 倍"的那一行，在除自身投影尺寸外的
每个维度上都有更多条件计算量——而它赢了。** 这正是容量假设的预测。

报告自己的帕累托表本来就把这两行记作 22 和 64，与 §1 的"同 NFE=64"直接矛盾，我之前
没有交叉核对。**结论"位置轴无效不是容量问题"撤回**；要重做的话，唯一可行形态是在同一
条 `pos` 解码路径上扫采样器宽度（旧通路无法在 22 次前向下做 K=8 精化——对它而言
"MaskGIT 轮数"和"trunk 前向数"是同一个量）。

### 6.3 段间/位置间耦合"差一个数量级"是扰动质量的产物（2 个 agent），配平后**方向反转**

两个探针从来没有按扰动质量配平过：

| 探针 | 码槽被破坏比例 | donor 结构 | acc_drop |
|---|---|---|---|
| `all_independent`（`utils/evaluation.py:122`，报告引用的 25.1pp） | **100%**（1024 位置 × 4 段全打乱） | 每段一个**不同** donor | 25.09pp |
| `per_segment`（同一函数，**一直有输出，从未被引用**） | **25%**（1 段 × 全部位置） | 共享 donor | 0.64~0.86pp |
| 位置 roll p=20%（`tools/diagnose_intra_scale.py:221`，报告引用的 1.3~2.9pp） | **20%** | 共享 donor | 1.32pp（最细尺度） |
| 位置 roll p=40% | 40% | 共享 donor | 4.33pp |

即报告把一个 **100% 质量、独立 donor** 的段轴扰动，对上了一个 **20% 质量、共享 donor**
的位置轴扰动。配平后（同为共享 donor、同为 ~25% 码槽、同为最细尺度 1024）：

- 位置轴：由 p=20%/40% 的幂律（指数 1.71）内插到 p=25% → **1.93pp**
- 段轴：`per_segment` 均值 → **0.77pp**

**位置轴反而强 2.5 倍。** 不是"段间强一个数量级"，是符号反了。

必须同时说清的边界：**这一对配平比较也不是两种解码器的忠实代理**。一次并行采样是
把 S 个段**互相独立**地采、也把 l 个位置**互相独立**地采；段轴的忠实代理就是
`all_independent`，而位置轴的忠实代理——**每位置独立 donor、100% 质量的 roll——从来
没有被测过**。所以正确的说法是：**这个对比在配平前是错的，配平后的可用版本说位置轴
耦合更强，而真正对口的那个量缺测。**

**补测结果（`tools/diagnose_intra_scale.py` 新增 metric D，作业 `cadence-diag-coup`，
64 val 对，acc_drop pp）**：同一批数据、同一冻结解码器、同一估计量，两个轴按
**相同码槽比例 × 两种 donor 结构**交叉：

| 尺度 | 码槽 | pos 共享 | pos 独立 | seg 共享 | seg 独立 | pos/seg |
|---|---|---|---|---|---|---|
| 8 (l=256) | 25% | 2.310 | 2.356 | 0.816 | 0.748 | **3.15** |
| | 50% | 6.351 | 6.308 | 3.871 | 3.807 | 1.66 |
| | 75% | 12.619 | 12.378 | 10.806 | 10.562 | 1.17 |
| | 100% | 21.194 | 20.940 | 21.194 | 21.141 | **0.99** |
| 9 (l=512) | 25% | 3.923 | 3.928 | 1.199 | 1.205 | **3.26** |
| | 50% | 11.263 | 11.494 | 6.931 | 6.970 | 1.65 |
| | 75% | 21.959 | 22.444 | 19.760 | 19.342 | 1.16 |
| | 100% | 35.425 | 36.093 | 35.425 | 35.066 | **1.03** |
| 10 (l=1024) | 25% | 1.941 | 1.962 | 0.708 | 0.696 | **2.82** |
| | 50% | 6.389 | 6.352 | 4.271 | 4.070 | 1.56 |
| | 75% | 13.907 | 13.989 | 12.254 | 12.181 | 1.15 |
| | 100% | 24.690 | 24.704 | 24.690 | 24.417 | **1.01** |

**锚点核对**：`seg_indep @100%` 尺度 10 = 24.42pp，对得上历史 `all_independent` 的
25.09pp（不同数据切片，故不完全相同）。所以 metric D 确实复现了老探针。

三条读法：

1. **两个轴之间不存在耦合不对称。** 比值从 25% 码槽的 2.8~3.3 单调衰减到 100% 的
   **1.00**，三个尺度完全一致。100% 时两者按构造都等于"整条最细尺度被摧毁"，收敛到
   同一个数正是这个探针**自洽**的证据。所谓"差一个数量级"，**全部**来自把 100% 质量
   对上 20% 质量。
2. **donor 结构几乎不起作用。** 全部 24 个格子里 |共享 − 独立| 最大只有 **0.668pp**。
   所以"独立 donor 摧毁了位置内联合分布"这个故事**不是**驱动因素——驱动因素只是
   **有多少码槽被独立地重画**。
3. **在低比例端反倒是位置轴耦合更强**（25% 时 2.8~3.3 倍）。掉落对独立重画的槽数是
   **超线性**的（幂律指数约 1.7）。

**因此，「段轴赢是因为段间耦合远强于位置间耦合」这个机制解释彻底作废。** 两个轴的
尺度内耦合结构在配平后是一样的。

必须分清的是：**现象仍在，解释没了。** 段轴解码在 test 上四个 benchmark 的 MAUVE
全部第一（§7 三方终表），这是绝对数，不依赖任何探针。作废的只是"为什么"。至于新的
机制解释——目前**没有**一个被数据支持的候选，不应当现编。可查的方向有两个：
段轴在 K=S=4 时达到**完全顺序化**而位置轴在 K=8 下每轮仍有约 l/8 个位置并行抽取；
以及 `seg:all:4` 覆盖全部 11 个尺度而 `pos:8,9,10:8` 只覆盖 3 个（footprint 未配平，
`seg:8,9,10:4` 在 `b2sg` 上是分布内的，可以直接跑）。**这一条留作公开问题，不写进
论文的机制叙述。**

**这条撤回的代价最大**，因为它是"段轴有效、位置轴无效"这个二分法的支柱之一：

| 支柱 | 状态 |
|---|---|
| 耦合探针差一个数量级 | **撤回**（本节） |
| EXP-2 容量对照 | **撤回**（§6.2） |
| 段轴 K 曲线单调性 | 仍然成立 |
| §1.6：段轴在**两个** sel 集上同时最高，chunk 轴两集反号 | 仍然成立（本波新增） |
| `_finalSEG` 对 BD3 的 test 双超 | 仍然成立（绝对数，不依赖上述对照） |

也就是说：**"段轴解码在本架构上有效"这个正面主张不受影响**（它由 K 曲线、两集一致性
和 test 绝对数支撑）；受影响的是**"因为段间耦合远强于位置间耦合"这个机制解释**，以及
**"位置轴无效"这个负面主张的强度**——后者现在应降级为"在当前分辨率下测不出收益"。

### 6.4 被推翻的 15 条里，值得记的两条

- **「`--lr_supervise chunk` 同时把目标尺度的损失质量从 88% 压到 36%」**：机制属实
  （`retain_weight=0.5` 的底噪 + 逐样本归一化），但它不构成本波结论的威胁——`chunk`
  与 `all` 的实测差只有 0.46，深在噪声里（§1.5 读法 2）。
- **「`finetune_prefix_maskgit.py` 完全忽略 `train.scale_weight`，所以波 B 的"全链
  重加权"只覆盖 5630/7630 步」**：代码事实为真（该脚本从不读这个字段），但**是共模
  的**——主线 token 对照走的是同一个脚本、同样的 2000 步未加权。所以波 B 的对比仍然
  有效，需要改的只是"全链使用同一种权重"这句措辞。

## 7. 三方 test 终表（2026-09-04）：段轴 / 位置轴 / chunk 轴，第一次在同一条底座上

这是本项目第一次把三个尺度内解码轴放在**完全相同的单变量底座**上：三个臂同父
（`b2sq1`）、同 1000 步第三阶段、同为精确 7630 步 = 2.0002B、**`depth_projs` 都冻结在
零**、同 p5hot7、同 4×1000 test。唯一变量是 `mask_mode` 与对应的解码。

| benchmark | 段轴 `b2sg`+`seg:all:4` | 位置轴 `b2spf`+`pos:8` | chunk 轴 `b2slr3`+`lr:8:1` |
|---|---|---|---|
| wikipedia | 23.39/2.51/**0.120** | 23.06/2.30/0.100 | 22.96/2.27/0.093 |
| wikisource | 29.12/3.26/**0.051** | 28.88/3.11/0.027 | **29.16**/3.01/0.044 |
| tinystories | **27.60**/2.42/**0.006** | 27.28/2.26/0.006 | 26.71/2.17/0.006 |
| 1BW | **9.28**/0.33/**0.006** | 8.86/**0.34**/0.006 | 8.79/0.22/0.006 |

（R1/R2/MAUVE ×100；加粗 = 该 benchmark 该指标全场最好。）

三条读法：

1. **段轴在四个 benchmark 的 MAUVE 上全部第一**，R1 上四中三第一。这是第一次在
   干净单变量下拿到这个结论——此前的 seg-vs-pos 对比都带着 `b2sp` 的 `sampler_mix`
   污染和 depth 头冻结不一致（§6.1）。
2. **位置轴与 chunk 轴之间没有一致方向**：wiki 位置轴高 0.68，WS chunk 轴高 1.71，
   TS 位置轴高 0.02，1BW chunk 轴高 0.04。**符号在四个集上换了两次**——与 §1.6 的
   sel 结论（ρ=−0.90）完全一致。P0 定论坐实：**约束式左到右测不出收益**。
3. 顺带更新：旧的 `_finalPOS`（跑在 `sampler_mix` 的 `b2sp` 上）wiki 是 11.11，
   纯 `sampler_pos` 的 `_finalPOSF` 是 10.02。两者都低于段轴的 11.96，所以 §6.1 的
   污染没有改变 seg > pos 这个方向，只是把它测得更干净了。

**这张表是本波唯一没有被第二轮审计动到的东西**：它不依赖耦合探针（§6.3 撤回）、不
依赖采样头容量对照（§6.2 撤回）、不依赖 `b2sp`（§6.1 污染）。段轴解码有效这个正面
主张，现在由它 + 段轴 K 曲线单调性 + §1.6 的两 sel 集一致性支撑。

---

# 终章十二：2D MaskGIT（P0′）+ HMAR 权重插值（P1′）——预注册（2026-09-04，结果未出）

mentor 新需求推翻了上一波"确认前不叠加"的门（现在明确要求组合），并要求 P1 调整权重
函数补偿细尺度。**本节在任何结果落地之前写下设计与判读规则**，之后只填数字。

## 1. P0′：2D MaskGIT 设计

解码 `lrseg:<scales>:<C>:<Kseg>`（`_sampler_lrseg_scale`）：chunk 轴严格左到右（同
`lr`），当前 chunk 内段轴按置信度 top-k（同 `seg` 的日程，K_seg 轮、每轮每位置承诺
top-k 段）。全程 segment 约定（无因果右移），读出为并行对角选取（**不是** depth 链），
depth 冻结。门禁测试：C=1 与 `seg` 逐比特等价（全 K × 全 CFG）；NFE 逐格计数
= 每尺度 2·C·K_seg 采样器前向、backbone 恒 2。

训练臂 `b2s2d`：`b2sq1` + 1000 步 `--mask_mode sampler_lrseg --chunks 8
--mask_scales 8,9,10`、depth 冻结——与 `b2sg`/`b2spf`/`b2slr3` **同父、同预算、单变量**。
训练状态 = 解码访问的状态（chunk 前缀全承诺 + 当前 chunk 内每位置随机段子集 + 右侧全
遮蔽），**C 逐样本从 {1,2,4,8} 抽**，使扫描的每个推理 C 都在分布内（代价 = 每个 C 摊到
1/4 的监督，如实披露）。这次第四种解码在**任何测量之前**就有了配套掩码模式与门禁——
E1 教训的直接应用。

**扫描（全部 sel n=250 × 2 集，p5hot7，scales {8,9,10}，backbone 恒 22）：**

| 档 | 格 | 采样器前向合计 | 对应 mentor 组 |
|---|---|---|---|
| P=8（48） | C2·K4 | 48 | (1) 段轴原样、位置轴粗化 |
| P=8（48） | C8·K1 | 48 | (2) 位置轴原粒度、段轴粗化 |
| P=8（48） | C4·K2 | 48 | (3) 双粗化 |
| P=4（24） | C2·K2 | 24 | 双粗化、更低 NFE |
| P=4（24） | C4·K1 | 24 | 位置为主 |
| P=4（24） | **C1·K4（锚）** | 24 | 纯段轴@细尺度（同臂退化，逐比特=seg） |

另 + `seg:8,9,10:4` @ `b2sg`（跨臂锚，兼答 §6.3 的 footprint 缺测）。

**预注册判读规则**：
1. 主判据 sel_wikipedia MAUVE，但**任何正面结论要求两个 sel 集方向一致**（§1.6 的
   ρ=−0.90 教训）；|Δ| 落在实测 bootstrap sd（3.2~5.3）内 = 无信息。
2. 关键比较是**档内 iso-NFE**：P=4 档上「最好的 2D 格 vs C1·K4 纯段轴锚」直接回答
   "同价下 2D 是否优于纯段轴"。
3. test 一枪 ×2：(i) 5 个真 2D 格中按规则选出的赢家；(ii) C1·K4 锚（= 段轴 24 NFE 的
   注册数，本身对论文有独立价值）。每配置一枪，不加枪。
4. 若 2D 全部格在两集上都不优于锚：结论写「同 NFE 下 2D 组合不优于纯段轴」，方向按
   需求完成、如实关闭。

## 2. P1′：权重插值设计（与 mentor 的"discuss with Claude"对应）

**诊断回顾**：lognormal 修好 9/11 个尺度（含最难 q32 −0.078 bits）但 q1024 +0.790 bits，
而 q1024 独占 ladder 2047 位中的 1024 → 总描述长度恶化 10.4%，MAUVE 崩；token 反向
（相关 −0.369，50% 质量在最容易的 q1024）。**失败是"份额分错"不是"方向错"** → 设计一个
把细尺度权重买回来的单参数族：

**w_α(k) ∝ token(k)^α · lognormal(k)^(1−α)**（几何插值；α=1 精确= token 的 l_k/Σl，
α=0 精确= lognormal——两端点都是已测配置，有端点等价门禁测试）。

| α | w(q32)（最难） | w(q512)+w(q1024)（细对） | 与难度相关 |
|---|---|---|---|
| 0（=lognormal） | 15.05% | 14.09% | +0.935 |
| **0.25** | 11.32% | 27.80% | +0.541 |
| **0.50** | 6.93% | 45.27% | +0.003 |
| **0.75** | 3.53% | 62.02% | −0.252 |
| 1（=token） | 1.56% | 75.04% | −0.369 |

**臂**：`hw56a25/a50/a75`，各 = 主线 stage-1 配方（5630 步，depth_ar=false）仅换权重。
**预注册选择规则（不用 MAUVE、不用 test）**：
1. 选择信号 = **总 ladder 描述长度** Σ l_k·S·minCE_k（val），§2.7 已证它在固定采样分解
   下对 MAUVE 排序 6/6 全对，且零抽样方差。
2. 合格线：总 bits ≤ token 基线 `b2nd`@5630 的总 bits + 2σ_batch（σ≈997 bits，来自
   §2.5 的批间离散实测）。
3. 合格臂中取 **α 最小**者（重加权最强 → R1/R2 收益预期最大——eq/ln 两臂 R1 全涨是
   已测事实），补 +1000 MaskGIT +1000 段轴的完整课程链，**test 一枪** vs `_finalSEG`。
4. 三臂全不合格 → P1′ 负面收官，**不烧 test 枪**。

在跑：`b2s2d`（491073003279421）、`hw56a25/50/75`（1019194977340300 /
581330540360765 / 980187653186376），2026-09-04 深夜发出。

## 3. P0′ 结果（2026-09-04 晚，预注册规则逐条执行）

### 3.1 扫描表（sel n=250 ×2 集，R1/R2/MAUVE ×100）

| 配置（采样器 NFE） | sel_wikipedia | sel_wikisource |
|---|---|---|
| 2D C=2 K=4（48，组1） | 21.93/2.07/0.117 | 28.28/3.30/0.143 |
| 2D C=8 K=1（48，组2） | 22.14/1.99/**0.156** | 28.29/3.23/0.160 |
| 2D C=4 K=2（48，组3） | 21.98/1.99/0.143 | 28.48/3.29/0.102 |
| 2D C=2 K=2（24） | 22.09/2.02/0.100 | 28.49/3.33/**0.191** |
| 2D C=4 K=1（24） | 22.06/2.00/0.113 | 28.34/3.27/0.135 |
| 锚 C=1 K=4 = 纯段轴（24） | 21.98/2.02/0.136 | 28.19/3.33/0.129 |
| 跨臂锚 seg:8,9,10:4 @b2sg（24） | 22.14/2.11/0.139 | 28.15/3.36/0.184 |

判读（按 §1 预注册）：**同 NFE 档内 2D 测不出优于纯段轴**。iso-24 档两个 sel 集排序
相反（wiki 说锚最好，WS 说 C2K2 最好）；48 档最好的 C8K1 对锚 +2.02/+3.06，低于门槛
且贵一倍。跨臂锚（13.90）≈ 同臂锚（13.59），说明 `b2s2d` 训练本身健康。

### 3.2 test 两枪（按预注册触发，4×1000）

| benchmark | 2D 赢家 C8K1（48） | 纯段轴锚 C1K4（24） | 主线 seg:all:4（88） |
|---|---|---|---|
| wikipedia | 23.05/2.28/0.091 | 23.10/2.32/0.077 | 23.39/2.51/**0.120** |
| wikisource | 28.86/3.06/0.030 | 29.13/3.10/**0.043** | 29.12/3.26/**0.051** |
| tinystories | 26.98/2.20/0.006 | 27.21/2.25/0.006 | 27.60/2.42/0.006 |
| 1BW | 8.67/0.23/0.006 | 8.79/0.28/0.006 | 9.28/0.33/**0.006** |

2D 对锚**符号在四个集上翻转**（wiki +1.39 / WS −1.32）——test 也确认同 NFE 下无组合
收益。两者都明显低于 88-NFE 主线。

### 3.3 本波真正的发现：采样器的收益要求**全尺度覆盖**

覆盖曲线（`b2sg` 同 checkpoint、同 seg K=4，唯一变量 = 尺度集；sel）：

| 尺度集 | 采样器 NFE | wiki MAUVE | WS MAUVE |
|---|---|---|---|
| 无 | 0 | 0.072 | 0.114 |
| {8,9,10} 细-only | 24 | 0.139 | 0.184 |
| {0..7} 粗-only | 64 | 0.120 | 0.191 |
| {0..9}（缺 q1024） | 80 | 0.156 | 0.259 |
| **{0..10} 全部** | **88** | **0.229** | 0.204 |

三条读法：①**没有任何真子集接近全覆盖**——wiki 上最好的子集 15.62 对 22.89 差 7.3
（超出实测 sd 5.3）；②最后补上 q1024 的 8 次前向带来最大单跳（15.62→22.89）；但
③细-only（含 q1024）也只有 13.90，所以**不是 q1024 单尺度的功劳，是全链协同**——
残差梯子上任何一段留给并行独立采样，误差就沿链传播、封顶整体质量。

**结论改写**：mentor 的"压低采样器 NFE"目标与 wiki 主判据直接冲突——**省 NFE 不能
省在尺度覆盖上**。正确的省法是保住全覆盖、在每尺度内部用更便宜的日程。为此本波
追加了混合两组解码（`seg:<粗>:K + lrseg:<细>:C:K`，代码 d07a400，5 门禁）与混合训练臂
`b2s2dm`（sampler_seg2d：粗带 seg 揭示 + 细带 2D 揭示，同父 7630 步）。预注册扫描：
**44**（seg:0-7:2 + lrseg C2K1，全覆盖最便宜档）/ **88**（seg:0-7:4 + lrseg C2K2，与
主线严格 iso-NFE 且粗带完全相同 —— "覆盖对了之后细尺度 2D vs 纯段轴"的第一次干净
对决）/ **112**（细带 C4K2，向上探一格）/ 对照 seg:all:4 @b2s2dm（88）。判读规则同 §1
（两集一致才算数；test 至多一枪给 88 档赢家）。

## 4. P1′ 结果：α 门控通过，赢家 α=0.25（1.9σ 压线）

三臂 5630 步完成，总 ladder 描述长度（预注册选择信号；`b2nd` 复算 36619 ≈ 此前
36630 ✓）：

| 臂 | 总 bits | vs token | 合格（≤38613） | q32 | q1024 |
|---|---|---|---|---|---|
| b2nd（token） | 36619 | — | — | 5.742 | 3.829 |
| α=0.75 | 36538 | **−80** | ✓ | 5.687 | 3.852 |
| α=0.50 | 37246 | +627 | ✓ | 5.668 | 4.007 |
| **α=0.25** | 38511 | +1892 | ✓（1.9σ 压线） | **5.664** | 4.264 |
| α=0（lognormal，参照） | 40515 | +3896 | ✗ | 5.657 | 4.645 |
| equal（参照） | 41771 | +5152 | ✗ | 5.680 | 4.838 |

插值按设计工作：细尺度损伤随 α 单调修复，同时最难的 q32 在所有 α 下仍改善、
α=0.25 已几乎追平纯 lognormal（5.664 vs 5.657）。按预注册规则（合格臂取 α 最小）
**选 α=0.25**；必须写明它只以 102 bits（0.1σ）的余量压线，α=0.5 是稳妥备选。
课程链 `mg56a25`（+1000，gate 0.0286 与主线一致）已完成，`sg56a25`（+1000 段轴）在跑，
之后 test 一枪 `seg:all:4` 对 `_finalSEG`。原始 eval 曲线与门控计算固化在
`results/hmar_alpha/`（alpha_gate.json 可复算）。

## 5. 提交记录入库

`.job-cadence-*.yaml`（sgcli 提交时生成的作业配置）此前被 gitignore，是"臂的超参在
仓库里找不到"的根因（用户与审计各撞过一次）。自本波起全部 318 份提交记录以
`jobs/submitted/job-cadence-*.yaml` 入库（去前导点避开 ignore 规则），每个臂的完整
训练/解码配置即其 `env_variables`。

## 6. 混合覆盖扫描结果（2026-09-05 凌晨）：P0 方向彻底关闭

`b2s2dm`（sampler_seg2d：粗带 seg 揭示 + 细带 2D 揭示，同父 7630 步）上按 §3.3 预注册
执行（sel n=250 ×2 集，R1/R2/MAUVE）：

| 配置（采样器 NFE） | sel_wikipedia | sel_wikisource |
|---|---|---|
| seg粗K2 + 2D细C2K1（44） | 21.77/2.06/0.154 | 28.20/3.41/0.092 |
| seg粗K4 + 2D细C2K2（**88，iso 主线**） | 21.84/2.13/0.157 | 28.31/3.58/0.178 |
| seg粗K4 + 2D细C4K2（112） | 21.86/2.07/0.148 | 28.34/3.50/0.171 |
| **对照 seg:all:4 同臂（88）** | 21.98/2.12/**0.191** | 28.40/3.56/**0.311** |
| 参照 seg:all:4 @b2sg（88） | 22.33/2.33/0.229 | 28.62/3.65/0.204 |

判读：
1. **覆盖固定、NFE 匹配（88 vs 88、粗带处理完全相同）后，细尺度上 2D 仍输给纯段轴**：
   wiki 15.70 < 19.09、WS 17.84 < 31.11——**两个 sel 集方向一致**，这是 2D 假设在其
   最有利条件下的干净否定。按预注册，88 档赢家是对照本身（其注册 test 数已存在），
   不再加枪。
2. 细带加 NFE 无用（112 档 14.77 ≤ 88 档 15.70）；44 档全覆盖 15.44 ≈ 88 档 2D 细带
   ——一旦细带用了 2D，粗带花多少都被细带封顶。
3. 臂本身健康但略弱于主线臂（同解码 19.09 vs 22.89，−3.8 在噪声边缘）：sampler_seg2d
   把细带监督摊给 4 个 C 值（C=1 只占 25%），纯 seg 解码在细带略欠训——**方向性结论
   不受影响**（对照与 2D 同臂同摊薄），但绝对数不与主线并读。
4. WS 的 31.11 是该 sel 集史上最高值，按 §3.3（终章十一）的噪声测量应持保留读法；
   本节结论只依赖两集**方向一致**，不依赖该绝对值。

**P0 总定论（三轮之和）**：位置/chunk 轴的顺序机制在（a）裸细尺度、（b）同 NFE 对
纯段轴、（c）覆盖修正后 iso-NFE 三种设置下均无收益或为负；**段轴置信度序 + 全尺度
覆盖（seg:all:4，88）保持为最优解码**，且覆盖曲线（§3.3）显示 88 中的每一段花费都
不可省。方向关闭。

## 7. P1′ test 终枪：α=0.25 达成 mentor 要的 trade-off（本波唯一正结果）

`_finalSEGA25`（α=0.25 全链 + seg:all:4 @p5hot7，4×1000，一枪）对主线：

| benchmark | **α=0.25** | token 主线 | Δ R1 | Δ R2 | Δ MAUVE |
|---|---|---|---|---|---|
| wikipedia | 24.99/2.92/0.103 | 23.39/2.51/0.120 | **+1.60** | **+0.41** | −1.68 |
| wikisource | 30.39/3.41/0.047 | 29.12/3.26/0.051 | **+1.27** | **+0.15** | −0.44 |
| tinystories | 29.25/2.71/0.006 | 27.60/2.42/0.006 | **+1.65** | **+0.29** | −0.03 |
| 1BW | 9.43/0.36/0.007 | 9.28/0.33/0.006 | **+0.15** | **+0.03** | **+0.11** |

- **R1/R2 八格全涨**（四 benchmark × 两指标方向一致），wiki R1 24.99 几乎追平纯
  lognormal 链的 25.01，但 MAUVE 10.28 对 6.82——**同样的 R1 收益只花了 1/3 的
  MAUVE 代价**。ladder bits 的预测再次兑现（+1892 对 +3896）。
- MAUVE 只在 wiki/WS 轻微下探（−1.68/−0.44，n=1000 同集配对），TS 持平、1BW 反升。
- 对 BD3：R1 差距从 +0.2 拉大到 +1.8（24.99 vs 23.2），MAUVE 10.28 略低于其 11.39。

**定位**：α=0.25 是 token 与 lognormal 之间的一个真实新帕累托点——要 ROUGE 优先的
表述用它，要 MAUVE 优先保持 token 主线。是否切换主行属于论文叙事决策，不在本波
范围；两个配置的 test 数均已注册且可并列呈报。

## 8. 全家对比定位（2026-09-05，本波两个新注册点放进完整语境）

test 4×1000，R1/R2/MAUVE ×100，全部严格 2B、同数据同主干；数字由
`tools/build_master_table.py` 从 results/ 重建：

| 系统（backbone NFE） | Wikipedia | WikiSource | TinyStories | 1BW |
|---|---|---|---|---|
| **α=0.25 全链（22）** | **24.99/2.92**/0.103 | **30.39/3.41**/0.047 | **29.25/2.71**/0.006 | 9.43/0.36/**0.007** |
| token 主线（22） | 23.39/2.51/**0.120** | 29.12/3.26/0.051 | 27.60/2.42/0.006 | 9.28/0.33/0.006 |
| lognormal 全链（22） | 25.01/2.77/0.068 | 30.32/3.37/0.030 | 28.46/2.41/0.006 | 9.49/0.41/0.006 |
| BD3-LM（1024） | 23.21/2.29/0.114 | 27.53/2.67/**0.077** | 25.27/2.09/**0.006** | **9.82/0.41**/0.005 |
| AR（1024） | 21.00/2.30/0.119 | 24.29/2.44/0.074 | 14.19/0.98/0.005 | 1.89/0.05/0.005 |
| MDLM（1024） | 18.52/1.25/0.078 | 22.01/1.36/0.035 | 17.07/0.67/0.005 | 7.80/0.18/0.005 |

定位陈述（可直接进论文）：

1. **α=0.25 在流畅系统里拿下 3/4 个 benchmark 的 R1 第一**（wiki 24.99、WS 30.39、
   TS 29.25；1BW 9.43 仍次于 BD3 的 9.82），R2 同样 3/4 第一。对 BD3 的 wiki R1 优势
   从主线的 +0.2 拉大到 **+1.8**，NFE 仍是其 **1/46.5**。
2. **CADENCE 现在提供两个已注册的运行点**，同一 22-NFE 架构、只差损失权重：
   MAUVE 优先 → token 主线（11.96，全家最高，超 BD3 满预算）；ROUGE 优先 →
   α=0.25（R1/R2 大幅第一，wiki MAUVE 10.28 仅比 BD3 低 1.1）。两点都帕累托支配
   lognormal 端点（25.01/6.82：R1 只多 0.02，MAUVE 少 3.46）。
3. **2D/位置轴方向以三重否定关闭**（裸细尺度、同 NFE 对纯段轴、覆盖修正 iso-NFE），
   两个 test 注册点 `_final2D`/`_finalS24` 连同全部 sel 入库存档；采样器 NFE 的
   正确用法由覆盖曲线给出——全尺度覆盖不可省，省在尺度内日程上。
4. 老短板不变：WS MAUVE（4.66/5.10 vs BD3 7.67）与 1BW R1（9.43 vs 9.82）仍落后，
   照旧披露。

---

# 终章十三：lrseg2（chunk 真条件的 2D）+ ELF baseline 落地（2026-09-05）

## 1. mentor 的诊断与 lrseg2 修复

mentor 指出 2D 的 chunk 之间"基本没有正确的 dependency"。**代码核实：完全正确，能指到
行**——v1（`_sampler_lrseg_scale`）的 chunk 内精化走 segment 模式，而 segment 模式把
序列 reshape 成 `(B·L, S, d)`：注意力只在每个位置的 4 个段槽里跑，**chunk 之间由构造
即独立**，左到右只是切了置信度日程的作用域。这解释了 v1 全部格 ≈ 纯段轴略差。

`lrseg2`：每轮都走 **position 约定 + 逐槽 committed 码**（位置 i 的 token 携带已承诺
段、未承诺用 mask token，跨整尺度注意力）→ 当前 chunk 真条件在全部已生成 chunk 上；
chunk 内保持段轴置信度 top-k。训练侧 forward 支持**两趟采样读出按尺度带拼接**（粗带
纯 segment 约定 / 细带纯 position 约定，同一 trunk hidden——绕开 sampler_mix 的共享
参数混约定教训）。门禁：**双向机制测试**（position 模式下 chunk1 残差必须随 chunk0
的码变动、segment 模式下必须不动——v1 的独立性从此是回归测试）、每格 NFE 计数、
拼接逐带与单趟逐比特相等。

工程记录：全尺度高 NFE 臂 `b2s2f` 两次发射失败都在分支专属路径（空列表转 float 索引；
pass 交换后向 None 写入）——mask 构建已抽成 `build_lrseg2_masks` 模块级函数，两种带形
均有单元测试。**教训：只会在集群上被执行的分支 = 没被测试的分支。**

## 2. 修正后 2D 的高 NFE 扫描（b2s2e，NFE 88~256）

| 配置（采样器 NFE） | wiki | WS |
|---|---|---|
| seg粗K4+2D细C2K2（88） | 21.84/2.20/0.135 | 28.15/3.44/0.157 |
| seg粗K4+2D细C4K2（112） | 21.76/2.23/0.113 | 28.29/3.53/0.151 |
| seg粗K4+2D细C4K4（160） | 22.16/2.25/0.134 | 28.25/3.44/0.168 |
| **seg粗K4+2D细C8K2（160）** | 22.02/2.23/**0.173** | 28.00/3.55/**0.172** |
| seg粗K4+2D细C8K4（256） | 22.23/2.29/0.136 | 28.26/3.42/0.188 |
| 对照 seg:all:4 同臂（88） | 22.23/2.25/0.142 | 28.16/3.47/0.165 |
| 参照 seg:all:4 @b2sg（88） | 22.33/2.33/0.229 | 28.62/3.65/0.204 |

判读（诚实版）：①**首次**有 2D 格在两个 sel 集上同时高于同臂 seg 对照（C8K2@160：
+3.10/+0.77），方向一致但 wiki 差距在噪声带内；②NFE 不单调（112 最差、256 无增益）；
③**臂本身弱**：同一 seg:all:4 解码在 b2s2e 只有 14.17（主线臂 22.89）——sampler_lrseg2
把细带监督摊到 4 个 C 值且换了约定，1000 步不够两个约定都训熟。**目前没有任何 2D 格
接近主线 22.89 的上限**；④同臂对照本身还有分布瑕疵（细带只训过 position 约定，
`seg` 读出在细带 OOD）——分布内对照 `lrseg2 C1K4`（_e88b）补测中，判读以其为准。
512/1024 档等 `b2s2f`（第三次发射在跑）。

## 3. ELF baseline：两个坑、一个单变量大发现（初步）

### 3.1 坑一（我的）：EMA 视界没按预算重标

论文 `ema_decay=0.9999` 按 95k 步标定（初始化残留 e^−9.5）；照抄到 7630 步后
**EMA 里还有 0.9999^7630 ≈ 47% 的随机初始化**，而采样读 EMA → 全部输出是冒号刷屏。
教师强制重建定位（干净 latent，λ=清洁度）：

| 权重 | λ=0.95 | λ=0.7 |
|---|---|---|
| 原始权重 | **100.00%** | **98.34%** |
| 误标 EMA | 50.78% | 38.67% |

训练本身健康（loss 3.24→0.70 平滑）。修复：`--use_ema false` 用原始权重；同时以重标
衰减 0.99876 重训两臂（pre2/rnd2），pre2 的 EMA sel 与 raw 相当或更好。**普适教训：
把 95k 步 recipe 裁到 7.6k 步时，一切与时长耦合的常数（EMA 视界、warmup、日程）都要
重标。**

### 3.2 坑二（我的）：漏了全家的词数截断协议

全家系统的生成都被 `run_benchmark` 截断到参考词数（实测 gen/ref=1.00）；
`generate_elf` 漏了这步（1.92×）→ ROUGE/MAUVE 被长度混淆。已修，9 条截断版 sel 重跑
中（截断是确定性后处理、种子不变，不构成新抽样）。

### 3.3 初步结果（未截断协议，定性可用、定量待截断版）

| ELF 配置 | sel wiki | sel WS |
|---|---|---|
| pre（预训练 T5 enc）ODE32 CFG2 | 20.97/3.91/**0.284** | 25.88/5.53/**0.524** |
| pre ODE64 CFG2 | 20.96/4.18/0.234 | 25.34/5.49/0.445 |
| rnd（随机冻结 enc）三配置 | 13~14/1.1/**0.76~1.25** | 17~18/1.6/0.7~1.2 |
| pre2（重标 EMA）ODE32 CFG2 | 22.55/4.46/0.268 | 27.83/6.00/0.546 |

test（4×1000，未截断）：pre wiki 21.50/3.81/0.234、WS 7.66、**TS 14.37/1BW 2.82
（短 prompt 崩，与 AR 同型）**；rnd 全地板。

**单变量结论（已稳，不依赖截断）**：同数据、同预算、同 DiT，唯一差别=嵌入空间是否
预训练——pre 臂流畅、rnd 臂地板。**「连续隐扩散在 2B 档整体不成立」必须收窄为
「自含嵌入空间的连续隐扩散在 2B 不成立」**：我们此前两个实现与 ELF 的 rnd 臂都符合
收窄后的命题；ELF 的流畅性由 35M 外部预训练 encoder（C4 ~1T token）**进口**而来，
与全家族"零外部预训练权重"口径不对称，进对比表必须带脚注、不能与家族数并列同读。
定量表述（含 MAUVE 高低）待截断版 sel/test 落地后写定。

## 4. 终数（2026-09-05 傍晚）：2D 的天花板与 ELF 的正式四方对比

### 4.1 纯 2D NFE 阶梯（b2s2f，`--chunks 32` 全尺度臂；sel n=250）

| 配置（采样器 NFE） | wiki | WS |
|---|---|---|
| C=1 K=4（88，同臂对照，无 chunk 结构） | 21.95/2.33/0.186 | 28.03/3.57/0.083 |
| C=2 K=4（168） | 21.99/2.40/0.199 | 28.33/3.59/0.086 |
| C=4 K=4（312） | 22.10/2.37/0.129 | 28.13/3.61/0.365* |
| C=8 K=4（568） | 22.21/2.40/0.158 | 28.28/3.51/0.151 |
| **C=16 K=4（1016）** | 22.11/2.37/**0.210** | 28.36/3.51/**0.189** |
| 主线 seg:all:4 @b2sg（88） | 22.33/2.33/0.229 | 28.62/3.65/0.204 |

（*312 档的 WS 36.48 是孤立高点，wiki 同档反而最低——按 §3.3（终章十一）的噪声测量
读作离群，不采信。）

**2D 的性能天花板判读**：①最高档 C16K4@1016 对同臂对照两集同向胜出（+2.4/+10.6），
与 b2s2e 的 C8K2（对分布内对照 e88b 15.38/12.56 同向 +1.9/+4.7）互相印证——**修好
chunk 条件后，2D 信号是真实的、可复现的**；②但阶梯非单调，且**天花板只是追平主线**：
花 11.5× 的采样 NFE（1016 vs 88），拿到 21.01/18.92 vs 主线 22.89/20.42（噪声内持平）。
③结论：chunk 级条件在本架构上**有效但不经济**——它买到的东西，段轴置信度序在 88 NFE
下已经全部买到了。

**天花板注册数（`_final2Dv2` = lrseg2:all C16K4，采样器 NFE 1016，4×1000）**：

| bench | 2D 天花板 | 主线 seg:all:4（88） |
|---|---|---|
| wikipedia | 23.22/2.47/0.106 | 23.39/2.51/**0.120** |
| wikisource | 29.05/3.25/0.044 | 29.12/3.26/**0.051** |
| tinystories | **27.72/2.46**/0.006 | 27.60/2.42/0.006 |
| 1BW | 9.01/0.34/0.006 | **9.28**/0.33/**0.006** |

四集全部与主线持平略低——sel 的判读在 test 上逐字兑现。**P0/2D 方向就此完整关闭**：
机制有效（两臂 sel 互证）、经济无意义（11.5× NFE 换不来任何指标）。

### 4.2 ELF 正式 test（截断协议、重标 EMA、4×1000）——全表新格局

| benchmark | **ELF pre2** | ELF rnd（消融） | CADENCE α=0.25 | CADENCE token | BD3-LM |
|---|---|---|---|---|---|
| wikipedia | **28.35/5.06/0.269** | 17.09/1.35/0.007 | 24.99/2.92/0.103 | 23.39/2.51/0.120 | 23.21/2.29/0.114 |
| wikisource | **31.28/5.25/0.132** | 23.48/1.90/0.006 | 30.39/3.41/0.047 | 29.12/3.26/0.051 | 27.53/2.67/0.077 |
| tinystories | **34.49/6.02/0.007** | 24.07/1.67/0.005 | 29.25/2.71/0.006 | 27.60/2.42/0.006 | 25.27/2.09/0.006 |
| 1BW | **11.53**/0.59/0.005 | 9.36/0.31/0.005 | 9.43/0.36/**0.007** | 9.28/0.33/0.006 | 9.82/0.41/0.005 |

读法（必须整段进论文，不可只引数字）：

1. **ELF pre2 在四个 benchmark 的 R1/R2 全部第一、三个 benchmark 的 MAUVE 第一**
   （仅 1BW MAUVE 低于我们）。早前"TS/1BW 崩"是未截断协议 + 误标 EMA 的假象，
   修正后消失。
2. **但这不是"ELF 方法赢了 2B 对比"**：同一方法、随机 encoder 的消融臂全线退化
   （MAUVE 0.45~0.74 地板；其 R1 是高频词面重合，与 SSD-LM 同类，不可与流畅系统
   并读）。**差距的全部来源是那 35M 外部预训练 encoder（C4 ~1T token）**。
3. 资源口径三重不对称，进表须脚注：外部预训练权重（1T-token 语义预训练 vs 我们
   39.3B 自语料重建 tokenizer）；推理 NFE（≈128+ backbone 前向 vs 我们 22）；
   tokenizer（T5 词表，同语料重新 tokenize）。
4. **对论文叙事的净影响**：CADENCE 保持"**零外部权重、低 NFE 档**"内的最强
   （对 BD3/AR/MDLM 的结论全部不变）；ELF pre2 定义了"**进口预训练表示空间**"这一
   资源档的上限，且其消融恰好反向支撑我们收窄后的命题——**自含嵌入空间的连续隐扩散
   在 2B 不成立**。两档并列呈报、脚注分明，是最诚实也最有信息量的表法。
5. 方法学上值得写一句：把预训练空间当**生成基底**（ELF）远强于当**蒸馏目标**
   （TextLDM 的 REPA，同为外部知识注入却崩了）——这是本项目对"外部知识如何进小
   预算生成模型"的一条实证。

## 5. 2D 全结果汇编（2026-09-07，一处看全——供论文与组会直接引用）

每格 = R1/R2/MAUVE ×100。协议：sel（n=250）按预注册只建 wikipedia/wikisource 两个
选择集（TS/1BW 无 sel 切分，防选择泄漏）；四 benchmark 全量只在 test 注册枪（4×1000，
每配置一枪）时跑。参照系：主线 `seg:all:4` @88 —— sel 22.33/2.33/**0.229** |
28.62/3.65/**0.204**；test 23.39/2.51/**0.120** | 29.12/3.26/0.051 | 27.60/2.42/0.006 |
9.28/0.33/0.006。

### 5.1 test 注册点（4 benchmark）

| 配置（采样器 NFE） | Wikipedia | WikiSource | TinyStories | 1BW |
|---|---|---|---|---|
| 2D-v1 赢家 C8K1@细（48） | 23.05/2.28/0.091 | 28.86/3.06/0.030 | 26.98/2.20/0.006 | 8.67/0.23/0.006 |
| 段轴@细尺度锚 C1K4（24） | 23.10/2.32/0.077 | 29.13/3.10/0.043 | 27.21/2.25/0.006 | 8.79/0.28/0.006 |
| 2D-v2 天花板 C16K4 全尺度（1016） | 23.22/2.47/0.106 | 29.05/3.25/0.044 | 27.72/2.46/0.006 | 9.01/0.34/0.006 |

### 5.2 sel 全网格（NFE 16→1016）

**v1 lrseg（chunk 独立），`b2s2d`，细尺度 {8,9,10}**：

| 配置（NFE） | sel_wikipedia | sel_wikisource |
|---|---|---|
| C2K4（48） | 21.93/2.07/0.117 | 28.28/3.30/0.143 |
| C8K1（48） | 22.14/1.99/0.156 | 28.29/3.23/0.160 |
| C4K2（48） | 21.98/1.99/0.143 | 28.48/3.29/0.102 |
| C2K2（24） | 22.09/2.02/0.100 | 28.49/3.33/0.191 |
| C4K1（24） | 22.06/2.00/0.113 | 28.34/3.27/0.135 |
| 锚 C1K4（24） | 21.98/2.02/0.136 | 28.19/3.33/0.129 |

**v1 混合覆盖，`b2s2dm`（seg粗 + v1细）**：

| 配置（NFE） | sel_wikipedia | sel_wikisource |
|---|---|---|
| seg粗K2+细C2K1（44） | 21.77/2.06/0.154 | 28.20/3.41/0.092 |
| seg粗K4+细C2K2（88） | 21.84/2.13/0.157 | 28.31/3.58/0.178 |
| seg粗K4+细C4K2（112） | 21.86/2.07/0.148 | 28.34/3.50/0.171 |
| 对照 seg:all:4 同臂（88） | 21.98/2.12/0.191 | 28.40/3.56/0.311 |

**v2 lrseg2（chunk 真条件）混合帧，`b2s2e`（seg粗K4=64 + 2D细）**：

| 配置（总 NFE） | sel_wikipedia | sel_wikisource |
|---|---|---|
| 细C2K2（88） | 21.84/2.20/0.135 | 28.15/3.44/0.157 |
| 细C4K2（112） | 21.76/2.23/0.113 | 28.29/3.53/0.151 |
| 细C4K4（160） | 22.16/2.25/0.134 | 28.25/3.44/0.168 |
| **细C8K2（160）★** | 22.02/2.23/**0.173** | 28.00/3.55/**0.172** |
| 细C8K4（256） | 22.23/2.29/0.136 | 28.26/3.42/0.188 |
| 分布内对照 细C1K4（88） | 22.07/2.24/0.154 | 28.26/3.45/0.126 |
| 对照 seg:all:4 同臂（88，细带 OOD） | 22.23/2.25/0.142 | 28.16/3.47/0.165 |

**v2 纯 2D 全尺度，`b2s2f`（--chunks 32）**：

| 配置（总 NFE） | sel_wikipedia | sel_wikisource |
|---|---|---|
| 对照 C1K4 无 chunk（88） | 21.95/2.33/0.186 | 28.03/3.57/0.083 |
| C2K4（168） | 21.99/2.40/0.199 | 28.33/3.59/0.086 |
| C4K4（312） | 22.10/2.37/0.129 | 28.13/3.61/0.365（离群，按噪声测量不采信） |
| C8K4（568，≈512 档） | 22.21/2.40/0.158 | 28.28/3.51/0.151 |
| **C16K4（1016，≈1024 档）★** | 22.11/2.37/**0.210** | 28.36/3.51/**0.189** |

### 5.3 汇编读法（进论文的三句话）

1. **R1/R2 对 2D 的一切设置几乎不动**（21.8~22.3 / 2.0~2.4）——2D 的全部效应在 MAUVE。
2. **v2 的 chunk 条件真实但小**：两臂最佳格对各自无-chunk 对照均两集同向（+1.9/+4.7 与
   +2.4/+10.6），NFE 曲线锯齿不单调（112<88、568<168）。
3. **任何 NFE 档均未超过主线**：sel 峰值 21.01 vs 22.89、test 峰值 10.55 vs 11.96
   ——11.5× 采样算力只到追平边缘。方向以完整证据链关闭。

**待决**（用户已知悉，未拍板）：反摊薄实验——训练 C 固定为部署值（8/16）重训
`b2s2e2`/`b2s2f2`，单变量量化"混合抽 C 的监督摊薄"是否是 v2 臂偏弱（同臂 seg:all:4
14.17~19.09 vs 主线臂 22.89）的原因。

## 6. ELF 三点归因：完结（2026-09-07）

公平臂 encoder `t5enc_owt2_ours` 预训练完成：150k 步 = 39.3B token（与我们冻结
tokenizer 的侧预算逐字节同额），MLM masked-acc **61.3%**、loss 2.05——健康收敛。
公平臂 ELF-B 2B 训练（`--encoder local` + 重标 EMA 0.99876）完成，3 条 sel +
test 一枪（`_finalELFOURS`）全部落地。

### 6.1 sel（n=250，截断协议，R1/R2/MAUVE×100）

| encoder 出处 | sel_wikipedia | sel_wikisource |
|---|---|---|
| 随机冻结（地板，raw，s32c2 最好） | 15.00/0.94/0.013 | 20.03/1.60/0.014 |
| **39.3B 自语料 ODE64 CFG2** | **28.22/5.41/0.318** | **34.16/7.09/0.563** |
| 39.3B 自语料 ODE64 CFG1 | 25.34/3.84/0.237 | 30.86/4.91/0.494 |
| 39.3B 自语料 ODE32 CFG2 | 28.05/5.15/0.333 | 34.08/6.73/0.448 |
| C4~1T（pre2，s64c2） | 28.25/5.24/0.286 | 34.32/7.16/0.622 |
| 参照：CADENCE token 主线 | 22.33/2.33/0.229 | 28.62/3.65/0.204 |

配置按预注册规则选 **ODE64 CFG2**（wiki MAUVE 33.30 vs 31.83 差 1.47 < 2.5 带宽
→ 平手；R1 平手带内 → 以 WS MAUVE 56.34 vs 44.82 决）。

### 6.2 test（4×1000，一次性）

| 系统 | wiki | WS | TS | 1BW |
|---|---|---|---|---|
| ELF 公平臂（39.3B 自语料） | **28.99/5.25/0.307** | **32.14/5.34/0.253** | 33.76/6.25/0.006 | 10.80/0.45/0.006 |
| ELF pre2（C4~1T） | 28.35/5.06/0.269 | 31.28/5.25/0.132 | 34.49/6.02/0.007 | 11.53/0.59/0.005 |
| ELF rnd（随机冻结） | ≈地板（MAUVE 0.45–0.74） | | | |
| CADENCE 主线 seg:all:4 | 23.39/2.51/0.120 | 29.12/3.26/0.051 | 27.60/2.42/0.006 | 9.28/0.33/0.006 |

### 6.3 结论：中间点坐在天花板上

1. **不是外部数据规模的功劳**。自语料 39.3B encoder 的公平臂与 1T C4 版本在
   sel 上统计不可分，test 上 wiki/WS MAUVE 甚至**更高**（30.75 vs 26.89、
   25.25 vs 13.21——域内 encoder 反而占优；TS/1BW 与全家一样在地板）。
   "ELF 借了 1T 外部知识所以不公平"的异议就此消解。
2. **公平口径下 ELF 仍然领先主线**。两阶段预算形状现在完全对称：
   39.3B encoder + 2B flow vs 我们 39.3B tokenizer + 2B AR，同语料同 test 协议。
   差距是真的，不是资源不对称的伪影。
3. **归因收窄为机制而非数据**："预训练上下文嵌入空间作为生成基底"本身就是
   有效成分（随机空间=地板，任何像样的预训空间=天花板），且我们自己的侧预算
   就足以训出这个空间。与终章十三 §3 的 TextLDM/REPA 负结果合读：预训空间
   作**生成基底**有效、作**蒸馏目标**无效。
4. 遗留不对称仅剩两条（如实脚注）：~128 vs 22 NFE；T5 词表 vs GPT-2 词表。

工程连带（已单独成 commit）：`export-ignore` 修 293MB 快照慢链路超时（b37e373）、
submit 前断言 entry 存在（40fc14e）、`ENC_FULL` 恢复本地预训 encoder（8e6ff11）。

## 7. 多掩码训练波（mentor 2026-09-06 方向，在飞）

设计（`--sampler_mask_samples M`，commit 8b738d0）：每个昂贵的 trunk 前向
（blueprint h）上，独立采 M 个**合法**解码态（掩码分布一字未动），共享采样头在
同一 h 上跑 M 遍，M 份逐样本归一 CE 取平均。M=1 与旧路径逐比特等价（门禁测试
`test_multi_state_forward_matches_single_state_calls_bitwise`）；训练日志记
每 blueprint 监督槽数 / M / 峰值显存 / 步时。按 mentor 意见**不固定 chunk size**
（混合 C 保留推理端 test-time scaling 自由；固定 C 重训矩阵已按用户指示取消）。

六臂（各 2×8×H100，1000 步同预算）：2D 两遍拼接帧 `b2s2eM{2,4,8,16}` +
纯段轴主线帧 `b2sgM{4,16}`（与 `b2sg` 单变量 = M，直接回答"最好结果能否再涨"）。

**首发全灭的复盘（2026-09-07 07:20）**：六臂在入口第 60 秒齐死于
`no SRC ckpt`——不是检查点丢失（`b2sq1` 自 09-01 一直在 Volume 上），而是提交
命令把 6 个 env 键拼成了一个带引号整串，`SRC_FULL` 的值吞掉了后续全部
`KEY=VAL`（日志两处铁证：报错路径后面跟着一串本不该在路径里的 env 文本；开头
一行 config/codes/data 全部回落默认值 wt103/hybrid）。修复 = 每键独立 argv 重发
并核对生成 yaml。教训并入 submit 纪律：**发车后 cat 生成的 `.job-*.yaml` 核对
env_variables 每键一行**，与"entry 必须存在"同级。

重发六臂 run ID：918045047866126 / 507654946222421 / 281265648796269 /
458303924051467（2D M2/4/8/16）、407763998100664 / 363594366913867（seg M4/16）。

### 7.1 结果：M=8 是甜点，收益只属于监督被摊薄的臂

**2D 臂 M 阶梯**（b2s2eM{2,4,8,16} 各 7 格 × 2 sel 集，与 b2s2e(M=1) 同格同 tag，
完整数字进 MASTER_TABLES）：跨 7 格平均 wiki R1 = 22.04→22.07→22.13→**22.26**→22.01
（M=1→2→4→8→16），WS R2 = 3.47→3.63→3.60→**3.73**→3.60——**R1/R2 两个 sel 集
都在 M=8 峰值，且 M8>M1 在 14/14 格无一例外；M=16 倒 U 回落**。MAUVE 单格仍在
n=250 噪声带（wiki 均值随 M 缓升 14.1→18.4 但 WS 不确认，按两集一致性规则不采信）。

**注册点 = M8 C2K2@88**：wiki 22.29/2.31/**0.228**（同格 M1 13.52，两集同向
+9.2/+3.0）、WS 28.80/3.82/0.187——**88 NFE 上首次逼平主线 seg:all:4 的
22.89/20.42**（此前 2D 天花板要 1016 NFE 才追平）。按预注册规则（wiki MAUVE
优先，Δ5.9 > 2.5 带宽直接胜出）test 一枪 `_finalM8` 已发。

**纯段轴主线（mentor 问题二）：无收益**。b2sg(M=1) 22.33/2.33/0.229 |
28.62/3.65/0.204；M=4 22.39/2.30/0.155 | 28.54/3.53/0.172；M=16
22.58/2.30/0.124 | 28.72/3.68/0.210——R1/R2 全平（±0.25），MAUVE 两集不同向，
不做方向性结论。**机制读法：多掩码=恢复被摊薄的监督效率（2D 混合 C 训练臂收益
真实且随 M 到 8 递增），对监督已饱和的主线中性**——这本身是可写的机制结果。

**成本遥测**（每 blueprint）：监督槽 ≈ 线性于 M（M2 21.9k / M4 81.9k[seg] /
M16 327k）；峰值显存 13.1→28.9GB（M2→M16，16×H100 余量充足）；步时 M8≈2.1s、
M16≈2.4s（vs M1≈1s）——一次 trunk 前向摊薄 M 次采样头的成本曲线符合设计预期。

### 7.2 全阶段链 + 全量数据链（在飞）

用户拍板方向 = 严格 2B 从头多掩码（A 方案）+ 全量 12.8B"结果最好"版：
- **fs 四臂**（严格 2B：共享基座 b2nd 5630 + 采样阶段整段 2000 步 = 7630）：
  `b2sgfs{1,8}`（段轴 M=1/8）、`b2s2fs{1,8}`（2D 两带 M=1/8）。fs1 对照隔离 M；
  fs1-vs-b2sg 附带回答"跳过 mg 阶段值不值"。trunk 在采样阶段全程收梯度
  （finetune_prefix_maskgit.py 只冻 depth_projs），即 trunk 被多掩码目标
  共适应 2000/7630 = 26% 预算。
- **全量基座 `b12nd`**（12.8B 档，单独预算档不进 2B 主表）：从 0 训 36000 步
  （warmup 按时长同比放大 400→2000），codes 覆盖全量（train 12,423,461 窗
  ≈12.7B token，36000+12800 ≈ 1 epoch）。落地后分叉两条链
  （seg:all:4 主线 + 2D M8），后段切法（12800 纯多掩码采样 vs 6400 mg+6400 采样）
  由 fs 实测定。

### 7.3 fs 结果：mg 不可省 + 段轴 M8 空洞被补上（修正一条早期结论）

- **mg 阶段是真杠杆**：b2sgfs1（同预算、跳 mg、采样阶段 2000 步）对 b2sg：
  wiki MAUVE 22.89→14.02、WS 20.42→16.60 两集同向掉。2D 侧同结论
  （b2s2fs8 注册格 wiki 14.88 ≪ 含 mg 的 b2s2eM8 22.75）。
- **段轴 M=8 有收益——修正「纯段轴多掩码无收益」**：早前只测了 M4/M16，
  M8 恰好是空洞。b2sgfs8 对 b2sgfs1（唯一变量 M）全指标两集同涨，
  WS MAUVE 16.60→**26.48**（段轴 sel 历史最高）。2D 侧 fs8 对 fs1 注册格
  同样两集同涨（12.66→14.88 / 14.24→21.78）。
- **锁定全量链配方**：36000 基座 + 6400 mg + 6400 多掩码采样（M=8），
  段轴另跑 M=1 对冲。

## 8. 全量 12.8B 档（独立预算档，与 2B 主表不并读）

链 = `b12nd`（36000 步从 0，loss 4.19→2.63）→ `b12sq`（6400 mg）→ 三臂采样阶段
（`b12sg8`/`b12sg1`/`b12s2e8`），旧最优配方逐段 6.4× 等比放大，总步数 48800 ≈
1 epoch 不重复过数据。

**sel（n=250）**：

| 臂 | wiki | WS |
|---|---|---|
| 2B 主线 b2sg（参照） | 22.33/2.33/0.229 | 28.62/3.65/0.204 |
| 全量段轴 M=1（旧配方放大本体） | 27.77/4.03/0.141 | 33.47/5.63/0.309 |
| 全量段轴 M=8 | 27.71/4.19/0.192 | 33.67/5.80/0.251 |
| 全量 2D M=8 @C2K2（88） | 28.25/4.30/0.185 | 33.78/5.89/0.191 |
| 全量 2D M=8 @seg:all:4 | 27.97/4.08/0.209 | 33.69/5.75/0.190 |

三个读数：①**词面大涨且稳**（R1 +5.4~6.0、R2 +1.7~2.0，所有臂两集一致）；
②wiki MAUVE 回落 / WS MAUVE 大涨（两集不同向，test 定夺）；③**全量下混合训练
不再摊薄**——2D 臂的 seg:all:4 对照（20.89）反超段轴专训臂（19.21），2B 档的
"训练摊薄"是小数据现象。赢家按规则：段轴=M8（wiki Δ5.1>2.5）、2D=C2K2@88。

**test 终表（4×1000，一次性）**：

| 系统 | wiki | WS | TS | 1BW |
|---|---|---|---|---|
| 全量·2D M8 C2K2@88 | **29.00/4.43**/0.140 | **32.01/4.66**/0.058 | **33.72/5.01**/0.006 | 9.53/0.45/0.006 |
| 全量·段轴 M8 | 28.85/4.34/**0.144** | 31.57/4.38/**0.075** | 33.11/4.84/0.006 | 9.53/0.38/0.005 |
| 全量·段轴 M1（缩放锚点） | 28.87/4.33/0.125 | 31.67/4.49/0.071 | 33.31/4.92/0.006 | 9.15/0.48/0.006 |
| 2B 主线 _finalSEG | 23.39/2.51/0.120 | 29.12/3.26/0.051 | 27.60/2.42/0.006 | 9.28/0.33/0.006 |
| BD3-LM（2B） | 23.21/2.29/0.114 | 27.53/2.67/0.077 | 25.27/2.09/0.006 | 9.82/0.41/0.005 |
| ELF 公平臂（39.3B enc+2B flow） | 28.99/**5.25/0.307** | 32.14/5.34/0.253 | 33.76/6.25/0.007 | 10.80/0.45/0.006 |

**终局结论**：
1. **sel 上的 wiki MAUVE 回落在 test 没有出现**，反而全面反超：全量段轴 M8
   wiki MAUVE 14.40 > 2B 主线 11.96（+2.4），WS 7.51 > 5.10——第 N 次提醒
   sel 单格 MAUVE 不可作方向判断。
2. **数据缩放的分解（M1 锚点）**：纯加数据 6.4× = R1 +5.5、R2 +1.8、
   wiki MAUVE +0.5；多掩码 M8 再 +1.9 wiki MAUVE（真实但温和）。
3. **全量 2D M8 = 词面新旗舰**：R1/R2 在 wiki/WS/TS 全量三臂最高，
   wiki R1 29.00 与 ELF 公平臂 28.99 **打平**（NFE ≈1/6、零外部权重、
   GPT-2 词表同口径）；R2 与 MAUVE 仍是 ELF 领先（4.43 vs 5.25；
   14 vs 31）——连续基底在分布保真上的优势依旧，词面差距已抹平。
4. 预算披露：本档 48800 步 vs baseline 的 7630，表格永远分层呈现。

## 9. 公平性收尾波（2026-09-08，用户三点指令，在飞/部分落地）

### 9.1 ELF 拉齐配置

**NFE 对齐（已落地，test 4×1000）**：ELF 公平臂压到 ODE11×CFG2 = **22 前向**
（与家族相同）：wiki 27.39/4.13/0.245、WS 31.15/4.34/0.095、TS 32.24/4.90/0.006、
1BW 10.14/0.34/0.006。**降档但不塌**（对照：BD3 在 NFE 对齐时 wiki MAUVE 塌到
0.58）；WS MAUVE −63% 说明其优势相当一部分由 NFE 购买，但同预算+同 NFE 下
wiki 24.49 仍远高于 2B 主线 11.96——**预训练嵌入空间基底的机制优势为真**。
**预算对齐（已落地）**：`ours12` = 公平臂 flow 模型重训至 48800 步（12.8B，
与全量档同额；encoder 不变；EMA 视界按同一缩放规则 0.99876→0.999805）。

**全对齐终表（test 4×1000）**：

| 系统（12.8B 档） | wiki | WS | TS | 1BW |
|---|---|---|---|---|
| ELF-12.8B @128fwd（本配方） | 30.48/6.24/**0.358** | 33.07/6.72/**0.284** | 33.63/6.42/0.006 | 9.76/0.36/0.005 |
| **ELF-12.8B @22fwd（全对齐格）** | 28.50/4.62/0.178 | 31.78/4.89/0.094 | 31.09/4.36/0.006 | 9.95/0.28/0.005 |
| CADENCE 全量 2D M8 @22fwd | **29.00**/4.43/0.140 | **32.01**/4.66/0.058 | **33.72/5.01**/0.006 | 9.53/0.45/0.006 |
| CADENCE 全量段轴 M8 @22fwd | 28.85/4.34/0.144 | 31.57/4.38/0.075 | 33.11/4.84/0.006 | 9.53/0.38/0.005 |

**公平性收尾定论**：ELF 原生配方下 25 分的 wiki MAUVE 鸿沟（35.76 vs 13.99），
在同语料+同侧预算+同生成预算+对齐 NFE 的**全对齐格**收缩到 **3.8 分**；R1 反而
CADENCE 高 0.5、TS R1 高 2.6。**ELF 支配性优势的分解：NFE（大头，128→22 掉
18 分 MAUVE）> 数据档（中，2B→12.8B 涨 5 分）> 机制残差（~4 分 MAUVE +
更流利/更保守，见 §9.3 熵-PPL 权衡）**。数据缩放对两家同样有效（ELF wiki
MAUVE 30.75→35.76）。全对齐后两系统是"各有所长的同侪"：CADENCE 占 R1/TS/
参考级多样性/低 NFE 原生，ELF 占 R2/MAUVE/流利度。

### 9.2 协议变更（事后指标调整，如实披露）

用户指令：**MAUVE 特征化长度 256→1024；新增 Gen-PPL 与 unigram 熵为主指标；
R1/R2 降为辅助指标**。这是在看过全量档结果之后的主指标变更，所有引用处注明。
实现 = `tools/eval_extra.py` 对**已注册 gens 文件重打分、不重新生成**：
- `mauve_1024`：仅改 max_text_length（featurizer/簇数不动，同 n 内可比）；
- `gen_ppl`：gpt2-large 判官（MAUVE featurizer 同款外部模型），以 prompt 为
  条件只对续写 token 计损，corpus 级 + 逐句均值双口径（"GNPPL"以用户给出的
  出处为准对齐命名）；
- `unigram_entropy`：生成/参考双侧（**参考侧锚点防判官 PPL 被低熵词汤刷低**，
  SSD-LM 特意留在清单里作照妖镜——ROUGE-1 可刷高的教训在判官 PPL 上同样成立）。
清单 44 文件（CADENCE top3 + 2B 注册行 + α=0.25 + BD3/AR/MDLM/ELF×2/SSD-LM
× 4 benchmark），**44/44 已落地**（完整表在 MASTER_TABLES 尾部三张）。

### 9.3 新指标结果：三个发现

1. **MAUVE@1024 全场塌到地板**（所有系统 0.4~1.4，对照 @256 的 5~30）：评满
   全长后没有任何系统能匹配参考分布——**256 截断此前承担了全部区分度**（它评
   的是续写的前半段）。ELF 相对仍最高（1.2~1.4）但绝对量级塌 ~25×。@1024 作
   为主指标缺乏分辨力，建议主表并列呈现 @256（区分度）与 @1024（诚实的全长
   上界）。
2. **判官 PPL 的可刷性被锚点当场抓住**（不与熵并读必被骗）：SSD-LM 判官 PPL
   87~125 比 CADENCE 还"低"，但熵 6.9~7.5（参考 10.1~11.3）=低熵词汤；AR 在
   1BW corpus-PPL 5.6 / 逐句 55868 = 退化重复。ROUGE-1 可刷高的老教训在判官
   PPL 上同样成立。
3. **真实机制权衡浮出（新主指标下的叙事）**：
   - CADENCE 全量档：熵 11.18~11.23 ≈ 参考 11.34（**多样性到位**），判官 PPL
     271~366（≈BD3 档，逐 token 仍噪）；对 2B 主线 PPL 653→338 = 数据缩放
     大幅改善流利度。
   - ELF：判官 PPL 83~95（最接近参考 20 的系统），但熵低于参考 0.4~0.55 bits
     （**分布略压缩**）——"更流利但更保守"。
   - 全量档内部：2D M8 判官 PPL 全面最低（wiki 338 vs 段轴 363/366）＋熵
     略低 0.05 bits，与 R1/R2 最高一致 → **新指标下全量 2D M8 仍是 CADENCE
     注册点**。

### 8.1 全量档 2×2 收官（2026-09-09）：α=0.25 的 2B 增益不随数据缩放存活

段轴族 {token, α=0.25} × {M1, M8} 全部落地（test 4×1000）：

| 链 | wiki | WS | TS | 1BW |
|---|---|---|---|---|
| token M1 | 28.87/4.33/0.125 | 31.67/4.49/0.071 | 33.31/4.92/0.006 | 9.15/0.48/0.006 |
| token M8 | 28.85/4.34/**0.144** | 31.57/4.38/**0.075** | 33.11/4.84/0.006 | 9.53/0.38/0.005 |
| α=0.25 M1（支内规则赢家） | 28.40/4.40/0.140 | 31.07/4.58/0.066 | 33.37/**5.38**/0.006 | **10.17/0.56**/0.006 |
| （2D M8，参照） | **29.00/4.43**/0.140 | **32.01/4.66**/0.058 | **33.72**/5.01/0.006 | 9.53/0.45/0.006 |

**结论（判官指标落地后修正）**：2B 档 α=0.25 的"R1/R2 八格全涨"在 12.8B
不复现（wiki/WS R1 反降 0.5/0.6），**但其判官 PPL 是 CADENCE 全族最低**
（274/254/129/733，四集 −20~30%，熵保持 11.12）——更准确的判词是
**"α 重加权在 12.8B 把收益从词面换成了流利度"**，外加短 prompt 域残值
（1BW R1 10.17 = 家族最高）。12.8B 注册点维持 2D-M8（词面）/段轴 M8
（MAUVE），α-M1 作为"流利度侧"披露行。sel 侧 α-M1 的 wiki MAUVE 20.49
未迁移到 test（14.01）——sel 单格 MAUVE 不可判方向的第 N+1 次。
另：ELF@22 在 1BW 同时不流利且压缩（PPL 1439、熵 8.65 vs 参考 10.06），
是全对齐档其最差一格。

### 8.2 Full-recipe no-CFG comparison（2026-09-10；原'实验 1&2'，按 9-11 邮件更名——两套 recipe 各自最优的整体对比，非单变量消融）

两条全链从 0 重训（48800 步，全链 cond_drop_p=0；解码单分支 NFE 11+44，
时延估算 ~0.29s/样本 = CFG 版的 53%）：

| 链 | wiki | WS | TS | 1BW |
|---|---|---|---|---|
| 无 CFG（p5hot） | 24.81/2.95/0.088 | 28.88/3.12/0.045 | 26.57/2.23/0.006 | 9.60/0.32/0.006 |
| 无 CFG + α=0.25（p5cold） | 25.86/3.46/0.110 | 30.26/3.81/0.052 | 28.90/2.96/0.006 | **10.50**/0.42/0.005 |
| 有 CFG b12s2e8（参照） | **29.00/4.43/0.140** | **32.01/4.66**/0.058 | **33.72/5.01**/0.006 | 9.53/0.45/0.006 |

1. **关 CFG 代价严重**（R1 −4.2/−3.1/−7.2，R2 −1.5，wiki MAUVE −5.1）——
   数据放大 6.4× 也没有让 CFG 变得可省；NFE/时延减半买不回质量。
   注册配置维持 CFG-on。等时延对照：无CFG+α ~0.3s vs ELF@22fwd 0.3s，
   后者 wiki 28.50/4.62/0.178 全面占优 —— 无 CFG 路线在等时延口径也不成立。
2. **α 的价值是 regime 依赖的**：有 CFG 时词面增益为零（§8.1），无 CFG 时
   显著回归（R1 +1.05~+2.3、MAUVE +2.1、1BW R1 10.50=家族新高）——
   损失重加权与 CFG 是部分替代品（都在做"聚焦难成分"的事）。
3. 温度谱交互：无 CFG 素链选 p5hot、α 链选 p5cold（sel 两集规则），
   与 2B 无 CFG 时代的发现一致（无 CFG 下逐尺度调度是硬杠杆）。

### 8.3 Controlled inference-time CFG ablation（2026-09-11，按邮件需求执行）

审计先行：两组 resolved config 逐项断言除 cfg_schedule（7,…,1.5 vs 全 1）外
完全一致（同 ckpt b12s2e8/同种子/同 C2K2/同温度谱/同协议；cfg=1.0 走精确
单分支已核到代码行）。

| Benchmark | CFG ON（注册, 22+88） | 同 ckpt CFG OFF（11+44） | Δ | full-recipe no-CFG |
|---|---|---|---|---|
| Wikipedia | 29.00/4.43/0.140 | 24.75/2.94/0.088 | −4.24/−1.49/−5.15 | 24.81/2.95/0.088 |
| WikiSource | 32.01/4.66/0.058 | 28.85/3.16/0.058 | −3.16/−1.50/+0.06 | 28.88/3.12/0.045 |
| TinyStories | 33.72/5.01/0.006 | 26.53/2.13/0.006 | −7.19/−2.88/−0.04 | 26.57/2.23/0.006 |
| 1BW | 9.53/0.45/0.006 | 9.51/0.34/0.005 | −0.01/−0.11/−0.05 | 9.60/0.32/0.006 |

时延：CFG-off 实测 0.30 s/样本 vs CFG-on 0.55（NFE 11+44 vs 22+88）。

**三个结论（邮件的两问 + 一个赠品）**：
1. **CFG 本身是真实的推理侧质量杠杆**：同 ckpt 关 CFG 掉 R1 3.2~7.2——
   不是温度/top-p 没调好的补偿。
2. **重训 + 重调也追不回**：full-recipe no-CFG 与同 ckpt 关 CFG **逐格重合
   （差 ≤0.1）**——cond_drop 训练零成本、无 CFG 专属训练收益为零，
   全部价值在推理侧 null-分支引导。
3. 赠品：**1BW（短 prompt 域）对 CFG 完全不敏感**（Δ≈0）——唯一可以免费
   砍半 NFE 的 benchmark；与"prompt 信息占比低时引导无物可导"一致。

命名遵照需求：本节 = Controlled inference-time CFG ablation；
§8.2 更名为 Full-recipe no-CFG comparison（结论不变，措辞已改）。

### 8.4 2D 家族 2×2 补全（2026-09-11）：α×CFG 的替代交互在 2D 链复现

| 12.8B 2D M8（C2K2） | wiki | WS | TS | 1BW |
|---|---|---|---|---|
| **CFG + token（注册点）** | **29.00/4.43**/0.140 | **32.01**/4.66/0.058 | **33.72**/5.01/0.006 | 9.53/0.45/0.006 |
| CFG + α | 28.60/4.50/0.122 | 31.35/4.67/0.072 | 33.29/**5.44**/0.006 | 9.87/0.48/0.006 |
| 无 CFG + token | 24.81/2.95/0.088 | 28.88/3.12/0.045 | 26.57/2.23/0.006 | 9.60/0.32/0.006 |
| 无 CFG + α | 25.86/3.46/0.110 | 30.26/3.81/0.052 | 28.90/2.96/0.006 | 10.50/0.56/0.006 |

**交互效应（wiki R1）**：α 在无 CFG 下 +1.05、在有 CFG 下 −0.40——损失重加权
与 CFG 是替代品的结论在 2D 链上复现（§8.2 段轴同款）。注册点维持
CFG+token。附：CFG+α 的 sel WS MAUVE 34.76（家族 sel 历史最高）未迁移到
test（7.25）——sel 单格 MAUVE 海市蜃楼案例 +1。
