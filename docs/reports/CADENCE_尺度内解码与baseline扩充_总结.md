# CADENCE：尺度内解码顺序三实验 + baseline 扩充 —— 总结（2026-09-02/03）

独立可读的汇总。逐章过程与原始数字见 `CADENCE_2B调优波报告.md` 终章五~十一；
本文只给结论、证据链和可写进论文的主张。

**所有原始数字都在仓库里**：`results/benchgen_*/` 下是每个臂每个 benchmark 的
`*.metrics.json`（test 与 sel 都有）；机器生成的全量对照表见
[MASTER_TABLES.md](MASTER_TABLES.md) + `results/master_table.csv`（44 臂 156 格，
每格可溯源到具体文件），生成脚本 `tools/build_master_table.py`。本文引用的数字
若与之不符，以脚本生成的为准。

---

## 0. 一句话

在严格同预算（2B 梯度 token、同数据、同 12L×768 主干）下，**段轴置信度序解码
（segment-wise MaskGIT）成为新主行：Wikipedia 的 R1 与 MAUVE 同时超过满预算的
BD3-LM，而 backbone 前向次数只有 22 次，是 BD3 的 1/46.5**；位置轴的三种顺序机制
（MaskGIT、chunk=1 AR、约束式左到右）在当前分辨率下**都测不出收益**。
（**2026-09-04 更正**：原文写的是"都无效，且已排除采样头容量不足这一解释"——
"已排除容量"那句已撤回，见 §1.2；"无效"应降级为"测不出收益"，见 §8.1。）

---

## 1. 三个实验的答案

采样有两个正交轴：**位置轴**（一个尺度内的 l 个位置）× **段轴**（一个位置内的
S=4 个 PQ 段）。三个实验分别测这两个轴上的顺序机制。

| 实验 | 机制 | 结论 |
|---|---|---|
| EXP-1 | 段轴 MaskGIT（置信度序） | **有效，成为新主行** |
| EXP-2 | 位置轴 MaskGIT（置信度序） | 测不出收益（~~且与采样头容量无关~~ **撤回**，§1.2） |
| EXP-3 | 位置轴 chunk=1 AR（严格左到右） | 测不出收益（原写"三者中最差"，§1.3 已收回） |

### 1.1 EXP-1：段轴是唯一有效的尺度内顺序机制

同权重（`_b2sg`）、唯一变量是解码顺序：

| 解码 | sel wiki R1/R2/MAUVE | sel WS MAUVE |
|---|---|---|
| 段并行 | 21.89/2.05/7.16 | 11.41 |
| 段 MaskGIT K=2 | 21.93/2.23/15.58 | 10.95 |
| 段 MaskGIT K=3 | 21.99/2.31/16.25 | 9.11 |
| **段 MaskGIT K=4 = S** | 22.33/2.33/**22.89** | **20.42** |

净效应 **+15.73 sel wiki MAUVE**。K 曲线单调，且在 **K=S=4（每轮只承诺一个段，
完全顺序化）** 有一个大跳跃——段轴要的是完全顺序化的极限，不是"少数几轮迭代"，
所以帕累托上没有更便宜的甜点。代价是**零额外 backbone 前向**（22 次不变，仅加
88 次 2 层浅前向 = +6.5% FLOPs）。

### 1.2 EXP-2：采样头容量不是原因 —— **本节结论已于 2026-09-04 撤回**

> **⚠️ 撤回。** 下表两行既不是同 NFE、也不是只差采样头。`pos:8,9,10:8` 是 **22** 次
> backbone 前向，`REFINE 8,9,10:8` 是 **64** 次（`_refine_scale` 每轮重跑整条 12 层
> trunk）。而且"25,345 参数的小头"把揭示码注入在 **trunk 输入端**，它的条件网络是
> 整个 85M trunk——赢的那一行拥有严格更多的条件计算量，这正是容量假设的**预测**。
> 详见 `CADENCE_2B调优波报告.md` 终章十一 §6.2。

| 采样头 | sel wiki MAUVE | sel WS MAUVE | 实际 backbone 前向 |
|---|---|---|---|
| 新 STAR 式采样器（4.16M） | 13.14 | 10.22 | **22** |
| 旧 linear+gate（25,345 参数） | **15.79** | **15.93** | **64** |

原结论"把采样头放大 164 倍位置轴反而更差，所以不是容量问题"**不成立**：两行差 2.9 倍
条件计算量，且 wiki 的 −2.65 落在 n=250 抽样噪声内（sd 3.2~5.3）。

### 1.3 EXP-3：位置轴上越顺序化越差（**已被 §8.3 部分收回，见该节**）

test 完整排序（R1/R2 三者几乎无差，23.2~23.4 / 2.3~2.5）：

**段轴 11.96 > 位置轴 MaskGIT 11.11 > chunk=1 位置 AR 8.23**（wiki MAUVE）

> **2026-09-03 更正**：位置轴内部这两档（11.11 vs 8.23）的差距落在 MAUVE 估计量的
> 不稳定区间内——§8.3 用同一份数据换簇数直接测到过排序翻转。**段轴 vs 位置轴**的
> 差别仍然稳固（另有耦合探针与 K 曲线两条独立支撑），但位置轴内部的排序请勿引用。
>
> **2026-09-04 再更正**：上面括号里的"耦合探针"支撑**已撤回**（§2a）。段轴 vs 位置轴
> 现存支撑是 K 曲线单调性 + §8.1 的两 sel 集一致性。另外，`ar:8` 的撤回理由本身是
> 反的：`_b2sp` 实际训的是 `sampler_mix`，`sampler_causal` 恰是其一半，所以 `ar:8`
> 是位置轴两行里**匹配得更好**的那行（终章十一 §6.1）。

工程侧记：完整 `ar:8,9,10` 在本 harness 不可行——KV-cache 已把采样器 token-
forward 降 768×，但每位置的 aten 调度数几乎未减，GPU 上是 launch-bound，250 行
75.6 分钟未完成即被平台终止（两次复现）。注册行为单尺度 `ar:8`。这是工程限制，
不是机制结论，如实分开陈述。

---

## 2. 机制：为什么段轴赢、位置轴怎么都不行

两条**零训练、模块无关**的独立证据。

**(a) 耦合探针**（只测冻结 tokenizer + 解码器：把某尺度 p% 的位置在 batch 内
roll 打乱、保持边缘分布，看重建 token 准确率掉多少）：

| 尺度 | p=5% | p=10% | p=20% | p=40% |
|---|---|---|---|---|
| 8 (l=256) | 0.34pp | 0.73pp | 1.62pp | 4.42pp |
| 9 (l=512) | 0.50pp | 1.24pp | 2.92pp | 7.91pp |
| 10 (l=1024) | 0.23pp | 0.52pp | 1.32pp | 4.33pp |

对照本项目早先的**段间**耦合探针：最坏 **25.1pp**（正是它当初促成 depth-AR）。

> **⚠️「段间耦合比位置间耦合强一个数量级」已于 2026-09-04 撤回，并已补测。**
> 原对比不可并读：25.1pp 是 `all_independent`（**100%** 码槽），1.3~2.9pp 是位置探针
> p=20%（**20%** 码槽）。补测（metric D，同数据同解码器同估计量，按相同码槽比例 ×
> 两种 donor 结构交叉）给出的答案是：**两个轴之间不存在耦合不对称。**
>
> | 码槽比例 | 25% | 50% | 75% | 100% |
> |---|---|---|---|---|
> | pos/seg 比值（尺度 8/9/10） | 3.15/3.26/2.82 | 1.66/1.65/1.56 | 1.17/1.16/1.15 | **0.99/1.03/1.01** |
>
> 比值单调衰减到 **1.00**；donor 结构几乎不起作用（24 格里 |共享−独立| 最大 0.668pp）；
> 低比例端反倒是**位置轴更强**。所以"差一个数量级"**全部**是扰动质量的产物。
> **段轴赢这个现象仍在（见 §8.1 的三方 test 终表），但这个机制解释作废。**
> 详见 `CADENCE_2B调优波报告.md` 终章十一 §6.3。

**(b) masked vs next-scale 预测准确率**（reveal=50%，对标 HMAR App. D.3 的
5% → 65%）：最细尺度 0.277 → 0.350（+7.3pp），尺度 9 +4.5pp，尺度 8 +1.4pp，
粗尺度为负。**HMAR 在图像域报告的巨大 masked-gain 在文本 + 残差 PQ 上不成立。**
（保留：masked 一列经 `_b2sq2` 的 visible 通路测得，该 ckpt 的 `visible_gate`
仅 0.055，通路训练很弱；耦合探针不经过 planner，无此保留。）

---

## 3. 效率：从"更便宜"升级为帕累托支配

按 advisor 的要求把 baseline 的推理步数压到与我们相同（BD3 用
`sampling.first_hitting=False` + `num_steps`，MDLM 用 T）：

| 模型 | NFE | wiki R1 | wiki MAUVE |
|---|---|---|---|
| BD3-LM 满预算 | 1024 | 23.2 | 11.39 |
| BD3-LM 限步 | 256 | 18.2 | 0.57 |
| BD3-LM 限步 | 128 | 11.7 | 0.46 |
| **BD3-LM 限步** | **64** | **16.1** | **0.58** |
| MDLM 限步 | 64 | 18.5 | 5.63 |
| **CADENCE `_finalSEG`** | **22** | **23.4** | **11.96** |

**同样 64 次前向，BD3 的 wiki MAUVE 是 0.58，我们是 11.37~11.96**；而我们 22 次
前向就达到 BD3 满 1024 次的质量。**任何预算点上都没有 baseline 落在我们右上方。**

必须披露：BD3 训练时 block_size=16，压到每块 1~4 步是在其设计区间之外；限步曲线
**非单调**（NFE 128 比 NFE 64 更差），说明该区间本身不稳定。

另外一条与质量结果无关、恒成立的收获：STAR 式采样器把精化从"每轮重跑 12 层 ×
3071"改成"主干每尺度只算一次 + 2 层浅网络迭代"，**注册行的 backbone forward
从 64 降回 22，对 BD3 的优势从 16.0× 变成 46.5×**，且缓存是逐比特精确的
（`_assemble` 在不用输入侧通路时是纯函数、`maps` 在循环内不变）。

---

## 4. baseline 扩充：三个新家族在 2B 下全部退化

同数据、同 bins、同 12L×768 主干、同 2B 预算，自建最小实现：
**CMLM/Mask-Predict**（MaskGIT 的文本直系祖先）、**SSD-LM**（半自回归 simplex
扩散）、**CADENCE-LDM**（在我们自己的冻结 PQ 隐空间做高斯扩散）。

| 模型 | wiki R1/R2/MAUVE | distinct-2 | prompt 复制率 | 症状 |
|---|---|---|---|---|
| SSD-LM T=10 | **24.2/3.2**/0.50 | 0.886 | 6.8% | 高频功能词汤 |
| CADENCE-LDM 64 步 | 20.2/1.5/0.59 | 0.864 | 1.9% | 词沙拉 + token 复读 |
| CMLM T=64 | 11.6/1.7/0.88 | 0.353 | 14.4% | 严重退化 |
| TextLDM 架构复现（w=7, 50 步） | 10.6/0.2/0.42 | 0.985 | 0.1% | 高熵词碎片 |

**方法论发现（对论文有独立价值）**：SSD-LM 拿到**全表最高的 ROUGE-1**（四个
benchmark 全部第一，1BW 14.4 远超 BD3 的 9.8），而它生成的是语法破碎的功能词汤、
MAUVE 全在 0.5 地板，且**没有抄 prompt**（bigram 复制率仅 6.8%）。高 R1 完全来自
"the/of/and/a"这类高频词与参考文本的单元重合。

**追加（2026-09-03）：TextLDM 架构复现给出第四种退化形态，并支撑一个路线性结论。**
按论文架构自建（Transformer VAE 1:1 token→latent dim 64 + CE/KL/REPA，流匹配 DiT
不注入 timestep、序列拼接条件、50 步 Euler、CFG w=7），VAE 39.3B（与我们冻结
tokenizer 逐字节同额度）+ DiT 精确 2.0002B。结果 wiki 10.6/0.2/0.42，distinct-2
0.985、prompt 复制率 0.1%——既不复读也不抄 prompt，而是高熵词碎片。
关键在于我们现在有**两个隐空间来源完全不同的连续隐扩散实现**（CADENCE-LDM 借用
我们的冻结 PQ 隐空间；TextLDM 复现自学连续 VAE 并带 REPA 蒸馏），**都退化**；
而在**同一个冻结 PQ 隐空间上换成离散的下一尺度自回归，MAUVE 从 0.59 跳到 11.96**。
即：退化不是某个实现的缺陷，而是**连续隐扩散在 2B 预算档上的路线性问题**。
必须同时声明：论文的 DiT 预算是我们的约 800 倍，本结论**只对 2B 档成立，不能推论
"TextLDM 方法不行"**；失效点已定位在扩散阶段中段的速度场拟合（mse 中段自 step
2000 起锁死在 1.85，平凡解为 2.0），REPA 与 VAE 重建本身都正常。

结论：①这四家在 2B 下不构成有竞争力的 baseline，应作"预算下限示范"报告；
②**可比的流畅系统只有 AR / MDLM / BD3-LM / CADENCE 四家**，主表以它们为准；
③**只报 ROUGE 的对比不安全**，MAUVE 正确地把三个退化家族全判到地板。

---

## 5. 论文可用的三条主张

1. **同预算对 BD3-LM 的双超 + 46.5× 便宜**：`_finalSEG` 在 Wikipedia 的 R1
   （23.39 vs 23.2）与 MAUVE（11.96 vs 11.39）同时超过满预算 BD3-LM，R1 在
   四个 benchmark 里三个全场第一，backbone 前向 22 vs 1024。
2. **同 NFE 下 baseline 坍塌 → 帕累托支配**：把 BD3/MDLM 压到我们的前向预算，
   wiki MAUVE 掉到 0.46~5.63；不存在落在我们右上方的 baseline 配置。
3. **段轴解码有效**（原文写的是"段轴 vs 位置轴的机制二分"，**2026-09-04 降级**）：
   原本引的三条证据里，采样头容量实验（§1.2）和耦合探针（§2a）**都已撤回**。
   现存支撑是：段轴 K 曲线单调性、段轴在**两个** sel 集上同时最高而 chunk 轴两集
   反号（§8.1）、`_finalSEG` 对 BD3 的 test 双超（绝对数），以及 HMAR 的 masked-gain
   在我们这个 regime 不迁移。**正面主张"段轴解码有效"不受影响；被撤回的是
   "因为段间耦合远强于位置间耦合"这个机制解释，以及"位置轴无效"这个负面主张的
   强度——后者应降级为"在当前分辨率下测不出收益"。**

---

## 6. 必须披露的保留

- **sel→test 的 MAUVE 历史上反相关过**（三个注册臂 Spearman = −1），且
  `eval_generation.py` 的 MAUVE `num_buckets='auto'` = n/10，sel 用 ~25 簇、
  test 用 ~100 簇——sel 与 test 的 MAUVE 不可放进同一句话。
- **BD3 限步在其设计区间之外**，且限步曲线非单调。
- **WikiSource 的 MAUVE 仍落后**（`_finalSEG` 5.10 vs BD3 7.67）。
- **1BW 的 R1 仍差约 1 分**（9.28 vs 9.8）——窗口规划范式对单句续写的固有税。
- **退化 baseline 的 ROUGE 不可与流畅系统并读**（见 §4）。
- **两阶段口径**：CADENCE 的冻结 tokenizer（60M，150k 步 = 39.3B token）在 2B
  预算之外，单独披露；CADENCE 的 planner 总参 88M 看似小于 baseline 的 124M，
  是因为它不带 50257 词嵌入/输出头——主干层面（85M 非嵌入）全家族一致。
- **行分片改造后**，改动前注册的 test 行任何新跑都不可逐比特复现（一个采样流不能
  既依赖前面所有行、又与分片无关）；已注册指标作为测量仍然有效。

---

## 7. 产物

**代码**（commit 8e9e131 起）：
- `models/sampling_transformer.py` —— STAR 式独立采样器（2L×384，4.16M）；输出
  768 维残差加到现有 per-scale head 的输入状态，零初始化即精确 no-op
- `models/prefix_planner.py` —— 三个解码模式 `pos` / `seg` / `ar`；位置轴走
  depth-AR 链、段轴 depth 冻结（两轴正交）；残差按尺度门控使训练读出 == 推理读出
- `finetune_prefix_maskgit.py` —— `sampler_pos` / `sampler_seg` / `sampler_causal`
  / `sampler_mix` 四种掩码模式
- `tools/diagnose_intra_scale.py` + `jobs/diag_entry.sh` —— 零训练机制诊断
- `models/cmlm_baseline.py` / `models/ssdlm.py` / `models/latent_diffusion.py`
  / `models/textldm_{vae,dit}.py` —— 四个新 baseline 家族
- `tests/test_sampling_transformer.py` —— 门禁测试，含"训练读出 == 推理读出"
  （在两个缺陷各自单独存在时都会失败）与 KV-cache 逐比特等价

**checkpoint**（Volume `checkpoints/`）：`planner_prefix_owt2_pqsh_b2sp`（位置轴臂）、
`_b2sg`（段轴臂）、`_b2sq1/_b2sq2/_b2mgd/_b2pl/_b2mg3/_b2ck/_b2nd`（此前各臂）、
`cmlm_owt2`、`ssdlm_owt2`、`ldiff_owt2_pqsh`、`textvae_owt2_g2`、`textldm_dit_owt2`

**结果**：`results/benchgen_*/` 全部 sel 与 test 的 JSONL + metrics.json；
`results/diagnostics/diag_sq2.json`

**TextLDM 架构复现（已完成，commit b71357e）**：Transformer VAE `textvae_owt2_g2`
（150k 步 = 39.3B，teacher=gpt2，repa_cos 0.931）+ 流匹配 DiT `textldm_dit_owt2`
（精确 2.0002B）→ sel（w=7/3/1 全在 MAUVE 地板）→ test（wiki 10.61/0.23/0.42）。
定位是"**架构忠实、2B 预算的复现**"，不是"复现了 TextLDM 的结果"（论文 DiT 预算
是我们的约 800 倍）。半程的 gpt2-xl teacher 版 `textvae_owt2`@13400 步保留，
可作 teacher 规模消融。详见调优波报告终章十。

---

## 8. 追加波（2026-09-03）：约束式左到右解码 + HMAR 尺度重加权，两组皆负

逐章数字见 `CADENCE_2B调优波报告.md` 终章十一。这里只给结论。

### 8.1 P0：位置轴/chunk 轴上的约束式左到右 —— 测不出效应

把「全并行」和「严格逐位置 AR」之间连续插值（l 个位置切 C 个 chunk 左到右承诺、
chunk 内跑 K 轮 MaskGIT），扫 C∈{2,4,8,16}×K∈{1,4}，8 格共用同一 checkpoint。

**第一轮跑错了，必须先说。** 8 格扫描与其 test 一枪都跑在 `_b2sp` 上，而 `_b2sp`
的采样器有一半的 micro-batch 走 `sampler_pos`（均匀随机揭示；**更正**：`_b2sp` 实际
训练模式是 `sampler_mix`，不是 `sampler_pos`，见终章十一 §6.1）；`lr` 解码访问的是"左侧整块已承诺 +
当前 chunk 部分揭示"这类状态，实测在 q512/q1024 上均匀随机揭示里**一个都不出现**
（0/48174、0/48810）。**训练/推理不匹配 → 那 9 个测量（8 条 sel + `_finalLR`）全部
作废**，「左到右更差」不是机制结论。根因是代码里 `pos`/`seg`/`ar` 三种解码各有对应
掩码模式，我加了第四种解码 `lr` 却没加第四种掩码模式。

**更正轮：补 `sampler_lr` 掩码模式 + 两个新臂 + 交叉表。** 新臂
`b2slr`（监督=all）/`b2slr2`（监督=chunk）与位置轴臂 `b2sp` 同父（`b2sq1`）、同
1000 步、同为精确 7630 步 = 2.0002B，且**三者只差 mask_mode**（都 `depth_ar=true`
→ `depth_projs` 可训，因为 `pos`/`lr` 都经 `_depth_draw` 读出）。**主线 `b2sg` 不在
这个单变量组里**：`sampler_seg` 会无条件冻结深度头，所以它与这三个臂差两件事，
`lr` 的数字**不可**与主线段轴的 22.89 直接并读。按预注册规则读对角线（每臂用自己训
过的解码，sel_wikipedia MAUVE）：

| 各自匹配的配置 | sel_wiki MAUVE | 对 `pos` 的 Δ | 判读 |
|---|---|---|---|
| `pos` K=8（对照） | 13.14 | — | — |
| `lr` C16K4（监督=all） | 12.25 | −0.89 | 门槛内，无信息 |
| `lr` C16K4（监督=chunk） | 11.79 | −1.35 | 门槛内，无信息 |

三个配置全在 ±2.5 无信息带内，而 n=250 的 bootstrap sd 是 3.2~6.0——**不可分辨**。
两个 sel 集还给出相反顺序（wikisource 上 `lr`+chunk 16.95 > `pos` 10.22），本身就是
"该指标在这个分辨率上没信息"的旁证。顺带否证了我自己的第二个假设：严格匹配的
`chunk` 监督（11.79）与 `all`（12.25）差 0.46，深在噪声里。

**chunk 数扫描（补做，2026-09-04）——需求明写的一项。** 更正轮只跑了 C=16,K=4 一个
点，而这个 C 还是从作废的扫描里挑的。补做在 `b2slr3`（`sampler_lr`+监督 chunk+
**`depth_ar=false`**）上，对照 `b2spf`（`sampler_pos`+`depth_ar=false`）。换 depth 冻结
的臂有两个理由：一是最严格地满足"不叠段轴"（深度头冻在零 → `state_s == h`，连固定序
depth-AR 链都关掉，唯一结构就是 chunk 轴约束），二是与主线 `b2sg` 变成**单变量可比**。
K 固定为 1，因为训练用 `--chunks 16`，K=1 时 chunk 内不部分揭示，C∈{2,4,8,16} 全在
训练分布内；C<16 配 K>1 会是又一次训练不匹配，所以 K=4 只在 C=16 上报。

| 解码（sel n=250） | wiki MAUVE | 排名 | WS MAUVE | 排名 |
|---|---|---|---|---|
| `lr` C=2 K=1 | 16.86 | 2 | 14.62 | 3 |
| `lr` C=4 K=1 | 9.35 | 5 | **22.40** | 1 |
| `lr` C=8 K=1 | **21.88** | 1 | 9.76 | 5 |
| `lr` C=16 K=1 | 16.22 | 3 | 12.40 | 4 |
| `lr` C=16 K=4 | 10.55 | 4 | 15.17 | 2 |
| `pos` K=8（对照） | 18.35 | — | 12.94 | — |

**两个 sel 集把 chunk 数排成了几乎相反的顺序，Spearman ρ = −0.90。** 表面赢家完全由
"取 5 个噪声抽样的最大值"解释：5 格均值 14.97、sd 5.10（与实测 bootstrap sd 3.2~5.3
同量级），E[max of 5]=20.90，实测 21.88。而在方差小一个量级的指标上，对照 `pos` 在
wiki 的 R1/R2/RL/BERTScore/distinct-2 **五项全部第一，五个 lr 格无一超过**。

**与主线段轴单变量并读**（`b2slr3` 与 `b2sg` 现在只差 mask_mode，都 depth 冻结）：

| 配置 | wiki MAUVE | WS MAUVE | wiki R1 | wiki BERT |
|---|---|---|---|---|
| **段轴** `b2sg`+`seg:all:4` | **22.89** | **20.42** | **22.33** | **77.79** |
| 位置轴 `b2spf`+`pos:8` | 18.35 | 12.94 | 22.15 | 77.73 |
| chunk 轴 `b2slr3`+`lr` 最好格 | 21.88（C=8） | 22.40（C=4，**不同格**） | 21.88 | 77.61 |

段轴在**两个 sel 集上同时最高**，chunk 轴的"最好"在两个集上是两个不同的 C。
**两集一致 = 信号，两集反号 = 噪声**——这条判据把段轴和 chunk 轴分得很清楚。

**test 三方终表（2026-09-04，4×1000，R1/R2/MAUVE ×100）。** 三臂同父、同 7630 步、
**depth 全冻结**、同 p5hot7 —— 本项目第一次把三个尺度内解码轴放在完全相同的单变量
底座上：

| benchmark | 段轴 `seg:all:4` | 位置轴 `pos:8` | chunk 轴 `lr:8:1` |
|---|---|---|---|
| wikipedia | 23.39/2.51/**11.96** | 23.06/2.30/10.02 | 22.96/2.27/9.34 |
| wikisource | 29.12/3.26/**5.10** | 28.88/3.11/2.66 | **29.16**/3.01/4.37 |
| tinystories | **27.60**/2.42/**0.59** | 27.28/2.26/0.57 | 26.71/2.17/0.55 |
| 1BW | **9.28**/0.33/**0.64** | 8.86/**0.34**/0.56 | 8.79/0.22/0.60 |

**段轴四个 benchmark 的 MAUVE 全部第一**；位置轴与 chunk 轴之间**符号在四个集上换了
两次**（wiki 位置轴高 0.68、WS chunk 轴高 1.71、TS 高 0.02、1BW 高 0.04），与 sel 的
ρ=−0.90 完全一致。这张表不依赖任何已撤回的对照，是段轴主张现存最强的单变量证据。

**最终结论**：约束式左到右**测不出收益**（不是"更差"——两者可写强度完全不同），
而它贵 8 倍（每尺度 128 vs 16 次采样器前向）。据此关闭，理由是**同预算测不出收益 +
代价 8 倍**（~~机制上位置间耦合仅 1.3~2.9pp（段间 25.1pp）~~ 这条理由 2026-09-04
撤回，见 §2a）。按需求「confirm 之前不要
叠段轴」，**2D 嵌套设计（chunk 轴左到右 × chunk 内段轴置信度）不启动**。

**§1.3 那句"位置轴上越顺序化越差"应视为未定**——位置轴内部三档都落在同一估计量的
不稳定区间内，且其中 `ar:8` 那一档同样跑在 `_b2sp` 上（它的匹配模式本应是
`sampler_causal`），带同类污染。稳固的只有**段轴 vs 位置轴**的差别，它有耦合探针
~~（25.1pp vs 1.3~2.9pp）~~ **此条 2026-09-04 撤回（§2a）**与段轴 K 曲线单调性
两条独立支撑；现存支撑为 K 曲线单调性 + §8.1 的两 sel 集一致性。

### 8.2 P1：HMAR §4.3 尺度重加权 —— 机制生效，但代价放错了地方

难度曲线复现出 HMAR 的驼峰形（最难在 q32，minCE 5.739），拟合 log-normal
μ=1.98 σ=0.50，与难度相关 **+0.935**；而默认按 token 平均的 CE 与难度
**反相关 −0.369**（一半损失质量在最容易的 q1024）。所以先验上这个改动完全该做。
对应 HMAR Fig. 12 的图见 [docs/figures/scale_difficulty.svg](../figures/scale_difficulty.svg)。
（一处刻意偏离：HMAR 说 min **test** CE，我们用 min **val** CE——拿 test split 拟合
损失权重就是预注册协议禁止的测试集泄漏。详见终章十一 §2.1 的说明框。）

两波各三臂（波 A：7630 步单跑；波 B：完整课程链 5630+1000+1000），唯一变量是权重：

**主线（波 B = 逐段复刻主线课程链，解码 `seg:all:4`）的 test 一枪才是答案：**

| benchmark | 主线 token（`_finalSEG`） | lognormal 全链 |
|---|---|---|
| wikipedia | 23.39/2.51/**11.96** | 25.01/2.77/**6.82** |
| wikisource | 29.12/3.26/**5.10** | 30.32/3.37/**2.99** |
| tinystories | 27.60/2.42/0.59 | 28.46/2.41/0.55 |
| 1BW | 9.28/0.33/0.64 | 9.49/0.41/0.62 |

**四个集 R1/R2 全涨、MAUVE 全跌**，wiki 砍掉 43%。换簇数稳健性检查方向一致
（100 簇 +5.14、25 簇 +6.16），与 P0 相反——**P1 的结论稳**。

隔离实验（非主线，仅供分解机制）：

| 波 | 对照(token) | equal | lognormal |
|---|---|---|---|
| A test wiki MAUVE（**朴素基座，无 MaskGIT**） | **8.19** | 5.24 | 5.59 |
| B sel wiki MAUVE | **22.89** | 4.20 | 10.52 |
| ladder 总 bits（低=好） | **35362 / 36630** | +4543 / +4871 | +3688 / +3665 |

失效点定位得很干净：重加权**确实**让 11 个尺度里 9 个变好（含最难的 q32
−0.078 bits），但 **q1024 退化 0.790 bits**——而 q1024 独占 ladder 2047 个位置里的
1024 个，于是总描述长度反而恶化 10~13%（3.0~4.7σ）。**难度最高的尺度恰恰是位置最少
的尺度**，按难度加权正好把权重从「多而易」搬到「少而难」。

下一步不是放弃这条线，而是把位置数放回权重函数：w(k) ∝ (l_k/Σl)^α·lognormal(k)^(1−α)
的一维扫描，或只给 q512/q1024 设权重地板。

### 8.3 方法学：sel 选择协议对 MAUVE 不成立（本波最值钱的产出）

一天两次 sel→test 反转，我用 `tools/mauve_variance.py` 直接去量（不重新生成，只把
已有的 1000 行 test 生成反复重打分），结论**和"噪声"这个直觉解释不同，而且两次反转
成因不一样**：

| 配置 | n=1000, 100 簇（上报值） | 同数据, 25 簇 | 250 行 bootstrap sd |
|---|---|---|---|
| P0 位置轴 MaskGIT | **11.11** | 16.65 | 5.34 |
| P0 约束左到右 | 8.45 | **18.36** | 4.21 |
| P1 hw76tk（token） | **8.19** | **11.56** | 3.77 |
| P1 hw76ln | 5.59 | 7.21 | 3.17 |
| P1 hw76eq | 5.24 | 7.44 | — |

1. **P0 的反转是估计量造成的，不是噪声。** `eval_generation.py:55` 把 MAUVE 的
   `num_buckets` 留给库的 `'auto'` = n/10，所以 sel 用 ~25 簇、test 用 ~100 簇。把
   **同一份 1000 行 test 数据**改用 25 簇重算，**排序直接翻转**（位置轴 16.65 <
   左到右 18.36）。原因是粗化的抬升**依臂而异**（左到右 +9.91、位置轴 +5.54），
   所以这个变换**不保序**——"簇数只是给所有臂加同一偏移故不影响排序"这个看似严密的
   辩护被实测否证。P0 的 sel 其实**没测错**，它忠实测到了"25 簇下左到右更高"。
2. **P1 的反转是噪声，但结论对估计量稳健。** hw76tk 在 100 簇和 25 簇下**都赢**
   （8.19 vs 5.59/5.24；11.56 vs 7.21/7.44）。sel 上 ln 领先 +5.29 是运气：用 sel 的
   估计量在 test 数据上看，ln 与 tk 只差 +0.9，而 bootstrap sd 是 3.2~3.8。
3. **n=250 上 MAUVE 的抽样 sd 是 3.2~5.3 分，而我们的判读门槛 |Δ|<2.5 只有
   0.5~0.8σ**——从来没起到它想起的作用。有用的分解：同一份 sel 上做**配对**比较
   （P0 八格共用 checkpoint 与同 250 行）格间 sd 只有 1.95；换一份 250 行则是
   3.2~6.6。**配对比"另找 250 条"强得多。**

因此 **P0 的结论必须弱化**：不能说"按 MAUVE 左到右更差"（符号不稳定）。仍然站得住
的是——词面指标零收益（R1/R2 四个集差都 <0.2）、采样代价 8 倍（128 vs 16 次前向）、
~~机制上位置间耦合只有 1.3~2.9pp（段间 25.1pp）~~（**2026-09-04 撤回，见 §2a**）。
据此关闭该方向。**终章九"位置轴上越
顺序化越差"那句应视为未定**；稳固的只有段轴 vs 位置轴的差别（有耦合探针与 K 曲线
单调性两条独立支撑）。

操作性结论：
- **显式固定 `num_buckets`**，不让它随 n 漂（一行改动；改后新旧 test 行不可混读）。
- **纯解码顺序实验不用 sel**：共用 checkpoint，直接在 test 上做同 prompt 同种子的
  逐行配对比较。
- **训练侧比较改用 ladder 总描述长度**做选择信号（零方差、日志现成），**前提是各臂
  采样分解方式相同**；跨架构会失去分辨力（sh vs hw76tk 的 ladder bits 只差 0.34σ
  却差 1.83 MAUVE）。
- **扫描不做选择**，只看趋势；门槛按比较次数放宽（8 格取最大本身抬升 1.42σ）。
- **凡 MAUVE 结论都附一次换簇数的稳健性检查**。

### 8.4 审计：16 条候选发现全部被推翻，0 条确认

四路并行审计 + 每条发现单独派人对抗性反驳。有价值的三点：重加权实现被逐行验证正确
（权重下标↔尺度↔ladder 分块对齐精确，`token` 就是原 flat CE），所以"lognormal 输"
不是关于 bug 的话；两个足以推翻 P1 的替代解释被日志直接排除（六次评测加载的都是
`ckpt_step7630.pt`；16 卡/32 卡下 `n_accum` 断言保证全局 batch 精确为 256）；以及一条
被"推翻"的发现其实是对的（见 §8.3 第 1 点）——**能测的东西不要用推理定案**。

真实但本波未触发的缺口备查：`--resume auto` 不校验 checkpoint 里的
`train.scale_weight`（而平台推荐"失败就重投续训"）；`benchgen` 不检查训练完成即取最新
checkpoint；`Path.with_suffix` 让含点的 TAG 撞名；sel_* 基准文件的构建命令未记录在
repo 里，"与 test 不重叠"只有文字记录、无法复核。

---

## 9. 追加波（2026-09-04/05）：2D MaskGIT 三重否定关闭 + α 插值成为 ROUGE 侧新注册点

逐章数字见 `CADENCE_2B调优波报告.md` 终章十二。这里只给结论与全家定位。

### 9.1 P0（2D MaskGIT）：负面，方向关闭

mentor 设计的 2D（chunk 轴左到右 × chunk 内段轴置信度 top-k）配了专属训练臂
`b2s2d`/`b2s2dm`（同父 7630 步、揭示模式与解码状态精确匹配、门禁测试钉死）后，
三种设置全部无收益：

| 设置 | 结果 |
|---|---|
| 细尺度-only，三组 iso-NFE（mentor 原规格，48/24 两档） | 档内与纯段轴锚不可分辨，两 sel 集对 C×K 排序反相关；test 符号跨集翻转 |
| **覆盖曲线**（seg K=4 只改尺度集） | **没有任何真子集接近全覆盖**：{0..9}@80=15.62 vs 全覆盖@88=22.89；q1024 最后 8 次前向是最大单跳，但细-only 也只有 13.90 → 收益是全链协同 |
| 覆盖修正后 iso-NFE（88 vs 88、粗带同、同臂） | **2D 仍输**：wiki 15.70 < 19.09、WS 17.84 < 31.11，两集一致 |

**采样器 NFE 的正确花法**：全尺度覆盖不可省（88 里每一段都有贡献），要省只能省
尺度内日程——而尺度内的最优日程就是段轴置信度序本身。

### 9.2 P1（HMAR 权重调整）：α 插值给出 ROUGE↔MAUVE 旋钮，α=0.25 成为新注册点

设计 w_α ∝ token^α · lognormal^(1−α)（两端点=两个已测配置）；三臂 5630 步、
**总 ladder 描述长度做预注册门控**（不碰 MAUVE 不烧 test）：0.75/−80、0.50/+627、
0.25/+1892 bits，全部过线；按"合格取最小 α"选 0.25（1.9σ 压线，0.5 为备选），
补全课程链后一枪 test：

**R1/R2 八格全涨、MAUVE 近平**（wiki −1.68、WS −0.44、TS −0.03、1BW +0.11）。
wiki R1 24.99 ≈ 纯 lognormal 的 25.01，MAUVE 代价只有其 1/3（−1.68 vs −5.14）。

### 9.3 全家定位更新

- **流畅系统 R1 排名（4 benchmark）**：α=0.25 拿下 wiki/WS/TS 三个第一
  （24.99/30.39/29.25），1BW 次于 BD3（9.43 vs 9.82）。R2 同样 3/4 第一。
- **两个注册运行点**（同 22-NFE 架构，只差损失权重）：MAUVE 优先=token 主线
  （wiki 11.96，全家最高）；ROUGE 优先=α=0.25（对 BD3 的 R1 优势 +1.8，NFE 1/46.5）。
- lognormal 端点被两点帕累托支配；equal 全面劣。
- 老短板照旧披露：WS MAUVE、1BW R1 仍落后 BD3。

### 9.4 基建沉淀

混合两组解码（`SAMPLE_MODE='seg:<粗>:K+lrseg:<细>:C:K'`）、`sampler_seg2d` 混合训练模式、
`sampler_lrseg` 2D 训练模式、`interp` 权重族及其门控工具链全部入库；318 份作业提交
记录（`jobs/submitted/`）与全部原始 metrics/生成文本可从仓库直接复核。ELF
（arXiv 2605.10938，最强连续隐空间代表）两臂 2B 训练在跑，落地后并入 §4 的
baseline 家族表。

---

## 10. 终局（2026-09-05）：2D 天花板 + ELF 改变全表格局

### 10.1 2D（lrseg2，chunk 真条件版）最终判读

修好 chunk 依赖后信号真实（两个臂上 2D 最佳格对各自分布内对照均两集同向胜出），
但**天花板 = 用 11.5× 采样 NFE（C16K4@1016）追平主线 seg:all:4@88**（21.01/18.92 vs
22.89/20.42，噪声内）。结论：**chunk 级条件有效但不经济——段轴置信度序在 88 NFE 已
拿到全部收益**。天花板注册数 `_final2Dv2` 在跑。v1（chunk 独立）的三重否定不变。

### 10.2 ELF 正式 test（截断协议 + 重标 EMA）

**ELF pre2 四集 R1/R2 全第一、三集 MAUVE 第一**（wiki 28.35/5.06/26.89），但其随机
encoder 消融全线地板——**差距全部来自 35M 外部预训练 encoder（C4 ~1T token）**。
三重资源不对称（外部权重 / NFE≈128+ vs 22 / T5 词表）进表须脚注。net：CADENCE 保持
"零外部权重、低 NFE 档"最强；ELF 定义"进口表示空间档"上限，其消融反向支撑收窄后的
命题（自含嵌入空间的连续隐扩散 2B 不成立）。方法学：预训练空间当**生成基底**远强于当
**蒸馏目标**（对照 TextLDM 的 REPA 失败）。

两个已修的移植 bug 如实入档（EMA 视界未按预算重标→47% 随机初始化残留；漏了全家的
参考词数截断协议），修复前后数字均保留于 `results/benchgen_elf_*`。
